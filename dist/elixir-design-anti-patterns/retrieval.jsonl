{"references":[],"source_sha256":"a2c5e217c1d0367256752cdfae43e12cfa8d84c62c810e2f661f87b4769f3d27","source_start_line":12,"consequences":"- Each function has one documented return type, and the function name announces which shape the caller gets.\n- Specs narrow from a union-of-all-options into one type per function, so type inference and Dialyzer can check call sites instead of widening every caller to the full union.\n- Callers pattern match the result exhaustively without inspecting which options were passed or where they were set.\n- Options stay available for behavior that does not alter the return type; only the return-type-selecting option is removed.","rule_title":null,"routing_description":"When an option would change a function's return type, split each return shape into its own named function instead of selecting the type by option.","record_id":"elixir-design-anti-patterns:adr-001","applies_to":{"content_match":["options \\\\ []","when is_list(options)","Keyword.get(options,","keyword()) ::"]},"parent_id":null,"decision":"","retrieval_sha256":"1e582c807dbb2892860b960f53ca11e72515ee8b46f8cf54257b91868ad125f5","hydrate_id":"elixir-design-anti-patterns:adr-001","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-001\n\nDomain: elixir-design-anti-patterns\n\nADR: Alternative return types\n\nRouting title: Alternative return types\n\nRouting description: When an option would change a function's return type, split each return shape into its own named function instead of selecting the type by option.\n\nSource description: A function whose options change its return type forces its spec into a union of every possible shape that no caller can narrow, because the option selecting the shape is a runtime value. Give each return shape its own named function so each has a single, statically knowable return type.\n\n## Context\n\nThis anti-pattern is a function that takes an options keyword list where one of those options changes the function's return type. The classic shape is a parser whose `:discard_rest` option flips the result between a bare `integer()` and a `{integer(), String.t()}` tuple.\n\nOptions are optional and are frequently built dynamically: merged from application config, request parameters, or computed defaults. When the value that selects the return shape is itself a runtime value, no caller can statically know which shape comes back without tracing where every option was set.\n\nThe mechanism is type resolution at the call site. Elixir resolves the return shape here at runtime, so the function's typespec degenerates into a union of every shape any option combination can produce (`integer() | {integer(), String.t()} | :error`). Nothing ties a given call site to a single member of that union, so both a human reader and the type checker must treat every call as possibly returning any member. The wider the union, the less a `case` on the result can be made exhaustive, and the more defensive every call site becomes.\n\n## Consequences\n\n- Each function has one documented return type, and the function name announces which shape the caller gets.\n- Specs narrow from a union-of-all-options into one type per function, so type inference and Dialyzer can check call sites instead of widening every caller to the full union.\n- Callers pattern match the result exhaustively without inspecting which options were passed or where they were set.\n- Options stay available for behavior that does not alter the return type; only the return-type-selecting option is removed.\n\nRule headings:\n### Rule 1: One function name per return type","tags":["elixir","anti-pattern","design","api-design","return-types","typespecs"],"rule_number":null,"adr_title":"Alternative return types","languages":[],"source_description":"A function whose options change its return type forces its spec into a union of every possible shape that no caller can narrow, because the option selecting the shape is a runtime value. Give each return shape its own named function so each has a single, statically knowable return type.","ordinal":1,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-001-alternative-return-types.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-001: Alternative return types"],"context":"This anti-pattern is a function that takes an options keyword list where one of those options changes the function's return type. The classic shape is a parser whose `:discard_rest` option flips the result between a bare `integer()` and a `{integer(), String.t()}` tuple.\n\nOptions are optional and are frequently built dynamically: merged from application config, request parameters, or computed defaults. When the value that selects the return shape is itself a runtime value, no caller can statically know which shape comes back without tracing where every option was set.\n\nThe mechanism is type resolution at the call site. Elixir resolves the return shape here at runtime, so the function's typespec degenerates into a union of every shape any option combination can produce (`integer() | {integer(), String.t()} | :error`). Nothing ties a given call site to a single member of that union, so both a human reader and the type checker must treat every call as possibly returning any member. The wider the union, the less a `case` on the result can be made exhaustive, and the more defensive every call site becomes.","source_end_line":71,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":1,"routing_title":"Alternative return types","display_text":"## Context\n\nThis anti-pattern is a function that takes an options keyword list where one of those options changes the function's return type. The classic shape is a parser whose `:discard_rest` option flips the result between a bare `integer()` and a `{integer(), String.t()}` tuple.\n\nOptions are optional and are frequently built dynamically: merged from application config, request parameters, or computed defaults. When the value that selects the return shape is itself a runtime value, no caller can statically know which shape comes back without tracing where every option was set.\n\nThe mechanism is type resolution at the call site. Elixir resolves the return shape here at runtime, so the function's typespec degenerates into a union of every shape any option combination can produce (`integer() | {integer(), String.t()} | :error`). Nothing ties a given call site to a single member of that union, so both a human reader and the type checker must treat every call as possibly returning any member. The wider the union, the less a `case` on the result can be made exhaustive, and the more defensive every call site becomes.\n\n## Consequences\n\n- Each function has one documented return type, and the function name announces which shape the caller gets.\n- Specs narrow from a union-of-all-options into one type per function, so type inference and Dialyzer can check call sites instead of widening every caller to the full union.\n- Callers pattern match the result exhaustively without inspecting which options were passed or where they were set.\n- Options stay available for behavior that does not alter the return type; only the return-type-selecting option is removed."}
{"adr_id":1,"adr_title":"Alternative return types","applies_to":{"content_match":["options \\\\ []","when is_list(options)","Keyword.get(options,","keyword()) ::"]},"date":"2026-06-28","display_text":"### Rule 1: One function name per return type\n\nGive each return shape its own named function. Do not let an option select the return type.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t()) :: {integer(), String.t()} | :error\n  def parse(string) do\n    Integer.parse(string)\n  end\n\n  @spec parse_discard_rest(String.t()) :: integer() | :error\n  def parse_discard_rest(string) do\n    case Integer.parse(string) do\n      {int, _rest} -> int\n      :error -> :error\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t(), keyword()) :: integer() | {integer(), String.t()} | :error\n  def parse(string, options \\\\ []) when is_list(options) do\n    if Keyword.get(options, :discard_rest, false) do\n      case Integer.parse(string) do\n        {int, _rest} -> int\n        :error -> :error\n      end\n    else\n      Integer.parse(string)\n    end\n  end\nend\n```\n\n**Why:** In the wrong version the selecting option (`:discard_rest`) is a runtime value, so neither a reader nor the type checker can narrow the return at a call site. The spec collapses to a three-member union that every caller must defensively destructure, even though any concrete call returns exactly one shape. A `case` on the result cannot be made total, because the compiler cannot rule out the other members. Splitting into two named functions gives each a single, statically knowable return type: the name announces the shape, each spec is a smaller union, and the compiler's type inference can check each call site against one concrete return. Options remain appropriate for behavior that does not change the return type.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name per return type"],"hydrate_id":"elixir-design-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":2,"parent_id":"elixir-design-anti-patterns:adr-001","polarity":null,"record_id":"elixir-design-anti-patterns:adr-001:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"09bd6e74fcc4f9f71277d72dbc1152c6436f9518633edee9959ffc96f5f672c0","retrieval_text":"Record: elixir-design-anti-patterns:adr-001:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Alternative return types\n\n### Rule 1: One function name per return type\n\nGive each return shape its own named function. Do not let an option select the return type.\n\nWhy: In the wrong version the selecting option (`:discard_rest`) is a runtime value, so neither a reader nor the type checker can narrow the return at a call site. The spec collapses to a three-member union that every caller must defensively destructure, even though any concrete call returns exactly one shape. A `case` on the result cannot be made total, because the compiler cannot rule out the other members. Splitting into two named functions gives each a single, statically knowable return type: the name announces the shape, each spec is a smaller union, and the compiler's type inference can check each call site against one concrete return. Options remain appropriate for behavior that does not change the return type.","routing_description":"When an option would change a function's return type, split each return shape into its own named function instead of selecting the type by option.","routing_title":"Alternative return types","rule_number":1,"rule_title":"One function name per return type","schema_version":"retrieval-v1","source_description":"A function whose options change its return type forces its spec into a union of every possible shape that no caller can narrow, because the option selecting the shape is a runtime value. Give each return shape its own named function so each has a single, statically knowable return type.","source_end_line":64,"source_path":"adrs/elixir-design-anti-patterns/adr-001-alternative-return-types.md","source_sha256":"ed8a826358ca989381cf15ab393c8720323d01c6de7f753c6c6876294e92968b","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","design","api-design","return-types","typespecs"],"updated":null}
{"adr_id":1,"adr_title":"Alternative return types","applies_to":{"content_match":["options \\\\ []","when is_list(options)","Keyword.get(options,","keyword()) ::"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t()) :: {integer(), String.t()} | :error\n  def parse(string) do\n    Integer.parse(string)\n  end\n\n  @spec parse_discard_rest(String.t()) :: integer() | :error\n  def parse_discard_rest(string) do\n    case Integer.parse(string) do\n      {int, _rest} -> int\n      :error -> :error\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name per return type","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":3,"parent_id":"elixir-design-anti-patterns:adr-001:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-001:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"c7642653cea32651d4ed986c6da92201f825ccab43437c8aa41ab725bf5abf68","retrieval_text":"Record: elixir-design-anti-patterns:adr-001:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Alternative return types\n\nRouting title: Alternative return types\n\nRule 1: One function name per return type\n\nCorrect example (positive)\n\nGive each return shape its own named function. Do not let an option select the return type.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t()) :: {integer(), String.t()} | :error\n  def parse(string) do\n    Integer.parse(string)\n  end\n\n  @spec parse_discard_rest(String.t()) :: integer() | :error\n  def parse_discard_rest(string) do\n    case Integer.parse(string) do\n      {int, _rest} -> int\n      :error -> :error\n    end\n  end\nend\n```\n\nWhy: In the wrong version the selecting option (`:discard_rest`) is a runtime value, so neither a reader nor the type checker can narrow the return at a call site. The spec collapses to a three-member union that every caller must defensively destructure, even though any concrete call returns exactly one shape. A `case` on the result cannot be made total, because the compiler cannot rule out the other members. Splitting into two named functions gives each a single, statically knowable return type: the name announces the shape, each spec is a smaller union, and the compiler's type inference can check each call site against one concrete return. Options remain appropriate for behavior that does not change the return type.","routing_description":"When an option would change a function's return type, split each return shape into its own named function instead of selecting the type by option.","routing_title":"Alternative return types","rule_number":1,"rule_title":"One function name per return type","schema_version":"retrieval-v1","source_description":"A function whose options change its return type forces its spec into a union of every possible shape that no caller can narrow, because the option selecting the shape is a runtime value. Give each return shape its own named function so each has a single, statically knowable return type.","source_end_line":44,"source_path":"adrs/elixir-design-anti-patterns/adr-001-alternative-return-types.md","source_sha256":"35227360061a15e98667cd69a18e1da8701d7680cbdb4e026cd8dd583052b541","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","design","api-design","return-types","typespecs"],"updated":null}
{"adr_id":1,"adr_title":"Alternative return types","applies_to":{"content_match":["options \\\\ []","when is_list(options)","Keyword.get(options,","keyword()) ::"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t(), keyword()) :: integer() | {integer(), String.t()} | :error\n  def parse(string, options \\\\ []) when is_list(options) do\n    if Keyword.get(options, :discard_rest, false) do\n      case Integer.parse(string) do\n        {int, _rest} -> int\n        :error -> :error\n      end\n    else\n      Integer.parse(string)\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name per return type","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":4,"parent_id":"elixir-design-anti-patterns:adr-001:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-001:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"97ef103153927667160bf1449cb42c88863e60aaa046c4ad7f72e1b2d84590c8","retrieval_text":"Record: elixir-design-anti-patterns:adr-001:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Alternative return types\n\nRouting title: Alternative return types\n\nRule 1: One function name per return type\n\nWrong example (negative)\n\nGive each return shape its own named function. Do not let an option select the return type.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Parser do\n  @spec parse(String.t(), keyword()) :: integer() | {integer(), String.t()} | :error\n  def parse(string, options \\\\ []) when is_list(options) do\n    if Keyword.get(options, :discard_rest, false) do\n      case Integer.parse(string) do\n        {int, _rest} -> int\n        :error -> :error\n      end\n    else\n      Integer.parse(string)\n    end\n  end\nend\n```\n\nWhy: In the wrong version the selecting option (`:discard_rest`) is a runtime value, so neither a reader nor the type checker can narrow the return at a call site. The spec collapses to a three-member union that every caller must defensively destructure, even though any concrete call returns exactly one shape. A `case` on the result cannot be made total, because the compiler cannot rule out the other members. Splitting into two named functions gives each a single, statically knowable return type: the name announces the shape, each spec is a smaller union, and the compiler's type inference can check each call site against one concrete return. Options remain appropriate for behavior that does not change the return type.","routing_description":"When an option would change a function's return type, split each return shape into its own named function instead of selecting the type by option.","routing_title":"Alternative return types","rule_number":1,"rule_title":"One function name per return type","schema_version":"retrieval-v1","source_description":"A function whose options change its return type forces its spec into a union of every possible shape that no caller can narrow, because the option selecting the shape is a runtime value. Give each return shape its own named function so each has a single, statically knowable return type.","source_end_line":62,"source_path":"adrs/elixir-design-anti-patterns/adr-001-alternative-return-types.md","source_sha256":"d97cc07e86f5a4f946e8b8c11ca77a9c87f3615fa7bae79e1339d3536c9761ef","source_start_line":45,"status":"accepted","tags":["elixir","anti-pattern","design","api-design","return-types","typespecs"],"updated":null}
{"references":[],"source_sha256":"672f1dcd22cf7496003e858a3a3bccc6254b186c017eb27afc2bf072e78ab24f","source_start_line":12,"consequences":"- Invalid flag combinations stop being representable, and the hand-written priority logic that used to resolve them disappears.\n- Adding a state is a new atom value and a new clause, not another boolean that multiplies the combination space.\n- `case` over the atom is exhaustive, so the compiler and Dialyzer can flag an unhandled state.","rule_title":null,"routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","record_id":"elixir-design-anti-patterns:adr-002","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"parent_id":null,"decision":"","retrieval_sha256":"b6047ae917d645249600190986450e127cc3f1a01e9943671382d88b16e1104a","hydrate_id":"elixir-design-anti-patterns:adr-002","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-002\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\nRouting title: Boolean obsession\n\nRouting description: When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.\n\nSource description: Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.\n\n## Context\n\nBoolean obsession is using booleans to encode information that a richer type models better. The booleans themselves are not the problem. The problem appears when two or more booleans describe one underlying piece of state with overlapping or mutually exclusive values. N booleans make 2^N combinations representable, but usually only a few are valid. The invalid combinations do not disappear: they have to be excluded by hand, with priority logic that silently picks a winner when two flags are set at once.\n\nA function taking `admin: true` and `editor: true` is the canonical case: two flags configuring one notion of access.\n\nThis is the boolean-typed case of the Primitive obsession anti-pattern; see it for the general rule. The fix is the same: replace the primitives with one type whose values name exactly the valid states. An atom suffices when those states form a flat enumeration; reach for a tuple or other composite type when the collapsed state is structured rather than a flat set of names.\n\n## Consequences\n\n- Invalid flag combinations stop being representable, and the hand-written priority logic that used to resolve them disappears.\n- Adding a state is a new atom value and a new clause, not another boolean that multiplies the combination space.\n- `case` over the atom is exhaustive, so the compiler and Dialyzer can flag an unhandled state.\n\nRule headings:\n### Rule 1: Collapse overlapping booleans into a single atom\n### Rule 2: Prefer a named state atom over a lone boolean that will grow","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"rule_number":null,"adr_title":"Boolean obsession","languages":[],"source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","ordinal":5,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-002: Boolean obsession"],"context":"Boolean obsession is using booleans to encode information that a richer type models better. The booleans themselves are not the problem. The problem appears when two or more booleans describe one underlying piece of state with overlapping or mutually exclusive values. N booleans make 2^N combinations representable, but usually only a few are valid. The invalid combinations do not disappear: they have to be excluded by hand, with priority logic that silently picks a winner when two flags are set at once.\n\nA function taking `admin: true` and `editor: true` is the canonical case: two flags configuring one notion of access.\n\nThis is the boolean-typed case of the Primitive obsession anti-pattern; see it for the general rule. The fix is the same: replace the primitives with one type whose values name exactly the valid states. An atom suffices when those states form a flat enumeration; reach for a tuple or other composite type when the collapsed state is structured rather than a flat set of names.","source_end_line":79,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":2,"routing_title":"Boolean obsession","display_text":"## Context\n\nBoolean obsession is using booleans to encode information that a richer type models better. The booleans themselves are not the problem. The problem appears when two or more booleans describe one underlying piece of state with overlapping or mutually exclusive values. N booleans make 2^N combinations representable, but usually only a few are valid. The invalid combinations do not disappear: they have to be excluded by hand, with priority logic that silently picks a winner when two flags are set at once.\n\nA function taking `admin: true` and `editor: true` is the canonical case: two flags configuring one notion of access.\n\nThis is the boolean-typed case of the Primitive obsession anti-pattern; see it for the general rule. The fix is the same: replace the primitives with one type whose values name exactly the valid states. An atom suffices when those states form a flat enumeration; reach for a tuple or other composite type when the collapsed state is structured rather than a flat set of names.\n\n## Consequences\n\n- Invalid flag combinations stop being representable, and the hand-written priority logic that used to resolve them disappears.\n- Adding a state is a new atom value and a new clause, not another boolean that multiplies the combination space.\n- `case` over the atom is exhaustive, so the compiler and Dialyzer can flag an unhandled state."}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"### Rule 1: Collapse overlapping booleans into a single atom\n\nWhen several booleans encode one mutually exclusive state, replace them with one field whose atom values enumerate the valid states.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    case Keyword.get(options, :role, :default) do\n      :admin -> handle_admin(invoice)\n      :editor -> handle_editor(invoice)\n      :default -> handle_default(invoice)\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    cond do\n      options[:admin] -> handle_admin(invoice)\n      options[:editor] -> handle_editor(invoice)\n      true -> handle_default(invoice)\n    end\n  end\nend\n```\n\n**Why:** The two booleans admit four combinations, but only three are meaningful and `admin: true, editor: true` is a contradiction. The `cond` resolves that contradiction by clause order (admin wins), so the invalid state is reachable and the priority rule is invisible at the call site. A single `:role` atom makes only `:admin`, `:editor`, and `:default` representable: the contradiction cannot be constructed, and the `case` is exhaustive over the valid set. The same applies to data: a `User` struct carrying `:admin` and `:editor` booleans should carry one `:role` field, so the struct cannot hold an admin-and-editor record.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Collapse overlapping booleans into a single atom"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":6,"parent_id":"elixir-design-anti-patterns:adr-002","polarity":null,"record_id":"elixir-design-anti-patterns:adr-002:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"9293aae1ecae3abe9bb3086440b2594e37329000243737e614f30c411bea85dd","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\n### Rule 1: Collapse overlapping booleans into a single atom\n\nWhen several booleans encode one mutually exclusive state, replace them with one field whose atom values enumerate the valid states.\n\nWhy: The two booleans admit four combinations, but only three are meaningful and `admin: true, editor: true` is a contradiction. The `cond` resolves that contradiction by clause order (admin wins), so the invalid state is reachable and the priority rule is invisible at the call site. A single `:role` atom makes only `:admin`, `:editor`, and `:default` representable: the contradiction cannot be constructed, and the `case` is exhaustive over the valid set. The same applies to data: a `User` struct carrying `:admin` and `:editor` booleans should carry one `:role` field, so the struct cannot hold an admin-and-editor record.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":1,"rule_title":"Collapse overlapping booleans into a single atom","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":55,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"9917836dc1667ab587db04dceb78af9a58d97ca938e932908481e6a89935eaf4","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    case Keyword.get(options, :role, :default) do\n      :admin -> handle_admin(invoice)\n      :editor -> handle_editor(invoice)\n      :default -> handle_default(invoice)\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Collapse overlapping booleans into a single atom","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":7,"parent_id":"elixir-design-anti-patterns:adr-002:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-002:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"9707d0d8de191e9dad288e5121f93af1402f91962e1141bfafa75602b9953449","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\nRouting title: Boolean obsession\n\nRule 1: Collapse overlapping booleans into a single atom\n\nCorrect example (positive)\n\nWhen several booleans encode one mutually exclusive state, replace them with one field whose atom values enumerate the valid states.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    case Keyword.get(options, :role, :default) do\n      :admin -> handle_admin(invoice)\n      :editor -> handle_editor(invoice)\n      :default -> handle_default(invoice)\n    end\n  end\nend\n```\n\nWhy: The two booleans admit four combinations, but only three are meaningful and `admin: true, editor: true` is a contradiction. The `cond` resolves that contradiction by clause order (admin wins), so the invalid state is reachable and the priority rule is invisible at the call site. A single `:role` atom makes only `:admin`, `:editor`, and `:default` representable: the contradiction cannot be constructed, and the `case` is exhaustive over the valid set. The same applies to data: a `User` struct carrying `:admin` and `:editor` booleans should carry one `:role` field, so the struct cannot hold an admin-and-editor record.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":1,"rule_title":"Collapse overlapping booleans into a single atom","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":39,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"0bc0909ec28b873d6617768746ace04b21c8f3fad8d9cda32be84c7ddf4cd86c","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    cond do\n      options[:admin] -> handle_admin(invoice)\n      options[:editor] -> handle_editor(invoice)\n      true -> handle_default(invoice)\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Collapse overlapping booleans into a single atom","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":8,"parent_id":"elixir-design-anti-patterns:adr-002:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-002:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"5a8850fbb95517ff21f4f95d4f9af40032313e818c22b188b7a7a1dff22e9e86","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\nRouting title: Boolean obsession\n\nRule 1: Collapse overlapping booleans into a single atom\n\nWrong example (negative)\n\nWhen several booleans encode one mutually exclusive state, replace them with one field whose atom values enumerate the valid states.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Invoices do\n  def process(invoice, options \\\\ []) do\n    cond do\n      options[:admin] -> handle_admin(invoice)\n      options[:editor] -> handle_editor(invoice)\n      true -> handle_default(invoice)\n    end\n  end\nend\n```\n\nWhy: The two booleans admit four combinations, but only three are meaningful and `admin: true, editor: true` is a contradiction. The `cond` resolves that contradiction by clause order (admin wins), so the invalid state is reachable and the priority rule is invisible at the call site. A single `:role` atom makes only `:admin`, `:editor`, and `:default` representable: the contradiction cannot be constructed, and the `case` is exhaustive over the valid set. The same applies to data: a `User` struct carrying `:admin` and `:editor` booleans should carry one `:role` field, so the struct cannot hold an admin-and-editor record.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":1,"rule_title":"Collapse overlapping booleans into a single atom","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":53,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"2ea03bb65fa030660337adc067f2fc810757d9e5123586e91396c1207506e69a","source_start_line":40,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"### Rule 2: Prefer a named state atom over a lone boolean that will grow\n\nEven a single boolean argument is better expressed as an atom when the domain has, or will have, more than two states.\n\n**Correct:**\n\n```elixir\nMyApp.Invoices.update(invoice, status: :approved)\n```\n\n**Wrong:**\n\n```elixir\nMyApp.Invoices.update(invoice, approved: true)\n```\n\n**Why:** `approved: true` hard-codes a binary into the contract. Adding a third state such as `:pending` forces either a second boolean (`pending: true`), which reintroduces the overlapping-combination problem from Rule 1, or a breaking change to the boolean's meaning. A `status:` atom extends by adding one clause and one atom value, with no new representable contradictions. It also reads at the call site: `status: :approved` states the domain concept, `approved: true` states an implementation detail. Because booleans are internally the atoms `:true`/`:false`, there is no performance difference between the two forms.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Prefer a named state atom over a lone boolean that will grow"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-02","languages":["elixir"],"ordinal":9,"parent_id":"elixir-design-anti-patterns:adr-002","polarity":null,"record_id":"elixir-design-anti-patterns:adr-002:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"b3c5758507494b43c1e400cec7469fdca221a4a3b04b59a8559ddae5a1b8c25b","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-02\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\n### Rule 2: Prefer a named state atom over a lone boolean that will grow\n\nEven a single boolean argument is better expressed as an atom when the domain has, or will have, more than two states.\n\nWhy: `approved: true` hard-codes a binary into the contract. Adding a third state such as `:pending` forces either a second boolean (`pending: true`), which reintroduces the overlapping-combination problem from Rule 1, or a breaking change to the boolean's meaning. A `status:` atom extends by adding one clause and one atom value, with no new representable contradictions. It also reads at the call site: `status: :approved` states the domain concept, `approved: true` states an implementation detail. Because booleans are internally the atoms `:true`/`:false`, there is no performance difference between the two forms.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":2,"rule_title":"Prefer a named state atom over a lone boolean that will grow","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":73,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"18fd3d5b5e9b4e2e716cbd1913e56107d89d9974ef8b478eba910fcda1e185c8","source_start_line":56,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\nMyApp.Invoices.update(invoice, status: :approved)\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Prefer a named state atom over a lone boolean that will grow","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-02","languages":["elixir"],"ordinal":10,"parent_id":"elixir-design-anti-patterns:adr-002:rule-02","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-002:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"f01fd5293a0973211d052f9e60bbfb19855f6f45ea86b1b25051b5f96b1d23af","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-02:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\nRouting title: Boolean obsession\n\nRule 2: Prefer a named state atom over a lone boolean that will grow\n\nCorrect example (positive)\n\nEven a single boolean argument is better expressed as an atom when the domain has, or will have, more than two states.\n\n**Correct:**\n\n```elixir\nMyApp.Invoices.update(invoice, status: :approved)\n```\n\nWhy: `approved: true` hard-codes a binary into the contract. Adding a third state such as `:pending` forces either a second boolean (`pending: true`), which reintroduces the overlapping-combination problem from Rule 1, or a breaking change to the boolean's meaning. A `status:` atom extends by adding one clause and one atom value, with no new representable contradictions. It also reads at the call site: `status: :approved` states the domain concept, `approved: true` states an implementation detail. Because booleans are internally the atoms `:true`/`:false`, there is no performance difference between the two forms.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":2,"rule_title":"Prefer a named state atom over a lone boolean that will grow","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":65,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"373a50f8a8f69472257286ef4ff256c215de44e83b8d4ec0da067e066db271f8","source_start_line":60,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"adr_id":2,"adr_title":"Boolean obsession","applies_to":{"content_match":["admin: true","editor: true","approved: true","cond do","options[:admin]","is_admin"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\nMyApp.Invoices.update(invoice, approved: true)\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Prefer a named state atom over a lone boolean that will grow","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-002:rule-02","languages":["elixir"],"ordinal":11,"parent_id":"elixir-design-anti-patterns:adr-002:rule-02","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-002:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"74491870dac0bb00cda548d06e87f27d0d6716f077c21738622a50adc35855ae","retrieval_text":"Record: elixir-design-anti-patterns:adr-002:rule-02:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Boolean obsession\n\nRouting title: Boolean obsession\n\nRule 2: Prefer a named state atom over a lone boolean that will grow\n\nWrong example (negative)\n\nEven a single boolean argument is better expressed as an atom when the domain has, or will have, more than two states.\n\n**Wrong:**\n\n```elixir\nMyApp.Invoices.update(invoice, approved: true)\n```\n\nWhy: `approved: true` hard-codes a binary into the contract. Adding a third state such as `:pending` forces either a second boolean (`pending: true`), which reintroduces the overlapping-combination problem from Rule 1, or a breaking change to the boolean's meaning. A `status:` atom extends by adding one clause and one atom value, with no new representable contradictions. It also reads at the call site: `status: :approved` states the domain concept, `approved: true` states an implementation detail. Because booleans are internally the atoms `:true`/`:false`, there is no performance difference between the two forms.","routing_description":"When two or more booleans encode overlapping or mutually exclusive states, or a lone boolean flag is likely to gain a third state, replace them with a single atom field.","routing_title":"Boolean obsession","rule_number":2,"rule_title":"Prefer a named state atom over a lone boolean that will grow","schema_version":"retrieval-v1","source_description":"Multiple overlapping boolean options or struct fields encode one piece of state across 2^N representable combinations, most of which are invalid and must be resolved by hand-written priority logic. Replace them with a single atom field (or a composite type) whose values name only the valid states.","source_end_line":71,"source_path":"adrs/elixir-design-anti-patterns/adr-002-boolean-obsession.md","source_sha256":"d48f09013e13dda4e1f605c3c84447f8f256a50e2fab2ec24e21ef3c6da8ef91","source_start_line":66,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","atoms"],"updated":null}
{"references":[],"source_sha256":"a118d62a8b06672dae2a86db22a4ce3750ccc7cce2132151f6960d0fc91603f5","source_start_line":12,"consequences":"- Expected failures travel as `{:error, reason}` values matched by `case`, with no stacktrace capture or stack unwinding on the normal path.\n- `try/rescue` is reserved for genuinely exceptional conditions (invalid arguments, programmer error, boundary failures), where it catches only what is declared and does not absorb unrelated bugs.\n- Library functions ship in pairs: a tuple-returning `foo` and a raising `foo!` built on top, so callers keep the choice of whether a given error is exceptional.\n- Scripts, tests, and pipelines call the bang variant to fail fast with a clear message, while long-running code matches the tuple and recovers.","rule_title":null,"routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","record_id":"elixir-design-anti-patterns:adr-003","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"parent_id":null,"decision":"","retrieval_sha256":"15566d696a1163afe0a8d7619685e127ec5b4360d88659ebb479a4b34686653f","hydrate_id":"elixir-design-anti-patterns:adr-003","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-003\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\nRouting title: Exceptions for control-flow\n\nRouting description: When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.\n\nSource description: Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.\n\n## Context\n\nException handling is not itself an anti-pattern. The anti-pattern is using `raise` and `try/rescue` to drive ordinary, expected control flow instead of `case` and pattern matching on result tuples.\n\nThe mechanism matters. `raise` constructs an exception struct, captures a stacktrace by walking the current call stack, and then unwinds stack frames one by one until it reaches the nearest enclosing `try`. A tagged tuple like `{:error, reason}` is an ordinary return value matched in constant time by a `case`. Routing the expected branches of a computation through exceptions therefore pays stacktrace capture and stack unwinding on the normal path, where a `case` pays nothing.\n\nA `rescue` clause matches by exception type, so it catches every exception of that type raised anywhere inside the protected block. It cannot distinguish the failure you anticipated (a missing file) from an unrelated bug raised by some other call in the same `try`. Pattern matching on `{:error, reason}` only matches the specific failure the callee declared, so a real bug still crashes loudly instead of being silently absorbed.\n\nWhether a given error is exceptional is the caller's decision. A library that only raises removes that choice and forces every caller into `try/rescue`. The convention that preserves the choice: expose a function that returns `{:ok, value} | {:error, reason}`, and a bang variant (`foo!`) built on top that raises.\n\nRaising directly is correct for structural errors, not semantic ones. `File.read(123)` should raise because `123` is never a valid filename. Tests, scripts, and one-off pipelines call the bang variants to fail fast with a clear message. Some frameworks (Phoenix) deliberately let code raise and convert those exceptions into responses through a protocol. The anti-pattern is specifically using exceptions for the expected, recoverable branches of normal flow.\n\n## Consequences\n\n- Expected failures travel as `{:error, reason}` values matched by `case`, with no stacktrace capture or stack unwinding on the normal path.\n- `try/rescue` is reserved for genuinely exceptional conditions (invalid arguments, programmer error, boundary failures), where it catches only what is declared and does not absorb unrelated bugs.\n- Library functions ship in pairs: a tuple-returning `foo` and a raising `foo!` built on top, so callers keep the choice of whether a given error is exceptional.\n- Scripts, tests, and pipelines call the bang variant to fail fast with a clear message, while long-running code matches the tuple and recovers.\n\nRule headings:\n### Rule 1: Match result tuples; do not try/rescue an expected failure\n### Rule 2: Library authors expose a non-raising function and build the bang variant on top","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"rule_number":null,"adr_title":"Exceptions for control-flow","languages":[],"source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","ordinal":12,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-003: Exceptions for control-flow"],"context":"Exception handling is not itself an anti-pattern. The anti-pattern is using `raise` and `try/rescue` to drive ordinary, expected control flow instead of `case` and pattern matching on result tuples.\n\nThe mechanism matters. `raise` constructs an exception struct, captures a stacktrace by walking the current call stack, and then unwinds stack frames one by one until it reaches the nearest enclosing `try`. A tagged tuple like `{:error, reason}` is an ordinary return value matched in constant time by a `case`. Routing the expected branches of a computation through exceptions therefore pays stacktrace capture and stack unwinding on the normal path, where a `case` pays nothing.\n\nA `rescue` clause matches by exception type, so it catches every exception of that type raised anywhere inside the protected block. It cannot distinguish the failure you anticipated (a missing file) from an unrelated bug raised by some other call in the same `try`. Pattern matching on `{:error, reason}` only matches the specific failure the callee declared, so a real bug still crashes loudly instead of being silently absorbed.\n\nWhether a given error is exceptional is the caller's decision. A library that only raises removes that choice and forces every caller into `try/rescue`. The convention that preserves the choice: expose a function that returns `{:ok, value} | {:error, reason}`, and a bang variant (`foo!`) built on top that raises.\n\nRaising directly is correct for structural errors, not semantic ones. `File.read(123)` should raise because `123` is never a valid filename. Tests, scripts, and one-off pipelines call the bang variants to fail fast with a clear message. Some frameworks (Phoenix) deliberately let code raise and convert those exceptions into responses through a protocol. The anti-pattern is specifically using exceptions for the expected, recoverable branches of normal flow.","source_end_line":107,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":3,"routing_title":"Exceptions for control-flow","display_text":"## Context\n\nException handling is not itself an anti-pattern. The anti-pattern is using `raise` and `try/rescue` to drive ordinary, expected control flow instead of `case` and pattern matching on result tuples.\n\nThe mechanism matters. `raise` constructs an exception struct, captures a stacktrace by walking the current call stack, and then unwinds stack frames one by one until it reaches the nearest enclosing `try`. A tagged tuple like `{:error, reason}` is an ordinary return value matched in constant time by a `case`. Routing the expected branches of a computation through exceptions therefore pays stacktrace capture and stack unwinding on the normal path, where a `case` pays nothing.\n\nA `rescue` clause matches by exception type, so it catches every exception of that type raised anywhere inside the protected block. It cannot distinguish the failure you anticipated (a missing file) from an unrelated bug raised by some other call in the same `try`. Pattern matching on `{:error, reason}` only matches the specific failure the callee declared, so a real bug still crashes loudly instead of being silently absorbed.\n\nWhether a given error is exceptional is the caller's decision. A library that only raises removes that choice and forces every caller into `try/rescue`. The convention that preserves the choice: expose a function that returns `{:ok, value} | {:error, reason}`, and a bang variant (`foo!`) built on top that raises.\n\nRaising directly is correct for structural errors, not semantic ones. `File.read(123)` should raise because `123` is never a valid filename. Tests, scripts, and one-off pipelines call the bang variants to fail fast with a clear message. Some frameworks (Phoenix) deliberately let code raise and convert those exceptions into responses through a protocol. The anti-pattern is specifically using exceptions for the expected, recoverable branches of normal flow.\n\n## Consequences\n\n- Expected failures travel as `{:error, reason}` values matched by `case`, with no stacktrace capture or stack unwinding on the normal path.\n- `try/rescue` is reserved for genuinely exceptional conditions (invalid arguments, programmer error, boundary failures), where it catches only what is declared and does not absorb unrelated bugs.\n- Library functions ship in pairs: a tuple-returning `foo` and a raising `foo!` built on top, so callers keep the choice of whether a given error is exceptional.\n- Scripts, tests, and pipelines call the bang variant to fail fast with a clear message, while long-running code matches the tuple and recovers."}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"### Rule 1: Match result tuples; do not try/rescue an expected failure\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    case File.read(file) do\n      {:ok, binary} -> IO.puts(binary)\n      {:error, reason} -> IO.puts(:stderr, \"could not read file #{file}: #{reason}\")\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    try do\n      IO.puts(File.read!(file))\n    rescue\n      e -> IO.puts(:stderr, Exception.message(e))\n    end\n  end\nend\n```\n\n**Why:** A missing file is an expected outcome of reading a file, not an exceptional one. The wrong version raises through `File.read!/1`, capturing a stacktrace and unwinding the stack on a branch that is part of normal operation, then rebuilds the message from the exception. The `rescue e ->` clause is also indiscriminate: it would swallow an unrelated exception raised by `IO.puts/1` or by any future code added to the `try` block, masking real bugs as if they were the missing-file case. `File.read/1` returns the failure as a value, so `case` selects the branch in constant time and only the declared `{:error, reason}` shape matches.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Match result tuples; do not try/rescue an expected failure"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":13,"parent_id":"elixir-design-anti-patterns:adr-003","polarity":null,"record_id":"elixir-design-anti-patterns:adr-003:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"119ce13a3afaab99c889780aa0d86d19a312a7cfde6ff98ead6f6ce20cf9edb9","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\n### Rule 1: Match result tuples; do not try/rescue an expected failure\n\nWhy: A missing file is an expected outcome of reading a file, not an exceptional one. The wrong version raises through `File.read!/1`, capturing a stacktrace and unwinding the stack on a branch that is part of normal operation, then rebuilds the message from the exception. The `rescue e ->` clause is also indiscriminate: it would swallow an unrelated exception raised by `IO.puts/1` or by any future code added to the `try` block, masking real bugs as if they were the missing-file case. `File.read/1` returns the failure as a value, so `case` selects the branch in constant time and only the declared `{:error, reason}` shape matches.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":1,"rule_title":"Match result tuples; do not try/rescue an expected failure","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":56,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"90cdee3e0c01bdf39c26fd437c567ea1d6b9b26dba7c7b1eba2545e9eac2b036","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    case File.read(file) do\n      {:ok, binary} -> IO.puts(binary)\n      {:error, reason} -> IO.puts(:stderr, \"could not read file #{file}: #{reason}\")\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Match result tuples; do not try/rescue an expected failure","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":14,"parent_id":"elixir-design-anti-patterns:adr-003:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-003:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"4fbb4787891119bb5b40ba555ef3aef64e880e72db695ebeba1366df6ef27bb9","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\nRouting title: Exceptions for control-flow\n\nRule 1: Match result tuples; do not try/rescue an expected failure\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    case File.read(file) do\n      {:ok, binary} -> IO.puts(binary)\n      {:error, reason} -> IO.puts(:stderr, \"could not read file #{file}: #{reason}\")\n    end\n  end\nend\n```\n\nWhy: A missing file is an expected outcome of reading a file, not an exceptional one. The wrong version raises through `File.read!/1`, capturing a stacktrace and unwinding the stack on a branch that is part of normal operation, then rebuilds the message from the exception. The `rescue e ->` clause is also indiscriminate: it would swallow an unrelated exception raised by `IO.puts/1` or by any future code added to the `try` block, masking real bugs as if they were the missing-file case. `File.read/1` returns the failure as a value, so `case` selects the branch in constant time and only the declared `{:error, reason}` shape matches.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":1,"rule_title":"Match result tuples; do not try/rescue an expected failure","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":40,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"4cf745591b16beee63e0cd3329b0e19547a97adf7ea7a9cd7116a4f3ff5db0a7","source_start_line":28,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    try do\n      IO.puts(File.read!(file))\n    rescue\n      e -> IO.puts(:stderr, Exception.message(e))\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Match result tuples; do not try/rescue an expected failure","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":15,"parent_id":"elixir-design-anti-patterns:adr-003:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-003:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"a1154e398f1beeb18b7dd0cede9f50343dd85e263ba1d481b011ba0a1744d5e3","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\nRouting title: Exceptions for control-flow\n\nRule 1: Match result tuples; do not try/rescue an expected failure\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Reader do\n  def print_file(file) do\n    try do\n      IO.puts(File.read!(file))\n    rescue\n      e -> IO.puts(:stderr, Exception.message(e))\n    end\n  end\nend\n```\n\nWhy: A missing file is an expected outcome of reading a file, not an exceptional one. The wrong version raises through `File.read!/1`, capturing a stacktrace and unwinding the stack on a branch that is part of normal operation, then rebuilds the message from the exception. The `rescue e ->` clause is also indiscriminate: it would swallow an unrelated exception raised by `IO.puts/1` or by any future code added to the `try` block, masking real bugs as if they were the missing-file case. `File.read/1` returns the failure as a value, so `case` selects the branch in constant time and only the declared `{:error, reason}` shape matches.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":1,"rule_title":"Match result tuples; do not try/rescue an expected failure","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":54,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"e1adab04b9d03add63be72520dd255976d0ca4a590de1fd3b981513787269099","source_start_line":41,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"### Rule 2: Library authors expose a non-raising function and build the bang variant on top\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.HTTP.Error do\n  defexception [:reason]\n\n  @impl true\n  def message(%{reason: reason}), do: \"request failed: #{inspect(reason)}\"\nend\n\ndefmodule MyApp.HTTP do\n  def fetch(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> {:ok, build_response(resp)}\n      {:error, reason} -> {:error, %MyApp.HTTP.Error{reason: reason}}\n    end\n  end\n\n  def fetch!(url) do\n    case fetch(url) do\n      {:ok, response} -> response\n      {:error, error} -> raise error\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.HTTP do\n  def fetch!(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> build_response(resp)\n      {:error, reason} -> raise \"request failed: #{inspect(reason)}\"\n    end\n  end\nend\n```\n\n**Why:** When the only API raises, every caller that wants to treat a failed request as ordinary flow has no option but `try/rescue`, inheriting the stacktrace-capture, unwind, and bug-masking costs from Rule 1. Returning `{:error, Exception.t}` hands the decision back to the caller: code that wants to recover matches the tuple, and code that does want to fail re-raises with `raise error`. The bang variant is exactly that re-raise wrapped once in the library, so `fetch!/1` is implemented on top of `fetch/1` rather than the reverse.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Library authors expose a non-raising function and build the bang variant on top"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-02","languages":["elixir"],"ordinal":16,"parent_id":"elixir-design-anti-patterns:adr-003","polarity":null,"record_id":"elixir-design-anti-patterns:adr-003:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"638e771006460254ece4090cef0e75ef1b5cf5788a0e98dc77cdbe76caa927db","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-02\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\n### Rule 2: Library authors expose a non-raising function and build the bang variant on top\n\nWhy: When the only API raises, every caller that wants to treat a failed request as ordinary flow has no option but `try/rescue`, inheriting the stacktrace-capture, unwind, and bug-masking costs from Rule 1. Returning `{:error, Exception.t}` hands the decision back to the caller: code that wants to recover matches the tuple, and code that does want to fail re-raises with `raise error`. The bang variant is exactly that re-raise wrapped once in the library, so `fetch!/1` is implemented on top of `fetch/1` rather than the reverse.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":2,"rule_title":"Library authors expose a non-raising function and build the bang variant on top","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":100,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"d6a954b947077fd089320317e1e926234526fc354af9b282d5f77debbcfd60c1","source_start_line":57,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.HTTP.Error do\n  defexception [:reason]\n\n  @impl true\n  def message(%{reason: reason}), do: \"request failed: #{inspect(reason)}\"\nend\n\ndefmodule MyApp.HTTP do\n  def fetch(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> {:ok, build_response(resp)}\n      {:error, reason} -> {:error, %MyApp.HTTP.Error{reason: reason}}\n    end\n  end\n\n  def fetch!(url) do\n    case fetch(url) do\n      {:ok, response} -> response\n      {:error, error} -> raise error\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Library authors expose a non-raising function and build the bang variant on top","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-02","languages":["elixir"],"ordinal":17,"parent_id":"elixir-design-anti-patterns:adr-003:rule-02","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-003:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"469f1afa81998a3efc316a69d14ad22a434e0c2ab82347c88261ed96c9531d55","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-02:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\nRouting title: Exceptions for control-flow\n\nRule 2: Library authors expose a non-raising function and build the bang variant on top\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.HTTP.Error do\n  defexception [:reason]\n\n  @impl true\n  def message(%{reason: reason}), do: \"request failed: #{inspect(reason)}\"\nend\n\ndefmodule MyApp.HTTP do\n  def fetch(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> {:ok, build_response(resp)}\n      {:error, reason} -> {:error, %MyApp.HTTP.Error{reason: reason}}\n    end\n  end\n\n  def fetch!(url) do\n    case fetch(url) do\n      {:ok, response} -> response\n      {:error, error} -> raise error\n    end\n  end\nend\n```\n\nWhy: When the only API raises, every caller that wants to treat a failed request as ordinary flow has no option but `try/rescue`, inheriting the stacktrace-capture, unwind, and bug-masking costs from Rule 1. Returning `{:error, Exception.t}` hands the decision back to the caller: code that wants to recover matches the tuple, and code that does want to fail re-raises with `raise error`. The bang variant is exactly that re-raise wrapped once in the library, so `fetch!/1` is implemented on top of `fetch/1` rather than the reverse.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":2,"rule_title":"Library authors expose a non-raising function and build the bang variant on top","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":85,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"f4e3b69beb50c5b6c9b7b0ae691f05d5a0cdcd0010bab357de485db95252546a","source_start_line":59,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"adr_id":3,"adr_title":"Exceptions for control-flow","applies_to":{"content_match":["try do","rescue","raise","File.read!","Exception.message"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.HTTP do\n  def fetch!(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> build_response(resp)\n      {:error, reason} -> raise \"request failed: #{inspect(reason)}\"\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Library authors expose a non-raising function and build the bang variant on top","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-003:rule-02","languages":["elixir"],"ordinal":18,"parent_id":"elixir-design-anti-patterns:adr-003:rule-02","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-003:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"6a7790871095fac30a8a4a54010f858c4dccde8ccc3dbab72b576a11a617947b","retrieval_text":"Record: elixir-design-anti-patterns:adr-003:rule-02:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Exceptions for control-flow\n\nRouting title: Exceptions for control-flow\n\nRule 2: Library authors expose a non-raising function and build the bang variant on top\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.HTTP do\n  def fetch!(url) do\n    case :httpc.request(url) do\n      {:ok, resp} -> build_response(resp)\n      {:error, reason} -> raise \"request failed: #{inspect(reason)}\"\n    end\n  end\nend\n```\n\nWhy: When the only API raises, every caller that wants to treat a failed request as ordinary flow has no option but `try/rescue`, inheriting the stacktrace-capture, unwind, and bug-masking costs from Rule 1. Returning `{:error, Exception.t}` hands the decision back to the caller: code that wants to recover matches the tuple, and code that does want to fail re-raises with `raise error`. The bang variant is exactly that re-raise wrapped once in the library, so `fetch!/1` is implemented on top of `fetch/1` rather than the reverse.","routing_description":"When handling an expected, recoverable failure, match an {:ok, _} | {:error, _} tuple with case instead of wrapping a raising call in try/rescue.","routing_title":"Exceptions for control-flow","rule_number":2,"rule_title":"Library authors expose a non-raising function and build the bang variant on top","schema_version":"retrieval-v1","source_description":"Driving expected control flow through raise and try/rescue captures a stacktrace and unwinds the stack on the normal path, and a rescue clause cannot tell an anticipated error from an unrelated bug it silently absorbs. Match result tuples with case, and have library functions return {:ok, value} | {:error, reason} with a bang variant built on top.","source_end_line":98,"source_path":"adrs/elixir-design-anti-patterns/adr-003-exceptions-for-control-flow.md","source_sha256":"2f587f33fac3d5f05f09dd8b7f13c7a62829cd454894ce8c2b4176c10495145b","source_start_line":86,"status":"accepted","tags":["elixir","anti-pattern","error-handling","control-flow","library-design"],"updated":null}
{"references":[{"raw_text":"ADR-002","source_line":95,"target_id":"elixir-design-anti-patterns:adr-002"}],"source_sha256":"6a58407ba1af36140a01849a5ca992ea47a8e126c706f67729641ef39e88cb92","source_start_line":12,"consequences":"- Parsing and validation happen once at the boundary. Downstream functions receive a typed struct and fail fast on the wrong shape via the function clause, instead of `is_binary/1` matching any binary.\n- Field access is by key, checked against the struct definition, instead of re-extracting from a string on every call.\n- Money arithmetic is exact: integer minor units or `Decimal` carry no representation error, and currency travels with the amount.\n- The boolean-valued instance of this pattern (a bare `true`/`false` standing in for a domain state) is covered separately in ADR-002.","rule_title":null,"routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","record_id":"elixir-design-anti-patterns:adr-004","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"parent_id":null,"decision":"","retrieval_sha256":"95ef60d341ef4b9f3014332c603a254eb6e7fa48ed61fd28cafed77d61cc6752","hydrate_id":"elixir-design-anti-patterns:adr-004","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-004\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\nRouting title: Primitive obsession\n\nRouting description: When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.\n\nSource description: Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.\n\n## Context\n\nPrimitive obsession is the use of Elixir's basic types (integer, float, binary) to carry structured domain information that a composite type (tuple, map, or struct) would represent directly. An address flattened into a single string, or money held as a float, is the typical shape.\n\nA primitive carries no shape. The guards that test it (`is_binary/1`, `is_float/1`) match every value of that type, so the pattern matcher cannot separate a valid address from arbitrary text, or a currency amount from any other float. Validity is therefore unenforceable at the boundary and gets re-derived inside every consumer: each function that needs a field re-parses the string. The repeated extraction is both a correctness hazard (every call site can parse differently) and dead work.\n\nHolding money in a float is the same mistake: a float is the wrong representation for a value that must be exact.\n\n## Consequences\n\n- Parsing and validation happen once at the boundary. Downstream functions receive a typed struct and fail fast on the wrong shape via the function clause, instead of `is_binary/1` matching any binary.\n- Field access is by key, checked against the struct definition, instead of re-extracting from a string on every call.\n- Money arithmetic is exact: integer minor units or `Decimal` carry no representation error, and currency travels with the amount.\n- The boolean-valued instance of this pattern (a bare `true`/`false` standing in for a domain state) is covered separately in ADR-002.\n\nRule headings:\n### Rule 1: Model structured values with a struct, parsed once at the boundary\n### Rule 2: Model money and currency with exact types, not floats","tags":["elixir","anti-pattern","design","data-modeling","structs"],"rule_number":null,"adr_title":"Primitive obsession","languages":[],"source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","ordinal":19,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-004: Primitive obsession"],"context":"Primitive obsession is the use of Elixir's basic types (integer, float, binary) to carry structured domain information that a composite type (tuple, map, or struct) would represent directly. An address flattened into a single string, or money held as a float, is the typical shape.\n\nA primitive carries no shape. The guards that test it (`is_binary/1`, `is_float/1`) match every value of that type, so the pattern matcher cannot separate a valid address from arbitrary text, or a currency amount from any other float. Validity is therefore unenforceable at the boundary and gets re-derived inside every consumer: each function that needs a field re-parses the string. The repeated extraction is both a correctness hazard (every call site can parse differently) and dead work.\n\nHolding money in a float is the same mistake: a float is the wrong representation for a value that must be exact.","source_end_line":96,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":4,"routing_title":"Primitive obsession","display_text":"## Context\n\nPrimitive obsession is the use of Elixir's basic types (integer, float, binary) to carry structured domain information that a composite type (tuple, map, or struct) would represent directly. An address flattened into a single string, or money held as a float, is the typical shape.\n\nA primitive carries no shape. The guards that test it (`is_binary/1`, `is_float/1`) match every value of that type, so the pattern matcher cannot separate a valid address from arbitrary text, or a currency amount from any other float. Validity is therefore unenforceable at the boundary and gets re-derived inside every consumer: each function that needs a field re-parses the string. The repeated extraction is both a correctness hazard (every call site can parse differently) and dead work.\n\nHolding money in a float is the same mistake: a float is the wrong representation for a value that must be exact.\n\n## Consequences\n\n- Parsing and validation happen once at the boundary. Downstream functions receive a typed struct and fail fast on the wrong shape via the function clause, instead of `is_binary/1` matching any binary.\n- Field access is by key, checked against the struct definition, instead of re-extracting from a string on every call.\n- Money arithmetic is exact: integer minor units or `Decimal` carry no representation error, and currency travels with the amount.\n- The boolean-valued instance of this pattern (a bare `true`/`false` standing in for a domain state) is covered separately in ADR-002."}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"### Rule 1: Model structured values with a struct, parsed once at the boundary\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Address do\n  defstruct [:street, :city, :state, :postal_code, :country]\nend\n\ndefmodule MyApp do\n  def parse(address) when is_binary(address) do\n    # Returns %MyApp.Address{}\n  end\n\n  def extract_postal_code(%MyApp.Address{} = address) do\n    address.postal_code\n  end\n\n  def fill_in_country(%MyApp.Address{} = address) do\n    # Fill in missing country...\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp do\n  def extract_postal_code(address) when is_binary(address) do\n    # Re-parse the string to find the postal code...\n  end\n\n  def fill_in_country(address) when is_binary(address) do\n    # Re-parse the string to find the country...\n  end\nend\n```\n\n**Why:** `is_binary(address)` matches every binary, so the guard cannot tell a real address from arbitrary text, and each function must re-parse the string to reach a field. Parsing once into `%MyApp.Address{}` moves extraction to a single place: downstream functions pattern-match the struct, so a wrong shape fails the function clause immediately instead of parsing garbage. A struct also fixes its key set at compile time, so `address.postal_code` and `%MyApp.Address{postal_code: code}` are checked against the definition. A misspelled field is a compile error, not the silent `nil` a free-form map returns from `Map.get/2`.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Model structured values with a struct, parsed once at the boundary"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":20,"parent_id":"elixir-design-anti-patterns:adr-004","polarity":null,"record_id":"elixir-design-anti-patterns:adr-004:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"60a5fa129d66a8c21213a62168c3f3d386f218a43afb55eca38bf1cbb20c5011","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\n### Rule 1: Model structured values with a struct, parsed once at the boundary\n\nWhy: `is_binary(address)` matches every binary, so the guard cannot tell a real address from arbitrary text, and each function must re-parse the string to reach a field. Parsing once into `%MyApp.Address{}` moves extraction to a single place: downstream functions pattern-match the struct, so a wrong shape fails the function clause immediately instead of parsing garbage. A struct also fixes its key set at compile time, so `address.postal_code` and `%MyApp.Address{postal_code: code}` are checked against the definition. A misspelled field is a compile error, not the silent `nil` a free-form map returns from `Map.get/2`.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":1,"rule_title":"Model structured values with a struct, parsed once at the boundary","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":61,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"a16169ac98c70285522cec63aab90bfdba965db5d04f9b40b8b9f3e54877b1a0","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Address do\n  defstruct [:street, :city, :state, :postal_code, :country]\nend\n\ndefmodule MyApp do\n  def parse(address) when is_binary(address) do\n    # Returns %MyApp.Address{}\n  end\n\n  def extract_postal_code(%MyApp.Address{} = address) do\n    address.postal_code\n  end\n\n  def fill_in_country(%MyApp.Address{} = address) do\n    # Fill in missing country...\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Model structured values with a struct, parsed once at the boundary","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":21,"parent_id":"elixir-design-anti-patterns:adr-004:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-004:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"96a24a7c97dcf8de0e437f70cf01ea6b78d4f4d9728c99d0b8e21f54df6023f7","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\nRouting title: Primitive obsession\n\nRule 1: Model structured values with a struct, parsed once at the boundary\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Address do\n  defstruct [:street, :city, :state, :postal_code, :country]\nend\n\ndefmodule MyApp do\n  def parse(address) when is_binary(address) do\n    # Returns %MyApp.Address{}\n  end\n\n  def extract_postal_code(%MyApp.Address{} = address) do\n    address.postal_code\n  end\n\n  def fill_in_country(%MyApp.Address{} = address) do\n    # Fill in missing country...\n  end\nend\n```\n\nWhy: `is_binary(address)` matches every binary, so the guard cannot tell a real address from arbitrary text, and each function must re-parse the string to reach a field. Parsing once into `%MyApp.Address{}` moves extraction to a single place: downstream functions pattern-match the struct, so a wrong shape fails the function clause immediately instead of parsing garbage. A struct also fixes its key set at compile time, so `address.postal_code` and `%MyApp.Address{postal_code: code}` are checked against the definition. A misspelled field is a compile error, not the silent `nil` a free-form map returns from `Map.get/2`.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":1,"rule_title":"Model structured values with a struct, parsed once at the boundary","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":45,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"372b3a11be3f7cd43c3039fc9019b41f9f8e49fe3c325ba5aa651c63cf3a4517","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp do\n  def extract_postal_code(address) when is_binary(address) do\n    # Re-parse the string to find the postal code...\n  end\n\n  def fill_in_country(address) when is_binary(address) do\n    # Re-parse the string to find the country...\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Model structured values with a struct, parsed once at the boundary","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":22,"parent_id":"elixir-design-anti-patterns:adr-004:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-004:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"24cf6705208fca3b58cd2a8c147e8943c454a3ab7f8760cb1532d7266a20a2a3","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\nRouting title: Primitive obsession\n\nRule 1: Model structured values with a struct, parsed once at the boundary\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp do\n  def extract_postal_code(address) when is_binary(address) do\n    # Re-parse the string to find the postal code...\n  end\n\n  def fill_in_country(address) when is_binary(address) do\n    # Re-parse the string to find the country...\n  end\nend\n```\n\nWhy: `is_binary(address)` matches every binary, so the guard cannot tell a real address from arbitrary text, and each function must re-parse the string to reach a field. Parsing once into `%MyApp.Address{}` moves extraction to a single place: downstream functions pattern-match the struct, so a wrong shape fails the function clause immediately instead of parsing garbage. A struct also fixes its key set at compile time, so `address.postal_code` and `%MyApp.Address{postal_code: code}` are checked against the definition. A misspelled field is a compile error, not the silent `nil` a free-form map returns from `Map.get/2`.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":1,"rule_title":"Model structured values with a struct, parsed once at the boundary","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":59,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"bec0781bd10cab69681618065e6023b5b4a965d423f7d0d261d804552bf8e7ac","source_start_line":46,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"### Rule 2: Model money and currency with exact types, not floats\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Money do\n  defstruct [:cents, :currency]\n\n  def add_tax(%__MODULE__{cents: cents} = money, rate) do\n    %{money | cents: round(cents * rate)}\n  end\nend\n\nMyApp.Money.add_tax(%MyApp.Money{cents: 1000, currency: :usd}, 1.0825)\n```\n\n**Wrong:**\n\n```elixir\ndef add_tax(price, rate) when is_float(price) do\n  price * rate\nend\n\nadd_tax(10.0, 1.0825)\n```\n\n**Why:** A `float` is an IEEE 754 double: it stores values in base 2, and decimal fractions like 0.1 and 0.2 have no exact binary representation, so `0.1 + 0.2` evaluates to `0.30000000000000004` and the error accumulates across operations. Holding the amount as an integer count of the smallest unit (cents), or as a `Decimal`, carries the value exactly; rounding happens explicitly and once. Pairing the amount with its `:currency` in a struct also prevents silently adding two different currencies, which a bare number permits.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Model money and currency with exact types, not floats"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":23,"parent_id":"elixir-design-anti-patterns:adr-004","polarity":null,"record_id":"elixir-design-anti-patterns:adr-004:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"f45a68cb1fea5e158a82d865bf6c7e7d7d2731611ed8e2a42fce76cecb1b394f","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-02\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\n### Rule 2: Model money and currency with exact types, not floats\n\nWhy: A `float` is an IEEE 754 double: it stores values in base 2, and decimal fractions like 0.1 and 0.2 have no exact binary representation, so `0.1 + 0.2` evaluates to `0.30000000000000004` and the error accumulates across operations. Holding the amount as an integer count of the smallest unit (cents), or as a `Decimal`, carries the value exactly; rounding happens explicitly and once. Pairing the amount with its `:currency` in a struct also prevents silently adding two different currencies, which a bare number permits.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":2,"rule_title":"Model money and currency with exact types, not floats","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":89,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"9ea53db30a5d3731d67daf2f30a5e2aa27c14f3afcedb0042f8ddaca13fa4b42","source_start_line":62,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Money do\n  defstruct [:cents, :currency]\n\n  def add_tax(%__MODULE__{cents: cents} = money, rate) do\n    %{money | cents: round(cents * rate)}\n  end\nend\n\nMyApp.Money.add_tax(%MyApp.Money{cents: 1000, currency: :usd}, 1.0825)\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Model money and currency with exact types, not floats","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":24,"parent_id":"elixir-design-anti-patterns:adr-004:rule-02","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-004:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"c204ccbf5d3e4bc12765c836a63202d114884e1c258bc2e5167cef94a7288bab","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-02:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\nRouting title: Primitive obsession\n\nRule 2: Model money and currency with exact types, not floats\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Money do\n  defstruct [:cents, :currency]\n\n  def add_tax(%__MODULE__{cents: cents} = money, rate) do\n    %{money | cents: round(cents * rate)}\n  end\nend\n\nMyApp.Money.add_tax(%MyApp.Money{cents: 1000, currency: :usd}, 1.0825)\n```\n\nWhy: A `float` is an IEEE 754 double: it stores values in base 2, and decimal fractions like 0.1 and 0.2 have no exact binary representation, so `0.1 + 0.2` evaluates to `0.30000000000000004` and the error accumulates across operations. Holding the amount as an integer count of the smallest unit (cents), or as a `Decimal`, carries the value exactly; rounding happens explicitly and once. Pairing the amount with its `:currency` in a struct also prevents silently adding two different currencies, which a bare number permits.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":2,"rule_title":"Model money and currency with exact types, not floats","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":77,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"5a894382a2374baae5e64337920b41efc0eee74fbb9750d63f0cf461b1532bc9","source_start_line":64,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"adr_id":4,"adr_title":"Primitive obsession","applies_to":{"content_match":["when is_binary(","is_float(","Float.round"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndef add_tax(price, rate) when is_float(price) do\n  price * rate\nend\n\nadd_tax(10.0, 1.0825)\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Model money and currency with exact types, not floats","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":25,"parent_id":"elixir-design-anti-patterns:adr-004:rule-02","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-004:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"b8c5b2b28ab18d916c43c4a53959e2c631b6c46618c9948e8c014789a1810b77","retrieval_text":"Record: elixir-design-anti-patterns:adr-004:rule-02:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Primitive obsession\n\nRouting title: Primitive obsession\n\nRule 2: Model money and currency with exact types, not floats\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndef add_tax(price, rate) when is_float(price) do\n  price * rate\nend\n\nadd_tax(10.0, 1.0825)\n```\n\nWhy: A `float` is an IEEE 754 double: it stores values in base 2, and decimal fractions like 0.1 and 0.2 have no exact binary representation, so `0.1 + 0.2` evaluates to `0.30000000000000004` and the error accumulates across operations. Holding the amount as an integer count of the smallest unit (cents), or as a `Decimal`, carries the value exactly; rounding happens explicitly and once. Pairing the amount with its `:currency` in a struct also prevents silently adding two different currencies, which a bare number permits.","routing_description":"When a string, integer, or float is repeatedly parsed or validated to stand in for a structured domain value, model it with a struct parsed once at the boundary, and use integer minor units or Decimal for money.","routing_title":"Primitive obsession","rule_number":2,"rule_title":"Model money and currency with exact types, not floats","schema_version":"retrieval-v1","source_description":"Carrying structured domain values (addresses, money) in bare strings or floats forces every consumer to re-parse and re-validate, because primitive guards like is_binary/1 match any value of the type. Parse once at the boundary into a struct, and model money as integer minor units or Decimal since IEEE 754 floats cannot represent decimal fractions exactly.","source_end_line":87,"source_path":"adrs/elixir-design-anti-patterns/adr-004-primitive-obsession.md","source_sha256":"019d7c2226ff6514bd516409992e93d5c33cd69dafc89a9f34ea6da88e3e9b66","source_start_line":78,"status":"accepted","tags":["elixir","anti-pattern","design","data-modeling","structs"],"updated":null}
{"references":[],"source_sha256":"9028885cfdc9b32388231105de797038144c531a1db440de824cb2c675344d8f","source_start_line":12,"consequences":"- Each operation carries a precise `@spec` instead of a union of disjoint types that constrains nothing.\n- Documentation describes one behavior, not a conditional table keyed on argument shape.\n- Call sites name the operation they invoke, so dispatch is visible at the call rather than hidden in the function head.\n- The split renames a public function, so it is a breaking change to the function's contract and must be propagated to callers.\n- Multi-clause dispatch is retained for genuinely related variations and for functions that behave uniformly across all inputs.","rule_title":null,"routing_description":"When clauses of one function handle unrelated inputs and the @doc narrates each case with \"if given X\" conditionals, split them into separately named functions.","record_id":"elixir-design-anti-patterns:adr-005","applies_to":{"content_match":["If given a","If it is a","def update(%Product{","def update(%Animal{","Product.t() | Animal.t()"]},"parent_id":null,"decision":"","retrieval_sha256":"bd6e771504d7434aa6b0a612dff256056308008f31d91df878490ed5996719f4","hydrate_id":"elixir-design-anti-patterns:adr-005","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-005\n\nDomain: elixir-design-anti-patterns\n\nADR: Unrelated multi-clause function\n\nRouting title: Unrelated multi-clause function\n\nRouting description: When clauses of one function handle unrelated inputs and the @doc narrates each case with \"if given X\" conditionals, split them into separately named functions.\n\nSource description: A multi-clause function shares one name, one @spec, and one @doc across every clause. Grouping unrelated behavior under that single contract widens the spec into a union of disjoint types that constrains no caller and forces the documentation into per-argument conditionals; split unrelated clauses into separately named functions.\n\n## Context\n\nMulti-clause functions dispatch on the shape of their arguments by pattern matching on the function head. This is a core Elixir mechanism and the right tool when a single operation has variations (an empty list versus a non-empty one, a struct with one field value versus another). The anti-pattern appears when one function name is reused to host genuinely unrelated business logic, so each clause does something the other clauses have no relationship to.\n\nThe cost comes from the fact that the function name, not the individual clause, is the unit of contract. All clauses share one `@spec` and one `@doc`. When the behaviors are unrelated, the `@spec` widens to a union of disjoint types (`update(Product.t() | Animal.t())`) that no longer constrains callers, and the `@doc` degenerates into a conditional narration of how the function behaves per argument combination. That conditional documentation is the reliable tell: if you cannot describe the function without \"if given an X, ...; if given a Y, ...\", the clauses are unrelated and belong under separate names.\n\n## Consequences\n\n- Each operation carries a precise `@spec` instead of a union of disjoint types that constrains nothing.\n- Documentation describes one behavior, not a conditional table keyed on argument shape.\n- Call sites name the operation they invoke, so dispatch is visible at the call rather than hidden in the function head.\n- The split renames a public function, so it is a breaking change to the function's contract and must be propagated to callers.\n- Multi-clause dispatch is retained for genuinely related variations and for functions that behave uniformly across all inputs.\n\nRule headings:\n### Rule 1: One function name, one operation","tags":["elixir","anti-pattern","design","multi-clause","pattern-matching"],"rule_number":null,"adr_title":"Unrelated multi-clause function","languages":[],"source_description":"A multi-clause function shares one name, one @spec, and one @doc across every clause. Grouping unrelated behavior under that single contract widens the spec into a union of disjoint types that constrains no caller and forces the documentation into per-argument conditionals; split unrelated clauses into separately named functions.","ordinal":26,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-005-unrelated-multi-clause-function.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-005: Unrelated multi-clause function"],"context":"Multi-clause functions dispatch on the shape of their arguments by pattern matching on the function head. This is a core Elixir mechanism and the right tool when a single operation has variations (an empty list versus a non-empty one, a struct with one field value versus another). The anti-pattern appears when one function name is reused to host genuinely unrelated business logic, so each clause does something the other clauses have no relationship to.\n\nThe cost comes from the fact that the function name, not the individual clause, is the unit of contract. All clauses share one `@spec` and one `@doc`. When the behaviors are unrelated, the `@spec` widens to a union of disjoint types (`update(Product.t() | Animal.t())`) that no longer constrains callers, and the `@doc` degenerates into a conditional narration of how the function behaves per argument combination. That conditional documentation is the reliable tell: if you cannot describe the function without \"if given an X, ...; if given a Y, ...\", the clauses are unrelated and belong under separate names.","source_end_line":68,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":5,"routing_title":"Unrelated multi-clause function","display_text":"## Context\n\nMulti-clause functions dispatch on the shape of their arguments by pattern matching on the function head. This is a core Elixir mechanism and the right tool when a single operation has variations (an empty list versus a non-empty one, a struct with one field value versus another). The anti-pattern appears when one function name is reused to host genuinely unrelated business logic, so each clause does something the other clauses have no relationship to.\n\nThe cost comes from the fact that the function name, not the individual clause, is the unit of contract. All clauses share one `@spec` and one `@doc`. When the behaviors are unrelated, the `@spec` widens to a union of disjoint types (`update(Product.t() | Animal.t())`) that no longer constrains callers, and the `@doc` degenerates into a conditional narration of how the function behaves per argument combination. That conditional documentation is the reliable tell: if you cannot describe the function without \"if given an X, ...; if given a Y, ...\", the clauses are unrelated and belong under separate names.\n\n## Consequences\n\n- Each operation carries a precise `@spec` instead of a union of disjoint types that constrains nothing.\n- Documentation describes one behavior, not a conditional table keyed on argument shape.\n- Call sites name the operation they invoke, so dispatch is visible at the call rather than hidden in the function head.\n- The split renames a public function, so it is a breaking change to the function's contract and must be propagated to callers.\n- Multi-clause dispatch is retained for genuinely related variations and for functions that behave uniformly across all inputs."}
{"adr_id":5,"adr_title":"Unrelated multi-clause function","applies_to":{"content_match":["If given a","If it is a","def update(%Product{","def update(%Animal{","Product.t() | Animal.t()"]},"date":"2026-06-28","display_text":"### Rule 1: One function name, one operation\n\nIf two clauses implement unrelated business rules, give them distinct names (and, where the rules diverge enough, distinct modules).\n\n**Correct:**\n\n```elixir\n@doc \"Updates a product.\"\n@spec update_product(Product.t()) :: Product.t()\ndef update_product(%Product{count: count, material: material}) do\n  # ...\nend\n\n@doc \"Updates an animal.\"\n@spec update_animal(Animal.t()) :: Animal.t()\ndef update_animal(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```\n\n**Wrong:**\n\n```elixir\n@doc \"\"\"\nUpdates a struct.\n\nIf given a product, it reprices and restocks it.\nIf given an animal, it feeds and reweighs it.\n\"\"\"\n@spec update(Product.t() | Animal.t()) :: Product.t() | Animal.t()\ndef update(%Product{count: count, material: material}) do\n  # ...\nend\n\ndef update(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```\n\n**Why:** All clauses of `update/1` share one `@spec` and one `@doc`. Because the behaviors are unrelated, the `@spec` widens to `Product.t() | Animal.t()`, so the type checker can no longer tell a caller that passed an animal that a product-shaped result is wrong: the union admits both. The single `@doc` collapses into per-argument conditionals, which is the diagnostic that the clauses do not belong together. Distinct names give each operation a precise spec, a self-describing doc, and a call site that states which behavior it invoked. Keep clauses under one name only when they are variations of a single operation (`update_product/1` may still match `%Product{count: 0}` against a `material` guard) or when the function behaves uniformly for every input (as `struct/2` does for any struct given); in both cases the shared contract stays coherent. Note that this refactoring renames the public function, so it ripples to every caller.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name, one operation"],"hydrate_id":"elixir-design-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":27,"parent_id":"elixir-design-anti-patterns:adr-005","polarity":null,"record_id":"elixir-design-anti-patterns:adr-005:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"9f1b6a5fddf5df4704a0631861b9b4968cda6218c2056e0134ab166db077c138","retrieval_text":"Record: elixir-design-anti-patterns:adr-005:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Unrelated multi-clause function\n\n### Rule 1: One function name, one operation\n\nIf two clauses implement unrelated business rules, give them distinct names (and, where the rules diverge enough, distinct modules).\n\nWhy: All clauses of `update/1` share one `@spec` and one `@doc`. Because the behaviors are unrelated, the `@spec` widens to `Product.t() | Animal.t()`, so the type checker can no longer tell a caller that passed an animal that a product-shaped result is wrong: the union admits both. The single `@doc` collapses into per-argument conditionals, which is the diagnostic that the clauses do not belong together. Distinct names give each operation a precise spec, a self-describing doc, and a call site that states which behavior it invoked. Keep clauses under one name only when they are variations of a single operation (`update_product/1` may still match `%Product{count: 0}` against a `material` guard) or when the function behaves uniformly for every input (as `struct/2` does for any struct given); in both cases the shared contract stays coherent. Note that this refactoring renames the public function, so it ripples to every caller.","routing_description":"When clauses of one function handle unrelated inputs and the @doc narrates each case with \"if given X\" conditionals, split them into separately named functions.","routing_title":"Unrelated multi-clause function","rule_number":1,"rule_title":"One function name, one operation","schema_version":"retrieval-v1","source_description":"A multi-clause function shares one name, one @spec, and one @doc across every clause. Grouping unrelated behavior under that single contract widens the spec into a union of disjoint types that constrains no caller and forces the documentation into per-argument conditionals; split unrelated clauses into separately named functions.","source_end_line":60,"source_path":"adrs/elixir-design-anti-patterns/adr-005-unrelated-multi-clause-function.md","source_sha256":"42e6dd05df12450570a4ae2b2ac7943f30785e7de1fe9106a4047a0798693bfe","source_start_line":20,"status":"accepted","tags":["elixir","anti-pattern","design","multi-clause","pattern-matching"],"updated":null}
{"adr_id":5,"adr_title":"Unrelated multi-clause function","applies_to":{"content_match":["If given a","If it is a","def update(%Product{","def update(%Animal{","Product.t() | Animal.t()"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\n@doc \"Updates a product.\"\n@spec update_product(Product.t()) :: Product.t()\ndef update_product(%Product{count: count, material: material}) do\n  # ...\nend\n\n@doc \"Updates an animal.\"\n@spec update_animal(Animal.t()) :: Animal.t()\ndef update_animal(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name, one operation","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":28,"parent_id":"elixir-design-anti-patterns:adr-005:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-005:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"98d51a4b4ac938822e7aafa14a6ca3d6d067ec39892190aac6c395d7850e3180","retrieval_text":"Record: elixir-design-anti-patterns:adr-005:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Unrelated multi-clause function\n\nRouting title: Unrelated multi-clause function\n\nRule 1: One function name, one operation\n\nCorrect example (positive)\n\nIf two clauses implement unrelated business rules, give them distinct names (and, where the rules diverge enough, distinct modules).\n\n**Correct:**\n\n```elixir\n@doc \"Updates a product.\"\n@spec update_product(Product.t()) :: Product.t()\ndef update_product(%Product{count: count, material: material}) do\n  # ...\nend\n\n@doc \"Updates an animal.\"\n@spec update_animal(Animal.t()) :: Animal.t()\ndef update_animal(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```\n\nWhy: All clauses of `update/1` share one `@spec` and one `@doc`. Because the behaviors are unrelated, the `@spec` widens to `Product.t() | Animal.t()`, so the type checker can no longer tell a caller that passed an animal that a product-shaped result is wrong: the union admits both. The single `@doc` collapses into per-argument conditionals, which is the diagnostic that the clauses do not belong together. Distinct names give each operation a precise spec, a self-describing doc, and a call site that states which behavior it invoked. Keep clauses under one name only when they are variations of a single operation (`update_product/1` may still match `%Product{count: 0}` against a `material` guard) or when the function behaves uniformly for every input (as `struct/2` does for any struct given); in both cases the shared contract stays coherent. Note that this refactoring renames the public function, so it ripples to every caller.","routing_description":"When clauses of one function handle unrelated inputs and the @doc narrates each case with \"if given X\" conditionals, split them into separately named functions.","routing_title":"Unrelated multi-clause function","rule_number":1,"rule_title":"One function name, one operation","schema_version":"retrieval-v1","source_description":"A multi-clause function shares one name, one @spec, and one @doc across every clause. Grouping unrelated behavior under that single contract widens the spec into a union of disjoint types that constrains no caller and forces the documentation into per-argument conditionals; split unrelated clauses into separately named functions.","source_end_line":39,"source_path":"adrs/elixir-design-anti-patterns/adr-005-unrelated-multi-clause-function.md","source_sha256":"caa027c4f00deee012535baa94af3052891b529d9f2f92302f58dfca0c7eafe4","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","design","multi-clause","pattern-matching"],"updated":null}
{"adr_id":5,"adr_title":"Unrelated multi-clause function","applies_to":{"content_match":["If given a","If it is a","def update(%Product{","def update(%Animal{","Product.t() | Animal.t()"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n@doc \"\"\"\nUpdates a struct.\n\nIf given a product, it reprices and restocks it.\nIf given an animal, it feeds and reweighs it.\n\"\"\"\n@spec update(Product.t() | Animal.t()) :: Product.t() | Animal.t()\ndef update(%Product{count: count, material: material}) do\n  # ...\nend\n\ndef update(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: One function name, one operation","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":29,"parent_id":"elixir-design-anti-patterns:adr-005:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-005:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"76dd4ba221d0bf841b54878fa5cbebed88efd5ae92d8181571fc54996e76fb7a","retrieval_text":"Record: elixir-design-anti-patterns:adr-005:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Unrelated multi-clause function\n\nRouting title: Unrelated multi-clause function\n\nRule 1: One function name, one operation\n\nWrong example (negative)\n\nIf two clauses implement unrelated business rules, give them distinct names (and, where the rules diverge enough, distinct modules).\n\n**Wrong:**\n\n```elixir\n@doc \"\"\"\nUpdates a struct.\n\nIf given a product, it reprices and restocks it.\nIf given an animal, it feeds and reweighs it.\n\"\"\"\n@spec update(Product.t() | Animal.t()) :: Product.t() | Animal.t()\ndef update(%Product{count: count, material: material}) do\n  # ...\nend\n\ndef update(%Animal{count: count, skin: skin}) do\n  # ...\nend\n```\n\nWhy: All clauses of `update/1` share one `@spec` and one `@doc`. Because the behaviors are unrelated, the `@spec` widens to `Product.t() | Animal.t()`, so the type checker can no longer tell a caller that passed an animal that a product-shaped result is wrong: the union admits both. The single `@doc` collapses into per-argument conditionals, which is the diagnostic that the clauses do not belong together. Distinct names give each operation a precise spec, a self-describing doc, and a call site that states which behavior it invoked. Keep clauses under one name only when they are variations of a single operation (`update_product/1` may still match `%Product{count: 0}` against a `material` guard) or when the function behaves uniformly for every input (as `struct/2` does for any struct given); in both cases the shared contract stays coherent. Note that this refactoring renames the public function, so it ripples to every caller.","routing_description":"When clauses of one function handle unrelated inputs and the @doc narrates each case with \"if given X\" conditionals, split them into separately named functions.","routing_title":"Unrelated multi-clause function","rule_number":1,"rule_title":"One function name, one operation","schema_version":"retrieval-v1","source_description":"A multi-clause function shares one name, one @spec, and one @doc across every clause. Grouping unrelated behavior under that single contract widens the spec into a union of disjoint types that constrains no caller and forces the documentation into per-argument conditionals; split unrelated clauses into separately named functions.","source_end_line":58,"source_path":"adrs/elixir-design-anti-patterns/adr-005-unrelated-multi-clause-function.md","source_sha256":"1233ec2b0766a5d0ee295cd8e6d82e93d10ec4289b265db905c05d6b31fb31bf","source_start_line":40,"status":"accepted","tags":["elixir","anti-pattern","design","multi-clause","pattern-matching"],"updated":null}
{"references":[],"source_sha256":"73bcfe53091b4c5534b288dc8a4031d95da8f4c8e1cc9277b70314a00064aee5","source_start_line":12,"consequences":"- Configuration lives at the call site (keyword lists), in the consumer's own supervision tree (child specs), or in the consumer's own module (`use` options), never in node-global application environment owned by the library.\n- Multiple consumers, and multiple call sites within one consumer, configure the same library aspect independently.\n- Library behavior is determined by its arguments, so tests do not mutate shared application environment between runs and can stay `async: true`.\n- Acceptable residual uses remain: swapping a behaviour-conforming implementation, or providing customizable defaults when generating code from data files, where the chosen value does not change the outcome.\n- Mix tasks read per-project configuration via `Mix.Project.config/0` or `OptionParser` command-line arguments rather than the global application environment.","rule_title":null,"routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","record_id":"elixir-design-anti-patterns:adr-006","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"parent_id":null,"decision":"","retrieval_sha256":"0bee1fb9de800a6dc75eb02cc71892d3c105bafa4af0fdbe1c99b140f23ce083","hydrate_id":"elixir-design-anti-patterns:adr-006","status":"accepted","retrieval_text":"Record: elixir-design-anti-patterns:adr-006\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRouting description: When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.\n\nSource description: A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.\n\n## Context\n\nThe application environment (`Application.get_env/3`, `Application.fetch_env!/2`, `Application.compile_env/3`) is a node-global key-value store, indexed by application name and key, used to parameterize values across an Elixir system. It is a useful mechanism and not an anti-pattern by itself. The anti-pattern is a library author reaching for it to configure the library's own behavior.\n\nBecause the store is global, there is exactly one value for each `{application, key}` pair on the node. A library that reads its configuration from the application environment therefore exposes exactly one configurable value to the entire world: every application that depends on the library, and every call site within those applications, shares it. Two consumers of the same library cannot configure the same aspect differently, and a single consumer cannot vary it between two calls.\n\nThe fix in every case is to source configuration from the caller rather than from library-owned global state: function arguments for runtime values, consumer-owned child specs for processes, and required options on `use` for compile-time values. Each pushes the decision to the point where the library is consumed.\n\n## Consequences\n\n- Configuration lives at the call site (keyword lists), in the consumer's own supervision tree (child specs), or in the consumer's own module (`use` options), never in node-global application environment owned by the library.\n- Multiple consumers, and multiple call sites within one consumer, configure the same library aspect independently.\n- Library behavior is determined by its arguments, so tests do not mutate shared application environment between runs and can stay `async: true`.\n- Acceptable residual uses remain: swapping a behaviour-conforming implementation, or providing customizable defaults when generating code from data files, where the chosen value does not change the outcome.\n- Mix tasks read per-project configuration via `Mix.Project.config/0` or `OptionParser` command-line arguments rather than the global application environment.\n\nRule headings:\n### Rule 1: Take runtime configuration as function arguments\n### Rule 2: Expose a child spec instead of starting your own supervision tree\n### Rule 3: Take compile-time configuration as options on `use`","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"rule_number":null,"adr_title":"Using application configuration for libraries","languages":[],"source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","ordinal":30,"polarity":null,"updated":null,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-006: Using application configuration for libraries"],"context":"The application environment (`Application.get_env/3`, `Application.fetch_env!/2`, `Application.compile_env/3`) is a node-global key-value store, indexed by application name and key, used to parameterize values across an Elixir system. It is a useful mechanism and not an anti-pattern by itself. The anti-pattern is a library author reaching for it to configure the library's own behavior.\n\nBecause the store is global, there is exactly one value for each `{application, key}` pair on the node. A library that reads its configuration from the application environment therefore exposes exactly one configurable value to the entire world: every application that depends on the library, and every call site within those applications, shares it. Two consumers of the same library cannot configure the same aspect differently, and a single consumer cannot vary it between two calls.\n\nThe fix in every case is to source configuration from the caller rather than from library-owned global state: function arguments for runtime values, consumer-owned child specs for processes, and required options on `use` for compile-time values. Each pushes the decision to the point where the library is consumed.","source_end_line":124,"domain":"elixir-design-anti-patterns","date":"2026-06-28","adr_id":6,"routing_title":"Using application configuration for libraries","display_text":"## Context\n\nThe application environment (`Application.get_env/3`, `Application.fetch_env!/2`, `Application.compile_env/3`) is a node-global key-value store, indexed by application name and key, used to parameterize values across an Elixir system. It is a useful mechanism and not an anti-pattern by itself. The anti-pattern is a library author reaching for it to configure the library's own behavior.\n\nBecause the store is global, there is exactly one value for each `{application, key}` pair on the node. A library that reads its configuration from the application environment therefore exposes exactly one configurable value to the entire world: every application that depends on the library, and every call site within those applications, shares it. Two consumers of the same library cannot configure the same aspect differently, and a single consumer cannot vary it between two calls.\n\nThe fix in every case is to source configuration from the caller rather than from library-owned global state: function arguments for runtime values, consumer-owned child specs for processes, and required options on `use` for compile-time values. Each pushes the decision to the point where the library is consumed.\n\n## Consequences\n\n- Configuration lives at the call site (keyword lists), in the consumer's own supervision tree (child specs), or in the consumer's own module (`use` options), never in node-global application environment owned by the library.\n- Multiple consumers, and multiple call sites within one consumer, configure the same library aspect independently.\n- Library behavior is determined by its arguments, so tests do not mutate shared application environment between runs and can stay `async: true`.\n- Acceptable residual uses remain: swapping a behaviour-conforming implementation, or providing customizable defaults when generating code from data files, where the chosen value does not change the outcome.\n- Mix tasks read per-project configuration via `Mix.Project.config/0` or `OptionParser` command-line arguments rather than the global application environment."}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"### Rule 1: Take runtime configuration as function arguments\n\n**Correct:**\n\n```elixir\ndefmodule DashSplitter do\n  def split(string, opts \\\\ []) when is_binary(string) and is_list(opts) do\n    parts = Keyword.get(opts, :parts, 2)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n\n# Each call site chooses its own behavior:\nDashSplitter.split(\"a-b-c-d\", parts: 3)\nDashSplitter.split(\"a-b-c-d\")\n```\n\n**Wrong:**\n\n```elixir\n# config/config.exs\nconfig :dash_splitter, parts: 3\n\ndefmodule DashSplitter do\n  def split(string) when is_binary(string) do\n    parts = Application.fetch_env!(:dash_splitter, :parts)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n```\n\n**Why:** The application environment is a node-global store indexed by `{application, key}`, so a library that calls `Application.fetch_env!(:dash_splitter, :parts)` reads exactly one slot for the entire node. Every dependent application, and every call site within them, is forced into the same `parts` value, and a single caller cannot ask for three parts here and five parts there. A keyword list parameter moves the choice to the call site: the default lives in the function head and any caller overrides it per call. The legitimate exception is using the application environment to swap one behaviour-conforming dependency for another (for example, an alternate CSV parser): that is fine because the outcome must be identical regardless of which implementation is chosen.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Take runtime configuration as function arguments"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":31,"parent_id":"elixir-design-anti-patterns:adr-006","polarity":null,"record_id":"elixir-design-anti-patterns:adr-006:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"f282ca0daf6d5c2397ed2800c8e09d1606aca99da272ca2d7bc03a7a0c773bd5","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\n### Rule 1: Take runtime configuration as function arguments\n\nWhy: The application environment is a node-global store indexed by `{application, key}`, so a library that calls `Application.fetch_env!(:dash_splitter, :parts)` reads exactly one slot for the entire node. Every dependent application, and every call site within them, is forced into the same `parts` value, and a single caller cannot ask for three parts here and five parts there. A keyword list parameter moves the choice to the call site: the default lives in the function head and any caller overrides it per call. The legitimate exception is using the application environment to swap one behaviour-conforming dependency for another (for example, an alternate CSV parser): that is fine because the outcome must be identical regardless of which implementation is chosen.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":1,"rule_title":"Take runtime configuration as function arguments","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":54,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"f2aa465a107be0cbff0e40ecb83e371084ebcb8703c29d699228f1f8cdab62f6","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule DashSplitter do\n  def split(string, opts \\\\ []) when is_binary(string) and is_list(opts) do\n    parts = Keyword.get(opts, :parts, 2)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n\n# Each call site chooses its own behavior:\nDashSplitter.split(\"a-b-c-d\", parts: 3)\nDashSplitter.split(\"a-b-c-d\")\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Take runtime configuration as function arguments","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":32,"parent_id":"elixir-design-anti-patterns:adr-006:rule-01","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-006:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"ccb19306b1bb3cab842c18fc5148e6ef0ca8e32f90c6a8f78d4b29ddbfe906a1","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-01:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 1: Take runtime configuration as function arguments\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule DashSplitter do\n  def split(string, opts \\\\ []) when is_binary(string) and is_list(opts) do\n    parts = Keyword.get(opts, :parts, 2)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n\n# Each call site chooses its own behavior:\nDashSplitter.split(\"a-b-c-d\", parts: 3)\nDashSplitter.split(\"a-b-c-d\")\n```\n\nWhy: The application environment is a node-global store indexed by `{application, key}`, so a library that calls `Application.fetch_env!(:dash_splitter, :parts)` reads exactly one slot for the entire node. Every dependent application, and every call site within them, is forced into the same `parts` value, and a single caller cannot ask for three parts here and five parts there. A keyword list parameter moves the choice to the call site: the default lives in the function head and any caller overrides it per call. The legitimate exception is using the application environment to swap one behaviour-conforming dependency for another (for example, an alternate CSV parser): that is fine because the outcome must be identical regardless of which implementation is chosen.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":1,"rule_title":"Take runtime configuration as function arguments","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":38,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"9d9a2e418446e80fe3877dfcc20e21acba560f0344028a8a352891d40d6f45c8","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# config/config.exs\nconfig :dash_splitter, parts: 3\n\ndefmodule DashSplitter do\n  def split(string) when is_binary(string) do\n    parts = Application.fetch_env!(:dash_splitter, :parts)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 1: Take runtime configuration as function arguments","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":33,"parent_id":"elixir-design-anti-patterns:adr-006:rule-01","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-006:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"63fe925b0ad6abea638181af6438fda1be7d80759a640926f989a3ab84dd19de","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-01:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 1: Take runtime configuration as function arguments\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# config/config.exs\nconfig :dash_splitter, parts: 3\n\ndefmodule DashSplitter do\n  def split(string) when is_binary(string) do\n    parts = Application.fetch_env!(:dash_splitter, :parts)\n    String.split(string, \"-\", parts: parts)\n  end\nend\n```\n\nWhy: The application environment is a node-global store indexed by `{application, key}`, so a library that calls `Application.fetch_env!(:dash_splitter, :parts)` reads exactly one slot for the entire node. Every dependent application, and every call site within them, is forced into the same `parts` value, and a single caller cannot ask for three parts here and five parts there. A keyword list parameter moves the choice to the call site: the default lives in the function head and any caller overrides it per call. The legitimate exception is using the application environment to swap one behaviour-conforming dependency for another (for example, an alternate CSV parser): that is fine because the outcome must be identical regardless of which implementation is chosen.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":1,"rule_title":"Take runtime configuration as function arguments","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":52,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"35a8b605dbbc12fcd808a006f14227d05e36182ff9ae115f575618555acf018e","source_start_line":39,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"### Rule 2: Expose a child spec instead of starting your own supervision tree\n\n**Correct:**\n\n```elixir\n# The library provides a child spec; the consumer starts it under its own tree\n# and reads its own application env only if it wants per-environment values.\nchildren = [\n  {DNSCluster, query: Application.get_env(:my_app, :dns_cluster_query) || :ignore}\n]\n\nSupervisor.start_link(children, strategy: :one_for_one)\n```\n\n**Wrong:**\n\n```elixir\n# The library starts its own tree and reads global config to parameterize it.\ndefmodule DNSCluster.Application do\n  use Application\n\n  def start(_type, _args) do\n    query = Application.fetch_env!(:dns_cluster, :query)\n    children = [{DNSCluster.Worker, query: query}]\n    Supervisor.start_link(children, strategy: :one_for_one)\n  end\nend\n```\n\n**Why:** An application starts exactly one supervision tree, so if the library starts its own and reads its options from the global application environment, those options are fixed once per node for all consumers. Exposing a child specification inverts ownership: the consumer lists the process under its own supervision tree and passes options at init time, reading its own application's environment only if and when it wants per-environment values. The library never forces the global lookup on anyone.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Expose a child spec instead of starting your own supervision tree"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-02","languages":["elixir"],"ordinal":34,"parent_id":"elixir-design-anti-patterns:adr-006","polarity":null,"record_id":"elixir-design-anti-patterns:adr-006:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"e05378de4f7d58e11820dde7ce43c96beff24b9bf079a12b81ed8f61d92060ea","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-02\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\n### Rule 2: Expose a child spec instead of starting your own supervision tree\n\nWhy: An application starts exactly one supervision tree, so if the library starts its own and reads its options from the global application environment, those options are fixed once per node for all consumers. Exposing a child specification inverts ownership: the consumer lists the process under its own supervision tree and passes options at init time, reading its own application's environment only if and when it wants per-environment values. The library never forces the global lookup on anyone.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":2,"rule_title":"Expose a child spec instead of starting your own supervision tree","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":85,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"0d9af4bc46aed2b1937180e72024c891489c95d05b38f0070424821bcf0b754a","source_start_line":55,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\n# The library provides a child spec; the consumer starts it under its own tree\n# and reads its own application env only if it wants per-environment values.\nchildren = [\n  {DNSCluster, query: Application.get_env(:my_app, :dns_cluster_query) || :ignore}\n]\n\nSupervisor.start_link(children, strategy: :one_for_one)\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Expose a child spec instead of starting your own supervision tree","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-02","languages":["elixir"],"ordinal":35,"parent_id":"elixir-design-anti-patterns:adr-006:rule-02","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-006:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"ff91f6b82766e47037b4992dec749ba4e732a839b00161c631ad5c4f55aeaf50","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-02:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 2: Expose a child spec instead of starting your own supervision tree\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\n# The library provides a child spec; the consumer starts it under its own tree\n# and reads its own application env only if it wants per-environment values.\nchildren = [\n  {DNSCluster, query: Application.get_env(:my_app, :dns_cluster_query) || :ignore}\n]\n\nSupervisor.start_link(children, strategy: :one_for_one)\n```\n\nWhy: An application starts exactly one supervision tree, so if the library starts its own and reads its options from the global application environment, those options are fixed once per node for all consumers. Exposing a child specification inverts ownership: the consumer lists the process under its own supervision tree and passes options at init time, reading its own application's environment only if and when it wants per-environment values. The library never forces the global lookup on anyone.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":2,"rule_title":"Expose a child spec instead of starting your own supervision tree","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":68,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"fb3de9aaf1acc4b4ab558533a83a5dab07a04d46a7f911ffb3cdf27f9d1ea8b3","source_start_line":57,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# The library starts its own tree and reads global config to parameterize it.\ndefmodule DNSCluster.Application do\n  use Application\n\n  def start(_type, _args) do\n    query = Application.fetch_env!(:dns_cluster, :query)\n    children = [{DNSCluster.Worker, query: query}]\n    Supervisor.start_link(children, strategy: :one_for_one)\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 2: Expose a child spec instead of starting your own supervision tree","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-02","languages":["elixir"],"ordinal":36,"parent_id":"elixir-design-anti-patterns:adr-006:rule-02","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-006:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"24cad21932dc06ed4624edf8b9a231f3ead6c9a69d708eb8c337ee8e0e0f6e07","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-02:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 2: Expose a child spec instead of starting your own supervision tree\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# The library starts its own tree and reads global config to parameterize it.\ndefmodule DNSCluster.Application do\n  use Application\n\n  def start(_type, _args) do\n    query = Application.fetch_env!(:dns_cluster, :query)\n    children = [{DNSCluster.Worker, query: query}]\n    Supervisor.start_link(children, strategy: :one_for_one)\n  end\nend\n```\n\nWhy: An application starts exactly one supervision tree, so if the library starts its own and reads its options from the global application environment, those options are fixed once per node for all consumers. Exposing a child specification inverts ownership: the consumer lists the process under its own supervision tree and passes options at init time, reading its own application's environment only if and when it wants per-environment values. The library never forces the global lookup on anyone.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":2,"rule_title":"Expose a child spec instead of starting your own supervision tree","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":83,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"2e4458da5f52ad69df92ff1802688263dabc2000921fe9ee7f8b4e948cd19f95","source_start_line":69,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"### Rule 3: Take compile-time configuration as options on `use`\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Ecto.Adapters.Postgres\nend\n\n# The consumer decides for itself whether to vary the value per environment:\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Application.compile_env(:my_app, :repo_adapter)\nend\n```\n\n**Wrong:**\n\n```elixir\n# The library bakes one global adapter at compile time for every consumer.\ndefmodule Ecto.Repo do\n  defmacro __using__(_opts) do\n    quote do\n      @adapter Application.compile_env(:ecto, :adapter)\n      # ... a single repo, configured once for the whole node\n    end\n  end\nend\n```\n\n**Why:** `Application.compile_env/3` is read once during compilation from the same node-global store and frozen into the compiled artifact, so a library that calls it internally bakes a single value for every consumer (for a repository library, that means one repo for the whole node). Requiring the value as an option on `use` generates the configured code per call site, so a consumer can define as many repositories as it needs and choose for itself whether to source each adapter from its own application environment. Code generation carries its own tradeoffs, so reserve it for configuration that genuinely must be fixed at compile time.","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 3: Take compile-time configuration as options on `use`"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-03","languages":["elixir"],"ordinal":37,"parent_id":"elixir-design-anti-patterns:adr-006","polarity":null,"record_id":"elixir-design-anti-patterns:adr-006:rule-03","record_kind":"rule","references":[],"retrieval_sha256":"3f19ccc5ebbf12eb0a11dfc16f62d6a8cf548e000008b40e75cc0b1f68114bb4","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-03\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\n### Rule 3: Take compile-time configuration as options on `use`\n\nWhy: `Application.compile_env/3` is read once during compilation from the same node-global store and frozen into the compiled artifact, so a library that calls it internally bakes a single value for every consumer (for a repository library, that means one repo for the whole node). Requiring the value as an option on `use` generates the configured code per call site, so a consumer can define as many repositories as it needs and choose for itself whether to source each adapter from its own application environment. Code generation carries its own tradeoffs, so reserve it for configuration that genuinely must be fixed at compile time.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":3,"rule_title":"Take compile-time configuration as options on `use`","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":116,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"920e742e1c8312f8baa59a2be03049c9f1aad3455e28ca5dfcb857a0a9e844de","source_start_line":86,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Ecto.Adapters.Postgres\nend\n\n# The consumer decides for itself whether to vary the value per environment:\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Application.compile_env(:my_app, :repo_adapter)\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 3: Take compile-time configuration as options on `use`","**Correct:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-03","languages":["elixir"],"ordinal":38,"parent_id":"elixir-design-anti-patterns:adr-006:rule-03","polarity":"positive","record_id":"elixir-design-anti-patterns:adr-006:rule-03:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"68dd9b40d9b0b96da911eb205b121aa220b1f1b73fbc81a215cbdf97242b3269","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-03:example:correct:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 3: Take compile-time configuration as options on `use`\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Ecto.Adapters.Postgres\nend\n\n# The consumer decides for itself whether to vary the value per environment:\ndefmodule MyApp.Repo do\n  use Ecto.Repo, adapter: Application.compile_env(:my_app, :repo_adapter)\nend\n```\n\nWhy: `Application.compile_env/3` is read once during compilation from the same node-global store and frozen into the compiled artifact, so a library that calls it internally bakes a single value for every consumer (for a repository library, that means one repo for the whole node). Requiring the value as an option on `use` generates the configured code per call site, so a consumer can define as many repositories as it needs and choose for itself whether to source each adapter from its own application environment. Code generation carries its own tradeoffs, so reserve it for configuration that genuinely must be fixed at compile time.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":3,"rule_title":"Take compile-time configuration as options on `use`","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":100,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"de38dd3711d893e020f3fe04846ef646a2e5eb4a9bba6b86524e8128408d67a9","source_start_line":88,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
{"adr_id":6,"adr_title":"Using application configuration for libraries","applies_to":{"content_match":["Application.fetch_env!","Application.get_env","Application.compile_env","use Application","config :"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# The library bakes one global adapter at compile time for every consumer.\ndefmodule Ecto.Repo do\n  defmacro __using__(_opts) do\n    quote do\n      @adapter Application.compile_env(:ecto, :adapter)\n      # ... a single repo, configured once for the whole node\n    end\n  end\nend\n```","domain":"elixir-design-anti-patterns","heading_path":["Decision","Rule 3: Take compile-time configuration as options on `use`","**Wrong:**"],"hydrate_id":"elixir-design-anti-patterns:adr-006:rule-03","languages":["elixir"],"ordinal":39,"parent_id":"elixir-design-anti-patterns:adr-006:rule-03","polarity":"negative","record_id":"elixir-design-anti-patterns:adr-006:rule-03:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"c7e37f355713aa10b45778154be139ce28ef2afc95dfa5b8049fa27951772434","retrieval_text":"Record: elixir-design-anti-patterns:adr-006:rule-03:example:wrong:01\n\nDomain: elixir-design-anti-patterns\n\nADR: Using application configuration for libraries\n\nRouting title: Using application configuration for libraries\n\nRule 3: Take compile-time configuration as options on `use`\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# The library bakes one global adapter at compile time for every consumer.\ndefmodule Ecto.Repo do\n  defmacro __using__(_opts) do\n    quote do\n      @adapter Application.compile_env(:ecto, :adapter)\n      # ... a single repo, configured once for the whole node\n    end\n  end\nend\n```\n\nWhy: `Application.compile_env/3` is read once during compilation from the same node-global store and frozen into the compiled artifact, so a library that calls it internally bakes a single value for every consumer (for a repository library, that means one repo for the whole node). Requiring the value as an option on `use` generates the configured code per call site, so a consumer can define as many repositories as it needs and choose for itself whether to source each adapter from its own application environment. Code generation carries its own tradeoffs, so reserve it for configuration that genuinely must be fixed at compile time.","routing_description":"When a library reads its own settings from Application.get_env, fetch_env!, or compile_env, move the configuration to function arguments, consumer-owned child specs, or required options on `use`.","routing_title":"Using application configuration for libraries","rule_number":3,"rule_title":"Take compile-time configuration as options on `use`","schema_version":"retrieval-v1","source_description":"A library that reads its own configuration from the application environment exposes a single node-global value per key, so every consumer and every call site is locked into one behavior. Take configuration as function arguments, consumer-owned child specs, or required options on `use` instead.","source_end_line":114,"source_path":"adrs/elixir-design-anti-patterns/adr-006-using-application-configuration-for-libraries.md","source_sha256":"7e3f24c2fdf87cd485eec3d29a331b951a7acf6acdec2883adf547ae1b078a63","source_start_line":101,"status":"accepted","tags":["elixir","anti-pattern","configuration","library-design","application-environment","supervision"],"updated":null}
