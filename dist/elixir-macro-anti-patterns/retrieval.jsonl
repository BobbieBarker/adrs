{"references":[],"source_sha256":"a65bd1feb7ad7364747cdb76dcff8a955f9777b1e0cd4bd6dc517b25764cc159","source_start_line":12,"consequences":"## Consequences\n\n- Module references passed to macros are recorded as runtime dependencies instead of compile-time ones.\n- Editing a referenced module no longer forces every using module, and its transitive graph, to recompile.\n- Incremental compile times stay bounded as the number of registered modules grows.\n- The technique applies only when the macro does not invoke functions, read structs, or inspect metadata of the module at compile time; when it must, the compile-time dependency is unavoidable.","rule_title":null,"routing_description":"When a macro stores a module reference passed as an argument for use inside a generated function, expand the literal into that function's context with Macro.expand_literals/2 so it stays a runtime dependency instead of forcing recompilation.","record_id":"elixir-macro-anti-patterns:adr-001","applies_to":{"content_match":["defmacro","unquote(mod)","@before_compile","Macro.expand_literals","Module.register_attribute","accumulate: true"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"b09b390d807cf570f41992e1970c85c6f5c6f905a1fad09e4a228d4f01d37b20","hydrate_id":"elixir-macro-anti-patterns:adr-001","status":"accepted","retrieval_text":"Record: elixir-macro-anti-patterns:adr-001\n\nDomain: elixir-macro-anti-patterns\n\nADR: Compile-time dependencies\n\nRouting title: Compile-time dependencies\n\nRouting description: When a macro stores a module reference passed as an argument for use inside a generated function, expand the literal into that function's context with Macro.expand_literals/2 so it stays a runtime dependency instead of forcing recompilation.\n\nSource description: Passing a module reference to a macro that embeds it in the module body makes the referenced module a compile-time dependency, so editing it forces dependent modules to recompile. Expand the literal into its function context with Macro.expand_literals/2 to keep it a runtime dependency.\n\n## Context\n\nMacros run at compile time, so using any macro adds a compile-time dependency from the calling module to the module that defines the macro. That dependency is expected and usually cheap.\n\nThe anti-pattern appears when the *arguments* passed to a macro also become compile-time dependencies. A macro that receives a module reference and embeds it into the module body (a module attribute, an `@before_compile` accumulator, anything outside a function clause) records that referenced module as a compile-time dependency of the caller, even when the referenced module is only invoked at runtime.\n\nA compile-time dependency forces dependent recompilation: changing the referenced module, or any module in its transitive runtime dependency graph, forces the calling module to recompile. In a builder-style API (a request pipeline, a router, a plug stack) where many modules are registered through one macro, a single edit can fan out into a large recompilation graph and dominate incremental build time.\n\n## Decision\n\n## Consequences\n\n- Module references passed to macros are recorded as runtime dependencies instead of compile-time ones.\n- Editing a referenced module no longer forces every using module, and its transitive graph, to recompile.\n- Incremental compile times stay bounded as the number of registered modules grows.\n- The technique applies only when the macro does not invoke functions, read structs, or inspect metadata of the module at compile time; when it must, the compile-time dependency is unavoidable.\n\nRule headings:\n### Rule 1: Expand module literals into the function context where they run","tags":["elixir","anti-pattern","macros","compilation","dependencies","metaprogramming"],"rule_number":null,"adr_title":"Compile-time dependencies","languages":[],"source_description":"Passing a module reference to a macro that embeds it in the module body makes the referenced module a compile-time dependency, so editing it forces dependent modules to recompile. Expand the literal into its function context with Macro.expand_literals/2 to keep it a runtime dependency.","ordinal":1,"polarity":null,"updated":null,"source_path":"adrs/elixir-macro-anti-patterns/adr-001-compile-time-dependencies.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-001: Compile-time dependencies"],"context":"## Context\n\nMacros run at compile time, so using any macro adds a compile-time dependency from the calling module to the module that defines the macro. That dependency is expected and usually cheap.\n\nThe anti-pattern appears when the *arguments* passed to a macro also become compile-time dependencies. A macro that receives a module reference and embeds it into the module body (a module attribute, an `@before_compile` accumulator, anything outside a function clause) records that referenced module as a compile-time dependency of the caller, even when the referenced module is only invoked at runtime.\n\nA compile-time dependency forces dependent recompilation: changing the referenced module, or any module in its transitive runtime dependency graph, forces the calling module to recompile. In a builder-style API (a request pipeline, a router, a plug stack) where many modules are registered through one macro, a single edit can fan out into a large recompilation graph and dominate incremental build time.","source_end_line":89,"domain":"elixir-macro-anti-patterns","date":"2026-06-28","adr_id":1,"routing_title":"Compile-time dependencies","display_text":"## Context\n\nMacros run at compile time, so using any macro adds a compile-time dependency from the calling module to the module that defines the macro. That dependency is expected and usually cheap.\n\nThe anti-pattern appears when the *arguments* passed to a macro also become compile-time dependencies. A macro that receives a module reference and embeds it into the module body (a module attribute, an `@before_compile` accumulator, anything outside a function clause) records that referenced module as a compile-time dependency of the caller, even when the referenced module is only invoked at runtime.\n\nA compile-time dependency forces dependent recompilation: changing the referenced module, or any module in its transitive runtime dependency graph, forces the calling module to recompile. In a builder-style API (a request pipeline, a router, a plug stack) where many modules are registered through one macro, a single edit can fan out into a large recompilation graph and dominate incremental build time.\n\n## Decision\n\n## Consequences\n\n- Module references passed to macros are recorded as runtime dependencies instead of compile-time ones.\n- Editing a referenced module no longer forces every using module, and its transitive graph, to recompile.\n- Incremental compile times stay bounded as the number of registered modules grows.\n- The technique applies only when the macro does not invoke functions, read structs, or inspect metadata of the module at compile time; when it must, the compile-time dependency is unavoidable."}
{"adr_id":1,"adr_title":"Compile-time dependencies","applies_to":{"content_match":["defmacro","unquote(mod)","@before_compile","Macro.expand_literals","Module.register_attribute","accumulate: true"]},"date":"2026-06-28","display_text":"### Rule 1: Expand module literals into the function context where they run\n\nWhen a macro stores a module reference for use inside a generated function, expand the literal as if it already lived in that function with `Macro.expand_literals/2`, so the reference is recorded as a runtime dependency.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    mod = Macro.expand_literals(mod, %{__CALLER__ | function: {:call, 2}})\n\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```\n\nA caller registers stages, and `@before_compile` reads `@stages` to emit a `call/2`:\n\n```elixir\ndefmodule MyApp.Web do\n  use MyApp.Pipeline\n\n  stage MyApp.Authentication\nend\n\n# generated by @before_compile:\ndef call(conn, _opts) do\n  MyApp.Authentication.call(conn)\nend\n```\n\n**Why:** In the wrong version, `stage MyApp.Authentication` runs at compile time and accumulates the module into the `@stages` attribute, outside any function body. That makes `MyApp.Authentication` a compile-time dependency of `MyApp.Web`, even though it is only ever called at runtime inside the generated `call/2`. Any change to `MyApp.Authentication`, or to its transitive runtime dependencies, then forces `MyApp.Web` to recompile. `Macro.expand_literals/2` resolves the literal as if it sat inside `call/2`, so the reference is tracked as a runtime dependency and edits to `MyApp.Authentication` no longer ripple back through the using modules. This holds only when the macro never calls a function on the module, accesses its structs, or reads its metadata at compile time. If it does any of those, the compile-time dependency is real and cannot be removed. To see which dependencies a file actually has, and whether each is compile-time, runtime, or export, run `mix xref trace path/to/file.ex`.","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Expand module literals into the function context where they run"],"hydrate_id":"elixir-macro-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":2,"parent_id":"elixir-macro-anti-patterns:adr-001","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-001:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"ecfab0f698f23ce5600e36a4365113f87b2872524649d98cf6c018c0ef5ff4e0","retrieval_text":"Record: elixir-macro-anti-patterns:adr-001:rule-01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Compile-time dependencies\n\n### Rule 1: Expand module literals into the function context where they run\n\nWhen a macro stores a module reference for use inside a generated function, expand the literal as if it already lived in that function with `Macro.expand_literals/2`, so the reference is recorded as a runtime dependency.\n\nWhy: In the wrong version, `stage MyApp.Authentication` runs at compile time and accumulates the module into the `@stages` attribute, outside any function body. That makes `MyApp.Authentication` a compile-time dependency of `MyApp.Web`, even though it is only ever called at runtime inside the generated `call/2`. Any change to `MyApp.Authentication`, or to its transitive runtime dependencies, then forces `MyApp.Web` to recompile. `Macro.expand_literals/2` resolves the literal as if it sat inside `call/2`, so the reference is tracked as a runtime dependency and edits to `MyApp.Authentication` no longer ripple back through the using modules. This holds only when the macro never calls a function on the module, accesses its structs, or reads its metadata at compile time. If it does any of those, the compile-time dependency is real and cannot be removed. To see which dependencies a file actually has, and whether each is compile-time, runtime, or export, run `mix xref trace path/to/file.ex`.","routing_description":"When a macro stores a module reference passed as an argument for use inside a generated function, expand the literal into that function's context with Macro.expand_literals/2 so it stays a runtime dependency instead of forcing recompilation.","routing_title":"Compile-time dependencies","rule_number":1,"rule_title":"Expand module literals into the function context where they run","schema_version":"retrieval-v1","source_description":"Passing a module reference to a macro that embeds it in the module body makes the referenced module a compile-time dependency, so editing it forces dependent modules to recompile. Expand the literal into its function context with Macro.expand_literals/2 to keep it a runtime dependency.","source_end_line":82,"source_path":"adrs/elixir-macro-anti-patterns/adr-001-compile-time-dependencies.md","source_sha256":"dcb9c6b8138b8790cb20f1fc31645ae62abc8be324943a7a7b4d492ebf9370e2","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","macros","compilation","dependencies","metaprogramming"],"updated":null}
{"adr_id":1,"adr_title":"Compile-time dependencies","applies_to":{"content_match":["defmacro","unquote(mod)","@before_compile","Macro.expand_literals","Module.register_attribute","accumulate: true"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    mod = Macro.expand_literals(mod, %{__CALLER__ | function: {:call, 2}})\n\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Expand module literals into the function context where they run","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":3,"parent_id":"elixir-macro-anti-patterns:adr-001:rule-01","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-001:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"92a33875977370d81b6ba92c2748d3b29c0ea38f027f2d8d9c836275d038264a","retrieval_text":"Record: elixir-macro-anti-patterns:adr-001:rule-01:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Compile-time dependencies\n\nRouting title: Compile-time dependencies\n\nRule 1: Expand module literals into the function context where they run\n\nCorrect example (positive)\n\nWhen a macro stores a module reference for use inside a generated function, expand the literal as if it already lived in that function with `Macro.expand_literals/2`, so the reference is recorded as a runtime dependency.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    mod = Macro.expand_literals(mod, %{__CALLER__ | function: {:call, 2}})\n\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```\n\nWhy: In the wrong version, `stage MyApp.Authentication` runs at compile time and accumulates the module into the `@stages` attribute, outside any function body. That makes `MyApp.Authentication` a compile-time dependency of `MyApp.Web`, even though it is only ever called at runtime inside the generated `call/2`. Any change to `MyApp.Authentication`, or to its transitive runtime dependencies, then forces `MyApp.Web` to recompile. `Macro.expand_literals/2` resolves the literal as if it sat inside `call/2`, so the reference is tracked as a runtime dependency and edits to `MyApp.Authentication` no longer ripple back through the using modules. This holds only when the macro never calls a function on the module, accesses its structs, or reads its metadata at compile time. If it does any of those, the compile-time dependency is real and cannot be removed. To see which dependencies a file actually has, and whether each is compile-time, runtime, or export, run `mix xref trace path/to/file.ex`.","routing_description":"When a macro stores a module reference passed as an argument for use inside a generated function, expand the literal into that function's context with Macro.expand_literals/2 so it stays a runtime dependency instead of forcing recompilation.","routing_title":"Compile-time dependencies","rule_number":1,"rule_title":"Expand module literals into the function context where they run","schema_version":"retrieval-v1","source_description":"Passing a module reference to a macro that embeds it in the module body makes the referenced module a compile-time dependency, so editing it forces dependent modules to recompile. Expand the literal into its function context with Macro.expand_literals/2 to keep it a runtime dependency.","source_end_line":46,"source_path":"adrs/elixir-macro-anti-patterns/adr-001-compile-time-dependencies.md","source_sha256":"c284ce14ba4cec68e51014f8d7a0fff3c838b07710c295d5df15e95caa5b605e","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","macros","compilation","dependencies","metaprogramming"],"updated":null}
{"adr_id":1,"adr_title":"Compile-time dependencies","applies_to":{"content_match":["defmacro","unquote(mod)","@before_compile","Macro.expand_literals","Module.register_attribute","accumulate: true"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```\n\nA caller registers stages, and `@before_compile` reads `@stages` to emit a `call/2`:\n\n```elixir\ndefmodule MyApp.Web do\n  use MyApp.Pipeline\n\n  stage MyApp.Authentication\nend\n\n# generated by @before_compile:\ndef call(conn, _opts) do\n  MyApp.Authentication.call(conn)\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Expand module literals into the function context where they run","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":4,"parent_id":"elixir-macro-anti-patterns:adr-001:rule-01","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-001:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"36779470b2b9a0967a25e90b42cc542015a181a855a5c30f74945fddbd652d14","retrieval_text":"Record: elixir-macro-anti-patterns:adr-001:rule-01:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Compile-time dependencies\n\nRouting title: Compile-time dependencies\n\nRule 1: Expand module literals into the function context where they run\n\nWrong example (negative)\n\nWhen a macro stores a module reference for use inside a generated function, expand the literal as if it already lived in that function with `Macro.expand_literals/2`, so the reference is recorded as a runtime dependency.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Pipeline do\n  defmacro __using__(_opts) do\n    quote do\n      Module.register_attribute(__MODULE__, :stages, accumulate: true)\n      @before_compile MyApp.Pipeline\n    end\n  end\n\n  defmacro stage(mod) do\n    quote do\n      @stages unquote(mod)\n    end\n  end\nend\n```\n\nA caller registers stages, and `@before_compile` reads `@stages` to emit a `call/2`:\n\n```elixir\ndefmodule MyApp.Web do\n  use MyApp.Pipeline\n\n  stage MyApp.Authentication\nend\n\n# generated by @before_compile:\ndef call(conn, _opts) do\n  MyApp.Authentication.call(conn)\nend\n```\n\nWhy: In the wrong version, `stage MyApp.Authentication` runs at compile time and accumulates the module into the `@stages` attribute, outside any function body. That makes `MyApp.Authentication` a compile-time dependency of `MyApp.Web`, even though it is only ever called at runtime inside the generated `call/2`. Any change to `MyApp.Authentication`, or to its transitive runtime dependencies, then forces `MyApp.Web` to recompile. `Macro.expand_literals/2` resolves the literal as if it sat inside `call/2`, so the reference is tracked as a runtime dependency and edits to `MyApp.Authentication` no longer ripple back through the using modules. This holds only when the macro never calls a function on the module, accesses its structs, or reads its metadata at compile time. If it does any of those, the compile-time dependency is real and cannot be removed. To see which dependencies a file actually has, and whether each is compile-time, runtime, or export, run `mix xref trace path/to/file.ex`.","routing_description":"When a macro stores a module reference passed as an argument for use inside a generated function, expand the literal into that function's context with Macro.expand_literals/2 so it stays a runtime dependency instead of forcing recompilation.","routing_title":"Compile-time dependencies","rule_number":1,"rule_title":"Expand module literals into the function context where they run","schema_version":"retrieval-v1","source_description":"Passing a module reference to a macro that embeds it in the module body makes the referenced module a compile-time dependency, so editing it forces dependent modules to recompile. Expand the literal into its function context with Macro.expand_literals/2 to keep it a runtime dependency.","source_end_line":80,"source_path":"adrs/elixir-macro-anti-patterns/adr-001-compile-time-dependencies.md","source_sha256":"f7d2f9f7a4c6bbd3e3584a723a271ea69fd8d69086eb9fcdd511a5b9ae16e69a","source_start_line":47,"status":"accepted","tags":["elixir","anti-pattern","macros","compilation","dependencies","metaprogramming"],"updated":null}
{"references":[],"source_sha256":"467efc1465b196339ea7240a1c5ff7178d220a1e43697a4e8ffcbcda5ddbbd50","source_start_line":12,"consequences":"## Consequences\n\n- Each call site expands to a fixed-size function call, so macro expansion cost stays flat as the number of invocations grows.\n- The bulk logic compiles once into one named function; host-module bytecode and `.beam` size no longer scale with call count.\n- Compilation time scales with call sites times a small stub instead of call sites times the full body.\n- The extracted function is callable and testable on its own, unlike logic buried in quoted AST.","rule_title":null,"routing_description":"When a macro's quote block contains substantial logic and is invoked many times, move the body into a regular function the macro calls so it compiles once instead of per call site.","record_id":"elixir-macro-anti-patterns:adr-002","applies_to":{"content_match":["defmacro","quote do","unquote(","Module.put_attribute","@store_route_for_compilation"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"e98bd12bf4750916b6e6a6fbf09d1a2542ef5161cdbff02c799de016e6ece32c","hydrate_id":"elixir-macro-anti-patterns:adr-002","status":"accepted","retrieval_text":"Record: elixir-macro-anti-patterns:adr-002\n\nDomain: elixir-macro-anti-patterns\n\nADR: Large code generation\n\nRouting title: Large code generation\n\nRouting description: When a macro's quote block contains substantial logic and is invoked many times, move the body into a regular function the macro calls so it compiles once instead of per call site.\n\nSource description: A macro injects its quoted body into the caller at every invocation, and the compiler compiles each copy, so a large quoted body multiplied across many call sites slows compilation and bloats the .beam file. Quote only a call to a regular function and put the bulk logic in that function.\n\n## Context\n\nA macro runs at compile time and injects its quoted AST into the calling module at every invocation site. The compiler then expands and compiles that injected code once per call site. When the quoted body is large and the macro is called many times, the cost multiplies: a web router with hundreds of `get/2` calls produces hundreds of independent copies of the macro body, each expanded and compiled into the host module's bytecode.\n\nTwo concrete costs follow. Compilation slows, because the compiler walks and compiles every copy of the generated code rather than one shared definition. The compiled artifact grows, because each expansion is duplicated into the `.beam` file; module size scales with the number of call sites times the size of the quoted body.\n\nThe fix is to keep the quoted body small. A macro only needs to generate code for the work that genuinely requires compile-time context (capturing `__MODULE__`, the literal arguments, or an attribute target). Everything else (validation, transformation, side effects) can run in an ordinary function the macro calls, so that logic is compiled once and dispatched to.\n\n## Decision\n\n## Consequences\n\n- Each call site expands to a fixed-size function call, so macro expansion cost stays flat as the number of invocations grows.\n- The bulk logic compiles once into one named function; host-module bytecode and `.beam` size no longer scale with call count.\n- Compilation time scales with call sites times a small stub instead of call sites times the full body.\n- The extracted function is callable and testable on its own, unlike logic buried in quoted AST.\n\nRule headings:\n### Rule 1: Quote a function call, not the function body","tags":["elixir","anti-pattern","macros","metaprogramming","compile-time","code-generation"],"rule_number":null,"adr_title":"Large code generation","languages":[],"source_description":"A macro injects its quoted body into the caller at every invocation, and the compiler compiles each copy, so a large quoted body multiplied across many call sites slows compilation and bloats the .beam file. Quote only a call to a regular function and put the bulk logic in that function.","ordinal":5,"polarity":null,"updated":null,"source_path":"adrs/elixir-macro-anti-patterns/adr-002-large-code-generation.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-002: Large code generation"],"context":"## Context\n\nA macro runs at compile time and injects its quoted AST into the calling module at every invocation site. The compiler then expands and compiles that injected code once per call site. When the quoted body is large and the macro is called many times, the cost multiplies: a web router with hundreds of `get/2` calls produces hundreds of independent copies of the macro body, each expanded and compiled into the host module's bytecode.\n\nTwo concrete costs follow. Compilation slows, because the compiler walks and compiles every copy of the generated code rather than one shared definition. The compiled artifact grows, because each expansion is duplicated into the `.beam` file; module size scales with the number of call sites times the size of the quoted body.\n\nThe fix is to keep the quoted body small. A macro only needs to generate code for the work that genuinely requires compile-time context (capturing `__MODULE__`, the literal arguments, or an attribute target). Everything else (validation, transformation, side effects) can run in an ordinary function the macro calls, so that logic is compiled once and dispatched to.","source_end_line":81,"domain":"elixir-macro-anti-patterns","date":"2026-06-28","adr_id":2,"routing_title":"Large code generation","display_text":"## Context\n\nA macro runs at compile time and injects its quoted AST into the calling module at every invocation site. The compiler then expands and compiles that injected code once per call site. When the quoted body is large and the macro is called many times, the cost multiplies: a web router with hundreds of `get/2` calls produces hundreds of independent copies of the macro body, each expanded and compiled into the host module's bytecode.\n\nTwo concrete costs follow. Compilation slows, because the compiler walks and compiles every copy of the generated code rather than one shared definition. The compiled artifact grows, because each expansion is duplicated into the `.beam` file; module size scales with the number of call sites times the size of the quoted body.\n\nThe fix is to keep the quoted body small. A macro only needs to generate code for the work that genuinely requires compile-time context (capturing `__MODULE__`, the literal arguments, or an attribute target). Everything else (validation, transformation, side effects) can run in an ordinary function the macro calls, so that logic is compiled once and dispatched to.\n\n## Decision\n\n## Consequences\n\n- Each call site expands to a fixed-size function call, so macro expansion cost stays flat as the number of invocations grows.\n- The bulk logic compiles once into one named function; host-module bytecode and `.beam` size no longer scale with call count.\n- Compilation time scales with call sites times a small stub instead of call sites times the full body.\n- The extracted function is callable and testable on its own, unlike logic buried in quoted AST."}
{"adr_id":2,"adr_title":"Large code generation","applies_to":{"content_match":["defmacro","quote do","unquote(","Module.put_attribute","@store_route_for_compilation"]},"date":"2026-06-28","display_text":"### Rule 1: Quote a function call, not the function body\n\nThe macro's `quote` block should expand to a single call into a regular function, passing the unquoted arguments. Put the bulk of the work in that function.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      MyApp.Router.__define__(__MODULE__, unquote(route), unquote(handler))\n    end\n  end\n\n  def __define__(module, route, handler) do\n    if not is_binary(route) do\n      raise ArgumentError, \"route must be a binary\"\n    end\n\n    if not is_atom(handler) do\n      raise ArgumentError, \"handler must be a module\"\n    end\n\n    Module.put_attribute(module, :store_route_for_compilation, {route, handler})\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      route = unquote(route)\n      handler = unquote(handler)\n\n      if not is_binary(route) do\n        raise ArgumentError, \"route must be a binary\"\n      end\n\n      if not is_atom(handler) do\n        raise ArgumentError, \"handler must be a module\"\n      end\n\n      @store_route_for_compilation {route, handler}\n    end\n  end\nend\n```\n\n**Why:** In the wrong version the entire validation-and-store body is the quoted AST, so it is inlined into the host module at every `get/2` call. A router with hundreds of routes expands and compiles hundreds of full copies of that body; compilation time and the resulting `.beam` size grow with the call count. In the correct version the `quote` injects one small call. The validation and `Module.put_attribute/3` logic is compiled exactly once as `__define__/3`, and each call site costs a single fixed-size dispatch regardless of how much work the function does. The macro is still justified here because it needs compile-time context (`__MODULE__` and the literal route); if a construct needs none of that, it should be a plain function rather than a macro (see ADR-003).","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Quote a function call, not the function body"],"hydrate_id":"elixir-macro-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":6,"parent_id":"elixir-macro-anti-patterns:adr-002","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-002:rule-01","record_kind":"rule","references":[{"raw_text":"ADR-003","source_line":73,"target_id":"elixir-macro-anti-patterns:adr-003"}],"retrieval_sha256":"d42aa6002f3b76f1e4527c206649647bdf54f7d5a55de81bbc95d24a8e86ec58","retrieval_text":"Record: elixir-macro-anti-patterns:adr-002:rule-01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Large code generation\n\n### Rule 1: Quote a function call, not the function body\n\nThe macro's `quote` block should expand to a single call into a regular function, passing the unquoted arguments. Put the bulk of the work in that function.\n\nWhy: In the wrong version the entire validation-and-store body is the quoted AST, so it is inlined into the host module at every `get/2` call. A router with hundreds of routes expands and compiles hundreds of full copies of that body; compilation time and the resulting `.beam` size grow with the call count. In the correct version the `quote` injects one small call. The validation and `Module.put_attribute/3` logic is compiled exactly once as `__define__/3`, and each call site costs a single fixed-size dispatch regardless of how much work the function does. The macro is still justified here because it needs compile-time context (`__MODULE__` and the literal route); if a construct needs none of that, it should be a plain function rather than a macro (see ADR-003).","routing_description":"When a macro's quote block contains substantial logic and is invoked many times, move the body into a regular function the macro calls so it compiles once instead of per call site.","routing_title":"Large code generation","rule_number":1,"rule_title":"Quote a function call, not the function body","schema_version":"retrieval-v1","source_description":"A macro injects its quoted body into the caller at every invocation, and the compiler compiles each copy, so a large quoted body multiplied across many call sites slows compilation and bloats the .beam file. Quote only a call to a regular function and put the bulk logic in that function.","source_end_line":74,"source_path":"adrs/elixir-macro-anti-patterns/adr-002-large-code-generation.md","source_sha256":"30ccec268c36fc610d4fc34709f05a2b63cc2ea5cde1a73056f57918d1544507","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","macros","metaprogramming","compile-time","code-generation"],"updated":null}
{"adr_id":2,"adr_title":"Large code generation","applies_to":{"content_match":["defmacro","quote do","unquote(","Module.put_attribute","@store_route_for_compilation"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      MyApp.Router.__define__(__MODULE__, unquote(route), unquote(handler))\n    end\n  end\n\n  def __define__(module, route, handler) do\n    if not is_binary(route) do\n      raise ArgumentError, \"route must be a binary\"\n    end\n\n    if not is_atom(handler) do\n      raise ArgumentError, \"handler must be a module\"\n    end\n\n    Module.put_attribute(module, :store_route_for_compilation, {route, handler})\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Quote a function call, not the function body","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":7,"parent_id":"elixir-macro-anti-patterns:adr-002:rule-01","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-002:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"23c93f114c122a02554284d746d16c5ad26f2c7f48f7e43e17dc341e01f97c51","retrieval_text":"Record: elixir-macro-anti-patterns:adr-002:rule-01:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Large code generation\n\nRouting title: Large code generation\n\nRule 1: Quote a function call, not the function body\n\nCorrect example (positive)\n\nThe macro's `quote` block should expand to a single call into a regular function, passing the unquoted arguments. Put the bulk of the work in that function.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      MyApp.Router.__define__(__MODULE__, unquote(route), unquote(handler))\n    end\n  end\n\n  def __define__(module, route, handler) do\n    if not is_binary(route) do\n      raise ArgumentError, \"route must be a binary\"\n    end\n\n    if not is_atom(handler) do\n      raise ArgumentError, \"handler must be a module\"\n    end\n\n    Module.put_attribute(module, :store_route_for_compilation, {route, handler})\n  end\nend\n```\n\nWhy: In the wrong version the entire validation-and-store body is the quoted AST, so it is inlined into the host module at every `get/2` call. A router with hundreds of routes expands and compiles hundreds of full copies of that body; compilation time and the resulting `.beam` size grow with the call count. In the correct version the `quote` injects one small call. The validation and `Module.put_attribute/3` logic is compiled exactly once as `__define__/3`, and each call site costs a single fixed-size dispatch regardless of how much work the function does. The macro is still justified here because it needs compile-time context (`__MODULE__` and the literal route); if a construct needs none of that, it should be a plain function rather than a macro (see ADR-003).","routing_description":"When a macro's quote block contains substantial logic and is invoked many times, move the body into a regular function the macro calls so it compiles once instead of per call site.","routing_title":"Large code generation","rule_number":1,"rule_title":"Quote a function call, not the function body","schema_version":"retrieval-v1","source_description":"A macro injects its quoted body into the caller at every invocation, and the compiler compiles each copy, so a large quoted body multiplied across many call sites slows compilation and bloats the .beam file. Quote only a call to a regular function and put the bulk logic in that function.","source_end_line":49,"source_path":"adrs/elixir-macro-anti-patterns/adr-002-large-code-generation.md","source_sha256":"1dc31c230cafeac409b1e4db9d9f1a46b12aa7d33bbe22fe40851fc90e502629","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","macros","metaprogramming","compile-time","code-generation"],"updated":null}
{"adr_id":2,"adr_title":"Large code generation","applies_to":{"content_match":["defmacro","quote do","unquote(","Module.put_attribute","@store_route_for_compilation"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      route = unquote(route)\n      handler = unquote(handler)\n\n      if not is_binary(route) do\n        raise ArgumentError, \"route must be a binary\"\n      end\n\n      if not is_atom(handler) do\n        raise ArgumentError, \"handler must be a module\"\n      end\n\n      @store_route_for_compilation {route, handler}\n    end\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Quote a function call, not the function body","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":8,"parent_id":"elixir-macro-anti-patterns:adr-002:rule-01","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-002:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"e5245604a70c2766837ca37c165cf84ba60e5db10ab0942f777627f218a25d1d","retrieval_text":"Record: elixir-macro-anti-patterns:adr-002:rule-01:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Large code generation\n\nRouting title: Large code generation\n\nRule 1: Quote a function call, not the function body\n\nWrong example (negative)\n\nThe macro's `quote` block should expand to a single call into a regular function, passing the unquoted arguments. Put the bulk of the work in that function.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Router do\n  defmacro get(route, handler) do\n    quote do\n      route = unquote(route)\n      handler = unquote(handler)\n\n      if not is_binary(route) do\n        raise ArgumentError, \"route must be a binary\"\n      end\n\n      if not is_atom(handler) do\n        raise ArgumentError, \"handler must be a module\"\n      end\n\n      @store_route_for_compilation {route, handler}\n    end\n  end\nend\n```\n\nWhy: In the wrong version the entire validation-and-store body is the quoted AST, so it is inlined into the host module at every `get/2` call. A router with hundreds of routes expands and compiles hundreds of full copies of that body; compilation time and the resulting `.beam` size grow with the call count. In the correct version the `quote` injects one small call. The validation and `Module.put_attribute/3` logic is compiled exactly once as `__define__/3`, and each call site costs a single fixed-size dispatch regardless of how much work the function does. The macro is still justified here because it needs compile-time context (`__MODULE__` and the literal route); if a construct needs none of that, it should be a plain function rather than a macro (see ADR-003).","routing_description":"When a macro's quote block contains substantial logic and is invoked many times, move the body into a regular function the macro calls so it compiles once instead of per call site.","routing_title":"Large code generation","rule_number":1,"rule_title":"Quote a function call, not the function body","schema_version":"retrieval-v1","source_description":"A macro injects its quoted body into the caller at every invocation, and the compiler compiles each copy, so a large quoted body multiplied across many call sites slows compilation and bloats the .beam file. Quote only a call to a regular function and put the bulk logic in that function.","source_end_line":72,"source_path":"adrs/elixir-macro-anti-patterns/adr-002-large-code-generation.md","source_sha256":"15d286549ddf245828f4dd764e6b21ee032e9cf9415c5ad6509903fe008dac4d","source_start_line":50,"status":"accepted","tags":["elixir","anti-pattern","macros","metaprogramming","compile-time","code-generation"],"updated":null}
{"references":[{"raw_text":"ADR-001","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-001"},{"raw_text":"ADR-005","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-005"}],"source_sha256":"a8bdbafc6df5bbf45dd7c0a654c5f7316d24d03d27769572d57aff1c3a9b6a51","source_start_line":12,"consequences":"## Consequences\n\n- `defmacro` is reserved for code that must inspect or transform unevaluated AST, so its presence signals genuine meta-programming rather than an ordinary computation in disguise.\n- The logic stays plain runtime code: directly testable in isolation and composable with the rest of the language, with no quoting, unquoting, or macro hygiene to get wrong.\n- Readers and tooling reason about the call as data flow, not as an expansion they must perform in their heads.","rule_title":null,"routing_description":"When a defmacro only computes over its already-evaluated arguments, replace it with a named function to drop the require dependency and inline expansion.","record_id":"elixir-macro-anti-patterns:adr-003","applies_to":{"content_match":["defmacro","quote do","unquote(","require "]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"1cd82e71e6bb62cfae93ed3de26eef6f281dbd9a7db2cc621e1ab082991b9614","hydrate_id":"elixir-macro-anti-patterns:adr-003","status":"accepted","retrieval_text":"Record: elixir-macro-anti-patterns:adr-003\n\nDomain: elixir-macro-anti-patterns\n\nADR: Unnecessary macros\n\nRouting title: Unnecessary macros\n\nRouting description: When a defmacro only computes over its already-evaluated arguments, replace it with a named function to drop the require dependency and inline expansion.\n\nSource description: A macro receives unevaluated AST at compile time and expands inline at every call site, forcing callers to `require` it; a function receives values at runtime and is shared as one compiled body. Use `defmacro` only when the code must manipulate unevaluated code, otherwise a named function is simpler and carries no compile-time dependency.\n\n## Context\n\nA macro is meta-programming. Its arguments arrive as quoted AST at compile time, its return value is also AST, and that AST is spliced into the call site and compiled in place. A function is different: it receives already-evaluated values at runtime and runs as one shared compiled body. The two mechanisms are not interchangeable. A macro is the right tool only when the code must inspect or transform unevaluated code before it runs (deferred evaluation, AST inspection, generating definitions).\n\nReaching for `defmacro` where a plain function would do adds cost and removes capability for nothing. Every caller must `require` the defining module, which creates a compile-time dependency on it (ADR-001), and when that dependency goes untracked it silently goes stale (ADR-005). The macro body expands into each call site instead of living in one place, and the macro is not a runtime value, so it cannot be captured with `&`, passed as an argument, or piped. Because macros are harder to write, read, and reason about, indiscriminate use compromises maintainability while buying nothing in return.\n\n## Decision\n\n## Consequences\n\n- `defmacro` is reserved for code that must inspect or transform unevaluated AST, so its presence signals genuine meta-programming rather than an ordinary computation in disguise.\n- The logic stays plain runtime code: directly testable in isolation and composable with the rest of the language, with no quoting, unquoting, or macro hygiene to get wrong.\n- Readers and tooling reason about the call as data flow, not as an expansion they must perform in their heads.\n\nRule headings:\n### Rule 1: Use a named function unless the code must manipulate unevaluated code","tags":["elixir","anti-pattern","macros","meta-programming","compile-time"],"rule_number":null,"adr_title":"Unnecessary macros","languages":[],"source_description":"A macro receives unevaluated AST at compile time and expands inline at every call site, forcing callers to `require` it; a function receives values at runtime and is shared as one compiled body. Use `defmacro` only when the code must manipulate unevaluated code, otherwise a named function is simpler and carries no compile-time dependency.","ordinal":9,"polarity":null,"updated":null,"source_path":"adrs/elixir-macro-anti-patterns/adr-003-unnecessary-macros.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-003: Unnecessary macros"],"context":"## Context\n\nA macro is meta-programming. Its arguments arrive as quoted AST at compile time, its return value is also AST, and that AST is spliced into the call site and compiled in place. A function is different: it receives already-evaluated values at runtime and runs as one shared compiled body. The two mechanisms are not interchangeable. A macro is the right tool only when the code must inspect or transform unevaluated code before it runs (deferred evaluation, AST inspection, generating definitions).\n\nReaching for `defmacro` where a plain function would do adds cost and removes capability for nothing. Every caller must `require` the defining module, which creates a compile-time dependency on it (ADR-001), and when that dependency goes untracked it silently goes stale (ADR-005). The macro body expands into each call site instead of living in one place, and the macro is not a runtime value, so it cannot be captured with `&`, passed as an argument, or piped. Because macros are harder to write, read, and reason about, indiscriminate use compromises maintainability while buying nothing in return.","source_end_line":55,"domain":"elixir-macro-anti-patterns","date":"2026-06-28","adr_id":3,"routing_title":"Unnecessary macros","display_text":"## Context\n\nA macro is meta-programming. Its arguments arrive as quoted AST at compile time, its return value is also AST, and that AST is spliced into the call site and compiled in place. A function is different: it receives already-evaluated values at runtime and runs as one shared compiled body. The two mechanisms are not interchangeable. A macro is the right tool only when the code must inspect or transform unevaluated code before it runs (deferred evaluation, AST inspection, generating definitions).\n\nReaching for `defmacro` where a plain function would do adds cost and removes capability for nothing. Every caller must `require` the defining module, which creates a compile-time dependency on it (ADR-001), and when that dependency goes untracked it silently goes stale (ADR-005). The macro body expands into each call site instead of living in one place, and the macro is not a runtime value, so it cannot be captured with `&`, passed as an argument, or piped. Because macros are harder to write, read, and reason about, indiscriminate use compromises maintainability while buying nothing in return.\n\n## Decision\n\n## Consequences\n\n- `defmacro` is reserved for code that must inspect or transform unevaluated AST, so its presence signals genuine meta-programming rather than an ordinary computation in disguise.\n- The logic stays plain runtime code: directly testable in isolation and composable with the rest of the language, with no quoting, unquoting, or macro hygiene to get wrong.\n- Readers and tooling reason about the call as data flow, not as an expansion they must perform in their heads."}
{"adr_id":3,"adr_title":"Unnecessary macros","applies_to":{"content_match":["defmacro","quote do","unquote(","require "]},"date":"2026-06-28","display_text":"### Rule 1: Use a named function unless the code must manipulate unevaluated code\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Math do\n  def sum(a, b), do: a + b\nend\n\nMyApp.Math.sum(3, 5)\nMyApp.Math.sum(3 + 1, 5 + 6)\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Math do\n  defmacro sum(a, b) do\n    quote do\n      unquote(a) + unquote(b)\n    end\n  end\nend\n\nrequire MyApp.Math\nMyApp.Math.sum(3, 5)\n```\n\n**Why:** The macro computes nothing the function cannot; both produce `8`. But `sum/2` as a macro runs at compile time on the AST of its arguments, forces every caller to `require MyApp.Math`, and expands `unquote(a) + unquote(b)` inline at each call site instead of sharing one compiled body. That `require` is a compile-time dependency (ADR-001). The function version drops the `require`, exists as a real runtime value (capturable as `&MyApp.Math.sum/2`, passable, pipeable), and is simpler to read, test, and reason about. Reserve `defmacro` for code that must operate on unevaluated AST.","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use a named function unless the code must manipulate unevaluated code"],"hydrate_id":"elixir-macro-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":10,"parent_id":"elixir-macro-anti-patterns:adr-003","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-003:rule-01","record_kind":"rule","references":[{"raw_text":"ADR-001","source_line":48,"target_id":"elixir-macro-anti-patterns:adr-001"}],"retrieval_sha256":"b21394b4baa8463ba38020d20f0f319cdf2532356cb97c4dee614b82383f1e5d","retrieval_text":"Record: elixir-macro-anti-patterns:adr-003:rule-01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Unnecessary macros\n\n### Rule 1: Use a named function unless the code must manipulate unevaluated code\n\nWhy: The macro computes nothing the function cannot; both produce `8`. But `sum/2` as a macro runs at compile time on the AST of its arguments, forces every caller to `require MyApp.Math`, and expands `unquote(a) + unquote(b)` inline at each call site instead of sharing one compiled body. That `require` is a compile-time dependency (ADR-001). The function version drops the `require`, exists as a real runtime value (capturable as `&MyApp.Math.sum/2`, passable, pipeable), and is simpler to read, test, and reason about. Reserve `defmacro` for code that must operate on unevaluated AST.","routing_description":"When a defmacro only computes over its already-evaluated arguments, replace it with a named function to drop the require dependency and inline expansion.","routing_title":"Unnecessary macros","rule_number":1,"rule_title":"Use a named function unless the code must manipulate unevaluated code","schema_version":"retrieval-v1","source_description":"A macro receives unevaluated AST at compile time and expands inline at every call site, forcing callers to `require` it; a function receives values at runtime and is shared as one compiled body. Use `defmacro` only when the code must manipulate unevaluated code, otherwise a named function is simpler and carries no compile-time dependency.","source_end_line":49,"source_path":"adrs/elixir-macro-anti-patterns/adr-003-unnecessary-macros.md","source_sha256":"7b46490e90271b59cc17a799e0730f8404a6fdba4ab42574ce123fcf84871f58","source_start_line":20,"status":"accepted","tags":["elixir","anti-pattern","macros","meta-programming","compile-time"],"updated":null}
{"adr_id":3,"adr_title":"Unnecessary macros","applies_to":{"content_match":["defmacro","quote do","unquote(","require "]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Math do\n  def sum(a, b), do: a + b\nend\n\nMyApp.Math.sum(3, 5)\nMyApp.Math.sum(3 + 1, 5 + 6)\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use a named function unless the code must manipulate unevaluated code","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":11,"parent_id":"elixir-macro-anti-patterns:adr-003:rule-01","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-003:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"59d02e3a9e11dae7349cb0be81a761e9427a17b3bc86d941d77f2c0ed21610fd","retrieval_text":"Record: elixir-macro-anti-patterns:adr-003:rule-01:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Unnecessary macros\n\nRouting title: Unnecessary macros\n\nRule 1: Use a named function unless the code must manipulate unevaluated code\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Math do\n  def sum(a, b), do: a + b\nend\n\nMyApp.Math.sum(3, 5)\nMyApp.Math.sum(3 + 1, 5 + 6)\n```\n\nWhy: The macro computes nothing the function cannot; both produce `8`. But `sum/2` as a macro runs at compile time on the AST of its arguments, forces every caller to `require MyApp.Math`, and expands `unquote(a) + unquote(b)` inline at each call site instead of sharing one compiled body. That `require` is a compile-time dependency (ADR-001). The function version drops the `require`, exists as a real runtime value (capturable as `&MyApp.Math.sum/2`, passable, pipeable), and is simpler to read, test, and reason about. Reserve `defmacro` for code that must operate on unevaluated AST.","routing_description":"When a defmacro only computes over its already-evaluated arguments, replace it with a named function to drop the require dependency and inline expansion.","routing_title":"Unnecessary macros","rule_number":1,"rule_title":"Use a named function unless the code must manipulate unevaluated code","schema_version":"retrieval-v1","source_description":"A macro receives unevaluated AST at compile time and expands inline at every call site, forcing callers to `require` it; a function receives values at runtime and is shared as one compiled body. Use `defmacro` only when the code must manipulate unevaluated code, otherwise a named function is simpler and carries no compile-time dependency.","source_end_line":32,"source_path":"adrs/elixir-macro-anti-patterns/adr-003-unnecessary-macros.md","source_sha256":"c596775f2f8f75fa2fa2e6dc129a396e1b6cec114706e8af4714b2e361f1d1bd","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","macros","meta-programming","compile-time"],"updated":null}
{"adr_id":3,"adr_title":"Unnecessary macros","applies_to":{"content_match":["defmacro","quote do","unquote(","require "]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Math do\n  defmacro sum(a, b) do\n    quote do\n      unquote(a) + unquote(b)\n    end\n  end\nend\n\nrequire MyApp.Math\nMyApp.Math.sum(3, 5)\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use a named function unless the code must manipulate unevaluated code","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":12,"parent_id":"elixir-macro-anti-patterns:adr-003:rule-01","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-003:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"209c0b269625d58d3537a2a35d74202e9f5e73402135eddee6dcfbb439b840a8","retrieval_text":"Record: elixir-macro-anti-patterns:adr-003:rule-01:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Unnecessary macros\n\nRouting title: Unnecessary macros\n\nRule 1: Use a named function unless the code must manipulate unevaluated code\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Math do\n  defmacro sum(a, b) do\n    quote do\n      unquote(a) + unquote(b)\n    end\n  end\nend\n\nrequire MyApp.Math\nMyApp.Math.sum(3, 5)\n```\n\nWhy: The macro computes nothing the function cannot; both produce `8`. But `sum/2` as a macro runs at compile time on the AST of its arguments, forces every caller to `require MyApp.Math`, and expands `unquote(a) + unquote(b)` inline at each call site instead of sharing one compiled body. That `require` is a compile-time dependency (ADR-001). The function version drops the `require`, exists as a real runtime value (capturable as `&MyApp.Math.sum/2`, passable, pipeable), and is simpler to read, test, and reason about. Reserve `defmacro` for code that must operate on unevaluated AST.","routing_description":"When a defmacro only computes over its already-evaluated arguments, replace it with a named function to drop the require dependency and inline expansion.","routing_title":"Unnecessary macros","rule_number":1,"rule_title":"Use a named function unless the code must manipulate unevaluated code","schema_version":"retrieval-v1","source_description":"A macro receives unevaluated AST at compile time and expands inline at every call site, forcing callers to `require` it; a function receives values at runtime and is shared as one compiled body. Use `defmacro` only when the code must manipulate unevaluated code, otherwise a named function is simpler and carries no compile-time dependency.","source_end_line":47,"source_path":"adrs/elixir-macro-anti-patterns/adr-003-unnecessary-macros.md","source_sha256":"afe9a1fe631433649d3f09bd1f937180ce0d0e9c67031bb388362fab06650e29","source_start_line":33,"status":"accepted","tags":["elixir","anti-pattern","macros","meta-programming","compile-time"],"updated":null}
{"references":[{"raw_text":"ADR-001","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-001"},{"raw_text":"ADR-005","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-005"},{"raw_text":"ADR-002","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-002"},{"raw_text":"ADR-003","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-003"},{"raw_text":"ADR-005","source_line":123,"target_id":"elixir-macro-anti-patterns:adr-005"}],"source_sha256":"4ac743a16592fb5fc510711ec67ecd0b99312f8887f50d090435d089d21e1ec6","source_start_line":12,"consequences":"## Consequences\n\n- Plain delegation uses lexically scoped `import`/`alias`; `use` is reserved for cases that genuinely require code injection.\n- Name conflicts surface at the call site instead of being introduced by hidden propagated imports.\n- The modules a file compile-depends on are visible in its directives rather than buried inside a `__using__/1` body (ADR-005).\n- Where `use` remains, the moduledoc nutrition facts label documents exactly what it injects into the caller's public API.","rule_title":null,"routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","record_id":"elixir-macro-anti-patterns:adr-004","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"93d2a44e736a35b87a30b92af7a4aceb60eaa8b7d2625750516b8c6b6078f90d","hydrate_id":"elixir-macro-anti-patterns:adr-004","status":"accepted","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\nRouting title: `use` instead of `import`\n\nRouting description: When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.\n\nSource description: `use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.\n\n## Context\n\n`import`, `alias`, and `use` all establish dependencies between modules, but `use` is categorically different. `import` and `alias` are lexically scoped directives: they shorten name resolution inside the file that writes them and generate no code. `use M, opts` instead expands at compile time to `require M; M.__using__(opts)`, and `__using__/1` is a macro whose returned quoted expression is spliced verbatim into the calling module.\n\nBecause that injected expression is arbitrary, it can import modules, alias modules, define functions, register attributes, and declare behaviours, all invisibly. The `use M` line at the call site reveals none of it. Reading it requires reading the internal details of `__using__/1` in `M`. This broad scope is the smell: an injected `import` silently shadows or conflicts with the caller's own functions, and a propagated `import` makes the caller compile-depend on a module it never named (the compile-time dependency machinery is ADR-001 and ADR-005). When the injected block grows large it compounds into ADR-002, and the question of whether the macro should exist at all is ADR-003.\n\n## Decision\n\n## Consequences\n\n- Plain delegation uses lexically scoped `import`/`alias`; `use` is reserved for cases that genuinely require code injection.\n- Name conflicts surface at the call site instead of being introduced by hidden propagated imports.\n- The modules a file compile-depends on are visible in its directives rather than buried inside a `__using__/1` body (ADR-005).\n- Where `use` remains, the moduledoc nutrition facts label documents exactly what it injects into the caller's public API.\n\nRule headings:\n### Rule 1: Use `import`/`alias` when no code injection is needed\n### Rule 2: When `use` is genuinely required, publish a nutrition facts label","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"rule_number":null,"adr_title":"`use` instead of `import`","languages":[],"source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","ordinal":13,"polarity":null,"updated":null,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-004: `use` instead of `import`"],"context":"## Context\n\n`import`, `alias`, and `use` all establish dependencies between modules, but `use` is categorically different. `import` and `alias` are lexically scoped directives: they shorten name resolution inside the file that writes them and generate no code. `use M, opts` instead expands at compile time to `require M; M.__using__(opts)`, and `__using__/1` is a macro whose returned quoted expression is spliced verbatim into the calling module.\n\nBecause that injected expression is arbitrary, it can import modules, alias modules, define functions, register attributes, and declare behaviours, all invisibly. The `use M` line at the call site reveals none of it. Reading it requires reading the internal details of `__using__/1` in `M`. This broad scope is the smell: an injected `import` silently shadows or conflicts with the caller's own functions, and a propagated `import` makes the caller compile-depend on a module it never named (the compile-time dependency machinery is ADR-001 and ADR-005). When the injected block grows large it compounds into ADR-002, and the question of whether the macro should exist at all is ADR-003.","source_end_line":125,"domain":"elixir-macro-anti-patterns","date":"2026-06-28","adr_id":4,"routing_title":"`use` instead of `import`","display_text":"## Context\n\n`import`, `alias`, and `use` all establish dependencies between modules, but `use` is categorically different. `import` and `alias` are lexically scoped directives: they shorten name resolution inside the file that writes them and generate no code. `use M, opts` instead expands at compile time to `require M; M.__using__(opts)`, and `__using__/1` is a macro whose returned quoted expression is spliced verbatim into the calling module.\n\nBecause that injected expression is arbitrary, it can import modules, alias modules, define functions, register attributes, and declare behaviours, all invisibly. The `use M` line at the call site reveals none of it. Reading it requires reading the internal details of `__using__/1` in `M`. This broad scope is the smell: an injected `import` silently shadows or conflicts with the caller's own functions, and a propagated `import` makes the caller compile-depend on a module it never named (the compile-time dependency machinery is ADR-001 and ADR-005). When the injected block grows large it compounds into ADR-002, and the question of whether the macro should exist at all is ADR-003.\n\n## Decision\n\n## Consequences\n\n- Plain delegation uses lexically scoped `import`/`alias`; `use` is reserved for cases that genuinely require code injection.\n- Name conflicts surface at the call site instead of being introduced by hidden propagated imports.\n- The modules a file compile-depends on are visible in its directives rather than buried inside a `__using__/1` body (ADR-005).\n- Where `use` remains, the moduledoc nutrition facts label documents exactly what it injects into the caller's public API."}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"### Rule 1: Use `import`/`alias` when no code injection is needed\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Library do\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  import MyApp.Library\n\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Helpers do\n  def foo, do: \"from helpers\"\nend\n\ndefmodule MyApp.Library do\n  defmacro __using__(_opts) do\n    quote do\n      import MyApp.Library\n      import MyApp.Helpers   # propagates a dependency the caller never named\n    end\n  end\n\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  use MyApp.Library\n\n  # ** (CompileError) imported MyApp.Helpers.foo/0 conflicts with local function\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```\n\n**Why:** `use MyApp.Library` expands to `MyApp.Library.__using__([])`, and the macro's quoted body (two `import` directives) is spliced into `MyApp.Client`. Nothing at the `use` line states that `MyApp.Helpers` is now imported, so `foo/0` collides with the local definition and the module fails to compile. The caller also picks up a compile-time dependency on every propagated import (ADR-001, ADR-005). `import` and `alias` are lexically scoped: they only affect name resolution in the file that writes them and emit no code, so the dependency set and the available names are exactly what the directives list.","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use `import`/`alias` when no code injection is needed"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":14,"parent_id":"elixir-macro-anti-patterns:adr-004","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-004:rule-01","record_kind":"rule","references":[{"raw_text":"ADR-001","source_line":66,"target_id":"elixir-macro-anti-patterns:adr-001"},{"raw_text":"ADR-005","source_line":66,"target_id":"elixir-macro-anti-patterns:adr-005"}],"retrieval_sha256":"4cad951f075ced84d8ff4e19d8c9f228ca9c7e31c077748c3cf8bef58c932892","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-01\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\n### Rule 1: Use `import`/`alias` when no code injection is needed\n\nWhy: `use MyApp.Library` expands to `MyApp.Library.__using__([])`, and the macro's quoted body (two `import` directives) is spliced into `MyApp.Client`. Nothing at the `use` line states that `MyApp.Helpers` is now imported, so `foo/0` collides with the local definition and the module fails to compile. The caller also picks up a compile-time dependency on every propagated import (ADR-001, ADR-005). `import` and `alias` are lexically scoped: they only affect name resolution in the file that writes them and emit no code, so the dependency set and the available names are exactly what the directives list.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":1,"rule_title":"Use `import`/`alias` when no code injection is needed","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":67,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"37fd3774efe8887165fae5442841af50a0f832f236f1b2207345f06f99b9f28a","source_start_line":20,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Library do\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  import MyApp.Library\n\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use `import`/`alias` when no code injection is needed","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":15,"parent_id":"elixir-macro-anti-patterns:adr-004:rule-01","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-004:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"90125d36be92e116d2cd73b92b5b004347a94c74b18af638f37d89ca2b6841bb","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-01:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\nRouting title: `use` instead of `import`\n\nRule 1: Use `import`/`alias` when no code injection is needed\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Library do\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  import MyApp.Library\n\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```\n\nWhy: `use MyApp.Library` expands to `MyApp.Library.__using__([])`, and the macro's quoted body (two `import` directives) is spliced into `MyApp.Client`. Nothing at the `use` line states that `MyApp.Helpers` is now imported, so `foo/0` collides with the local definition and the module fails to compile. The caller also picks up a compile-time dependency on every propagated import (ADR-001, ADR-005). `import` and `alias` are lexically scoped: they only affect name resolution in the file that writes them and emit no code, so the dependency set and the available names are exactly what the directives list.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":1,"rule_title":"Use `import`/`alias` when no code injection is needed","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":37,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"eb9d6933c1139d66b564968d88c06f50d644d2a3f74f6cc6b8bef371de59a091","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Helpers do\n  def foo, do: \"from helpers\"\nend\n\ndefmodule MyApp.Library do\n  defmacro __using__(_opts) do\n    quote do\n      import MyApp.Library\n      import MyApp.Helpers   # propagates a dependency the caller never named\n    end\n  end\n\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  use MyApp.Library\n\n  # ** (CompileError) imported MyApp.Helpers.foo/0 conflicts with local function\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Use `import`/`alias` when no code injection is needed","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":16,"parent_id":"elixir-macro-anti-patterns:adr-004:rule-01","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-004:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"0f1b4dffd8ef38324cefda6f9c936b0b6997ae5411bb455efc978e64fd58b0c8","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-01:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\nRouting title: `use` instead of `import`\n\nRule 1: Use `import`/`alias` when no code injection is needed\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Helpers do\n  def foo, do: \"from helpers\"\nend\n\ndefmodule MyApp.Library do\n  defmacro __using__(_opts) do\n    quote do\n      import MyApp.Library\n      import MyApp.Helpers   # propagates a dependency the caller never named\n    end\n  end\n\n  def from_lib, do: \"from library\"\nend\n\ndefmodule MyApp.Client do\n  use MyApp.Library\n\n  # ** (CompileError) imported MyApp.Helpers.foo/0 conflicts with local function\n  def foo, do: \"local client function\"\n\n  def run, do: from_lib() <> \" - \" <> foo()\nend\n```\n\nWhy: `use MyApp.Library` expands to `MyApp.Library.__using__([])`, and the macro's quoted body (two `import` directives) is spliced into `MyApp.Client`. Nothing at the `use` line states that `MyApp.Helpers` is now imported, so `foo/0` collides with the local definition and the module fails to compile. The caller also picks up a compile-time dependency on every propagated import (ADR-001, ADR-005). `import` and `alias` are lexically scoped: they only affect name resolution in the file that writes them and emit no code, so the dependency set and the available names are exactly what the directives list.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":1,"rule_title":"Use `import`/`alias` when no code injection is needed","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":65,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"14b6389dd0ca9e1543b22a1a9b71224bfd134182f64f77956c463170f45e6fc5","source_start_line":38,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"### Rule 2: When `use` is genuinely required, publish a nutrition facts label\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"\"\"\n  A supervised worker base.\n\n  > #### `use MyApp.Worker` {: .info}\n  >\n  > When you `use MyApp.Worker`, it sets `@behaviour MyApp.Worker`\n  > and defines a `child_spec/1` function so your module can be\n  > started directly under a supervisor.\n  \"\"\"\n\n  @callback handle_job(term()) :: :ok\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"A supervised worker base.\"\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n      import MyApp.Worker.Internals\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```\n\n**Why:** Some cases legitimately need injection: declaring a behaviour, defining `child_spec/1`, registering attributes (whether the macro is warranted at all is ADR-003). What the wrong version omits is any record of it. A developer reading `use MyApp.Worker` cannot see that a behaviour, a public `child_spec/1`, and an `import MyApp.Worker.Internals` were added without opening `__using__/1`. The `{: .info}` admonition is the documented contract for the names `use` injects into the caller. List only public-API additions: a private attribute such as `@_worker_meta` stays out of the label.","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When `use` is genuinely required, publish a nutrition facts label"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":17,"parent_id":"elixir-macro-anti-patterns:adr-004","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-004:rule-02","record_kind":"rule","references":[{"raw_text":"ADR-003","source_line":117,"target_id":"elixir-macro-anti-patterns:adr-003"}],"retrieval_sha256":"0b846747934057d900455d32b63d9f65e9b919f343d2da00a1360d58d5f5fb82","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-02\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\n### Rule 2: When `use` is genuinely required, publish a nutrition facts label\n\nWhy: Some cases legitimately need injection: declaring a behaviour, defining `child_spec/1`, registering attributes (whether the macro is warranted at all is ADR-003). What the wrong version omits is any record of it. A developer reading `use MyApp.Worker` cannot see that a behaviour, a public `child_spec/1`, and an `import MyApp.Worker.Internals` were added without opening `__using__/1`. The `{: .info}` admonition is the documented contract for the names `use` injects into the caller. List only public-API additions: a private attribute such as `@_worker_meta` stays out of the label.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":2,"rule_title":"When `use` is genuinely required, publish a nutrition facts label","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":118,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"8475024b89a7cc80df13ab4975d1707812f85e3f88a139988e9b5dfb530e59c1","source_start_line":68,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"\"\"\n  A supervised worker base.\n\n  > #### `use MyApp.Worker` {: .info}\n  >\n  > When you `use MyApp.Worker`, it sets `@behaviour MyApp.Worker`\n  > and defines a `child_spec/1` function so your module can be\n  > started directly under a supervisor.\n  \"\"\"\n\n  @callback handle_job(term()) :: :ok\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When `use` is genuinely required, publish a nutrition facts label","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":18,"parent_id":"elixir-macro-anti-patterns:adr-004:rule-02","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-004:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"6675f25f7227b2b55b3f05ecae6fac91d1ed559c691c3e05ecdcbbd3fecb0706","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-02:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\nRouting title: `use` instead of `import`\n\nRule 2: When `use` is genuinely required, publish a nutrition facts label\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"\"\"\n  A supervised worker base.\n\n  > #### `use MyApp.Worker` {: .info}\n  >\n  > When you `use MyApp.Worker`, it sets `@behaviour MyApp.Worker`\n  > and defines a `child_spec/1` function so your module can be\n  > started directly under a supervisor.\n  \"\"\"\n\n  @callback handle_job(term()) :: :ok\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```\n\nWhy: Some cases legitimately need injection: declaring a behaviour, defining `child_spec/1`, registering attributes (whether the macro is warranted at all is ADR-003). What the wrong version omits is any record of it. A developer reading `use MyApp.Worker` cannot see that a behaviour, a public `child_spec/1`, and an `import MyApp.Worker.Internals` were added without opening `__using__/1`. The `{: .info}` admonition is the documented contract for the names `use` injects into the caller. List only public-API additions: a private attribute such as `@_worker_meta` stays out of the label.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":2,"rule_title":"When `use` is genuinely required, publish a nutrition facts label","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":97,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"900f38a65a32a406c5d3913fa1b4f390842fc66bdf1fbe3235a79064250536f3","source_start_line":70,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"adr_id":4,"adr_title":"`use` instead of `import`","applies_to":{"content_match":["defmacro __using__","__using__(_opts)","__using__(opts)","use MyApp","quote do"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"A supervised worker base.\"\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n      import MyApp.Worker.Internals\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When `use` is genuinely required, publish a nutrition facts label","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":19,"parent_id":"elixir-macro-anti-patterns:adr-004:rule-02","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-004:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"55b2bd4e439b9630619d22d8e3009f315944e021ffe0b0ec6863b6c2900f071c","retrieval_text":"Record: elixir-macro-anti-patterns:adr-004:rule-02:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: `use` instead of `import`\n\nRouting title: `use` instead of `import`\n\nRule 2: When `use` is genuinely required, publish a nutrition facts label\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Worker do\n  @moduledoc \"A supervised worker base.\"\n\n  defmacro __using__(_opts) do\n    quote do\n      @behaviour MyApp.Worker\n      import MyApp.Worker.Internals\n\n      def child_spec(arg) do\n        %{id: __MODULE__, start: {__MODULE__, :start_link, [arg]}}\n      end\n    end\n  end\nend\n```\n\nWhy: Some cases legitimately need injection: declaring a behaviour, defining `child_spec/1`, registering attributes (whether the macro is warranted at all is ADR-003). What the wrong version omits is any record of it. A developer reading `use MyApp.Worker` cannot see that a behaviour, a public `child_spec/1`, and an `import MyApp.Worker.Internals` were added without opening `__using__/1`. The `{: .info}` admonition is the documented contract for the names `use` injects into the caller. List only public-API additions: a private attribute such as `@_worker_meta` stays out of the label.","routing_description":"When a library exposes a `__using__/1` that only imports or aliases other modules, replace `use` with explicit `import`/`alias` at the call site.","routing_title":"`use` instead of `import`","rule_number":2,"rule_title":"When `use` is genuinely required, publish a nutrition facts label","schema_version":"retrieval-v1","source_description":"`use M` expands at compile time to `M.__using__/1`, whose quoted body is injected verbatim into the caller, hiding propagated imports, behaviours, and definitions and creating compile-time dependencies. Prefer lexically scoped `import`/`alias` when no code injection is required.","source_end_line":116,"source_path":"adrs/elixir-macro-anti-patterns/adr-004-use-instead-of-import.md","source_sha256":"571c5d06c6ff2dac2b1220d747dbd41b183a983ca6817a494ea58f8c4442ef6f","source_start_line":98,"status":"accepted","tags":["elixir","anti-pattern","macros","use","code-generation","modularity"],"updated":null}
{"references":[{"raw_text":"ADR-001","source_line":16,"target_id":"elixir-macro-anti-patterns:adr-001"}],"source_sha256":"8e10908fa9794bdd3641181dc69253641edcfb69a1c31d5bb9e3c49803e3c8fe","source_start_line":12,"consequences":"## Consequences\n\n- Editing any dispatched-to module recompiles its callers; incremental builds stay consistent and never call a stale compiled version.\n- `mix xref trace` shows the intended compile-time edges to each concrete module instead of a single edge to the parent namespace.\n- Dynamic dispatch over a known set of modules stays expressible, either as a literal alias list or via a compile-time macro, without sacrificing dependency tracking.\n- The silent stale-build class of bug caused by runtime name construction is removed.","rule_title":null,"routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","record_id":"elixir-macro-anti-patterns:adr-005","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"1f4cf1ec053e32394cb3ffc9f26c525f1ddac56e0ed4090d7ba659aea72e87f6","hydrate_id":"elixir-macro-anti-patterns:adr-005","status":"accepted","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\nRouting title: Untracked compile-time dependencies\n\nRouting description: When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.\n\nSource description: Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.\n\n## Context\n\nAn Elixir alias such as `OtherModule.Foo` is just the atom `:\"Elixir.OtherModule.Foo\"`. When that alias appears literally in source, even outside any function and even inside a dynamic dispatch like `mod.example()`, the compiler records it in the calling module's compile-time dependency set. That set drives incremental compilation: when `OtherModule.Foo` changes, every module holding a compile-time dependency on it is recompiled.\n\nThis anti-pattern is the inverse of ADR-001 Compile-time dependencies. There the problem is too many tracked compile-time edges forcing wide recompilation; here the problem is an edge the compiler cannot see at all. It appears when a module name is built programmatically, either via `Module.concat/2` or by writing the underlying atom directly, instead of being written as a literal alias. The constructed name is identical at runtime, but the literal alias never enters the AST, so the dependency tracker never records it. The compiler sees a dependency only on the parent namespace `OtherModule`, never on `OtherModule.Foo` or `OtherModule.Bar`.\n\nThe failure is silent and surfaces as a stale build: editing `OtherModule.Foo` does not recompile the module that calls it, so the caller keeps invoking an outdated compiled version until a full rebuild. `mix xref trace path/to/file.ex` reports which modules a file depends on and whether each edge is compile-time, runtime, or export, and is the tool for confirming a dependency is actually tracked.\n\n## Decision\n\n## Consequences\n\n- Editing any dispatched-to module recompiles its callers; incremental builds stay consistent and never call a stale compiled version.\n- `mix xref trace` shows the intended compile-time edges to each concrete module instead of a single edge to the parent namespace.\n- Dynamic dispatch over a known set of modules stays expressible, either as a literal alias list or via a compile-time macro, without sacrificing dependency tracking.\n- The silent stale-build class of bug caused by runtime name construction is removed.\n\nRule headings:\n### Rule 1: Reference modules by their literal alias\n### Rule 2: When a name must be built dynamically, build it inside a macro","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"rule_number":null,"adr_title":"Untracked compile-time dependencies","languages":[],"source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","ordinal":20,"polarity":null,"updated":null,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-005: Untracked compile-time dependencies"],"context":"## Context\n\nAn Elixir alias such as `OtherModule.Foo` is just the atom `:\"Elixir.OtherModule.Foo\"`. When that alias appears literally in source, even outside any function and even inside a dynamic dispatch like `mod.example()`, the compiler records it in the calling module's compile-time dependency set. That set drives incremental compilation: when `OtherModule.Foo` changes, every module holding a compile-time dependency on it is recompiled.\n\nThis anti-pattern is the inverse of ADR-001 Compile-time dependencies. There the problem is too many tracked compile-time edges forcing wide recompilation; here the problem is an edge the compiler cannot see at all. It appears when a module name is built programmatically, either via `Module.concat/2` or by writing the underlying atom directly, instead of being written as a literal alias. The constructed name is identical at runtime, but the literal alias never enters the AST, so the dependency tracker never records it. The compiler sees a dependency only on the parent namespace `OtherModule`, never on `OtherModule.Foo` or `OtherModule.Bar`.\n\nThe failure is silent and surfaces as a stale build: editing `OtherModule.Foo` does not recompile the module that calls it, so the caller keeps invoking an outdated compiled version until a full rebuild. `mix xref trace path/to/file.ex` reports which modules a file depends on and whether each edge is compile-time, runtime, or export, and is the tool for confirming a dependency is actually tracked.","source_end_line":92,"domain":"elixir-macro-anti-patterns","date":"2026-06-28","adr_id":5,"routing_title":"Untracked compile-time dependencies","display_text":"## Context\n\nAn Elixir alias such as `OtherModule.Foo` is just the atom `:\"Elixir.OtherModule.Foo\"`. When that alias appears literally in source, even outside any function and even inside a dynamic dispatch like `mod.example()`, the compiler records it in the calling module's compile-time dependency set. That set drives incremental compilation: when `OtherModule.Foo` changes, every module holding a compile-time dependency on it is recompiled.\n\nThis anti-pattern is the inverse of ADR-001 Compile-time dependencies. There the problem is too many tracked compile-time edges forcing wide recompilation; here the problem is an edge the compiler cannot see at all. It appears when a module name is built programmatically, either via `Module.concat/2` or by writing the underlying atom directly, instead of being written as a literal alias. The constructed name is identical at runtime, but the literal alias never enters the AST, so the dependency tracker never records it. The compiler sees a dependency only on the parent namespace `OtherModule`, never on `OtherModule.Foo` or `OtherModule.Bar`.\n\nThe failure is silent and surfaces as a stale build: editing `OtherModule.Foo` does not recompile the module that calls it, so the caller keeps invoking an outdated compiled version until a full rebuild. `mix xref trace path/to/file.ex` reports which modules a file depends on and whether each edge is compile-time, runtime, or export, and is the tool for confirming a dependency is actually tracked.\n\n## Decision\n\n## Consequences\n\n- Editing any dispatched-to module recompiles its callers; incremental builds stay consistent and never call a stale compiled version.\n- `mix xref trace` shows the intended compile-time edges to each concrete module instead of a single edge to the parent namespace.\n- Dynamic dispatch over a known set of modules stays expressible, either as a literal alias list or via a compile-time macro, without sacrificing dependency tracking.\n- The silent stale-build class of bug caused by runtime name construction is removed."}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"### Rule 1: Reference modules by their literal alias\n\n**Correct:**\n\n```elixir\ndefmodule MyModule do\n  mods = [OtherModule.Foo, OtherModule.Bar]\n\n  for mod <- mods do\n    mod.example()\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```\n\n**Why:** Even though the `for` dispatches dynamically, the literal aliases `OtherModule.Foo` and `OtherModule.Bar` appear in source outside any function, so the compiler records a compile-time dependency on each and recompiles `MyModule` when either changes. `Module.concat(OtherModule, part)` produces the same atom at runtime, but the compiler sees only `OtherModule`; the identical hole opens if you write the atom directly as `:\"Elixir.OtherModule.Foo\"`. A later edit to `OtherModule.Foo` then leaves `MyModule` compiled against a stale version with no recompilation triggered.","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Reference modules by their literal alias"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":21,"parent_id":"elixir-macro-anti-patterns:adr-005","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-005:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"394580cdc7492659c5936fa144f2102f5e16f26d8727cacfe89c051051670462","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\n### Rule 1: Reference modules by their literal alias\n\nWhy: Even though the `for` dispatches dynamically, the literal aliases `OtherModule.Foo` and `OtherModule.Bar` appear in source outside any function, so the compiler records a compile-time dependency on each and recompiles `MyModule` when either changes. `Module.concat(OtherModule, part)` produces the same atom at runtime, but the compiler sees only `OtherModule`; the identical hole opens if you write the atom directly as `:\"Elixir.OtherModule.Foo\"`. A later edit to `OtherModule.Foo` then leaves `MyModule` compiled against a stale version with no recompilation triggered.","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":1,"rule_title":"Reference modules by their literal alias","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":49,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"356c088ff45138ab882810af846520bb67ffa97ebf6305fd39d3a036921a92ee","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyModule do\n  mods = [OtherModule.Foo, OtherModule.Bar]\n\n  for mod <- mods do\n    mod.example()\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Reference modules by their literal alias","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":22,"parent_id":"elixir-macro-anti-patterns:adr-005:rule-01","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-005:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"da41f5070caa786d20c0a135a31e255262fbe87d9c5d911be91d3383626ce74a","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-01:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\nRouting title: Untracked compile-time dependencies\n\nRule 1: Reference modules by their literal alias\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyModule do\n  mods = [OtherModule.Foo, OtherModule.Bar]\n\n  for mod <- mods do\n    mod.example()\n  end\nend\n```\n\nWhy: Even though the `for` dispatches dynamically, the literal aliases `OtherModule.Foo` and `OtherModule.Bar` appear in source outside any function, so the compiler records a compile-time dependency on each and recompiles `MyModule` when either changes. `Module.concat(OtherModule, part)` produces the same atom at runtime, but the compiler sees only `OtherModule`; the identical hole opens if you write the atom directly as `:\"Elixir.OtherModule.Foo\"`. A later edit to `OtherModule.Foo` then leaves `MyModule` compiled against a stale version with no recompilation triggered.","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":1,"rule_title":"Reference modules by their literal alias","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":35,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"433b7f4417d4826bec7f247039540e73146de7896c5c0bfa97234acea2589b5c","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 1: Reference modules by their literal alias","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":23,"parent_id":"elixir-macro-anti-patterns:adr-005:rule-01","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-005:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"8370f2b7df98054349bf1bdf317c9c972596329748f497a1a503b0b431f441e3","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-01:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\nRouting title: Untracked compile-time dependencies\n\nRule 1: Reference modules by their literal alias\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```\n\nWhy: Even though the `for` dispatches dynamically, the literal aliases `OtherModule.Foo` and `OtherModule.Bar` appear in source outside any function, so the compiler records a compile-time dependency on each and recompiles `MyModule` when either changes. `Module.concat(OtherModule, part)` produces the same atom at runtime, but the compiler sees only `OtherModule`; the identical hole opens if you write the atom directly as `:\"Elixir.OtherModule.Foo\"`. A later edit to `OtherModule.Foo` then leaves `MyModule` compiled against a stale version with no recompilation triggered.","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":1,"rule_title":"Reference modules by their literal alias","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":47,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"d9ebce0ac1a05c3209ecb390104abba99daee1f299c65b638b33b2d28a7275cc","source_start_line":36,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"### Rule 2: When a name must be built dynamically, build it inside a macro\n\n**Correct:**\n\n```elixir\ndefmodule MyMacro do\n  defmacro call_examples(parts) do\n    for part <- parts do\n      quote do\n        # Builds the literal alias OtherModule.Foo at expansion time\n        OtherModule.unquote(part).example()\n      end\n    end\n  end\nend\n\ndefmodule MyModule do\n  import MyMacro\n  call_examples([:Foo, :Bar])\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```\n\n**Why:** `OtherModule.unquote(part)` is spliced during macro expansion, so the expanded AST of `MyModule` contains the literal aliases `OtherModule.Foo` and `OtherModule.Bar` before dependency tracking runs. The edges are recorded exactly as if the aliases had been typed by hand. `Module.concat/2` instead builds the atom at runtime, after dependency tracking has finished, so nothing is recorded. This is a legitimate use of a macro, the one job being to move name construction from runtime to compile time (ADR-003 covers when a macro is unnecessary).","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When a name must be built dynamically, build it inside a macro"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":24,"parent_id":"elixir-macro-anti-patterns:adr-005","polarity":null,"record_id":"elixir-macro-anti-patterns:adr-005:rule-02","record_kind":"rule","references":[{"raw_text":"ADR-003","source_line":84,"target_id":"elixir-macro-anti-patterns:adr-003"}],"retrieval_sha256":"353182818e68235c76cd346a3f62a5f224b51204cfa4ffd39ec9cd53944f61a7","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-02\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\n### Rule 2: When a name must be built dynamically, build it inside a macro\n\nWhy: `OtherModule.unquote(part)` is spliced during macro expansion, so the expanded AST of `MyModule` contains the literal aliases `OtherModule.Foo` and `OtherModule.Bar` before dependency tracking runs. The edges are recorded exactly as if the aliases had been typed by hand. `Module.concat/2` instead builds the atom at runtime, after dependency tracking has finished, so nothing is recorded. This is a legitimate use of a macro, the one job being to move name construction from runtime to compile time (ADR-003 covers when a macro is unnecessary).","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":2,"rule_title":"When a name must be built dynamically, build it inside a macro","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":85,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"9c0e6abbe2834635d3fdfa088ea8a34cad1b60dcd58600c1ff647dd8976d7b8b","source_start_line":50,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyMacro do\n  defmacro call_examples(parts) do\n    for part <- parts do\n      quote do\n        # Builds the literal alias OtherModule.Foo at expansion time\n        OtherModule.unquote(part).example()\n      end\n    end\n  end\nend\n\ndefmodule MyModule do\n  import MyMacro\n  call_examples([:Foo, :Bar])\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When a name must be built dynamically, build it inside a macro","**Correct:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":25,"parent_id":"elixir-macro-anti-patterns:adr-005:rule-02","polarity":"positive","record_id":"elixir-macro-anti-patterns:adr-005:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"a5286ed5fe871961e08feaed4e395bff796ed755a58d7237bf53a9f4d95751d6","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-02:example:correct:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\nRouting title: Untracked compile-time dependencies\n\nRule 2: When a name must be built dynamically, build it inside a macro\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyMacro do\n  defmacro call_examples(parts) do\n    for part <- parts do\n      quote do\n        # Builds the literal alias OtherModule.Foo at expansion time\n        OtherModule.unquote(part).example()\n      end\n    end\n  end\nend\n\ndefmodule MyModule do\n  import MyMacro\n  call_examples([:Foo, :Bar])\nend\n```\n\nWhy: `OtherModule.unquote(part)` is spliced during macro expansion, so the expanded AST of `MyModule` contains the literal aliases `OtherModule.Foo` and `OtherModule.Bar` before dependency tracking runs. The edges are recorded exactly as if the aliases had been typed by hand. `Module.concat/2` instead builds the atom at runtime, after dependency tracking has finished, so nothing is recorded. This is a legitimate use of a macro, the one job being to move name construction from runtime to compile time (ADR-003 covers when a macro is unnecessary).","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":2,"rule_title":"When a name must be built dynamically, build it inside a macro","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":71,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"c073e0bc1b1dd05367586658017454e8191e80076945cc8a43a7ba99aab9d8a1","source_start_line":52,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
{"adr_id":5,"adr_title":"Untracked compile-time dependencies","applies_to":{"content_match":["Module.concat","Module.concat(","Module.safe_concat",":\"Elixir."]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```","domain":"elixir-macro-anti-patterns","heading_path":["Decision","Rule 2: When a name must be built dynamically, build it inside a macro","**Wrong:**"],"hydrate_id":"elixir-macro-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":26,"parent_id":"elixir-macro-anti-patterns:adr-005:rule-02","polarity":"negative","record_id":"elixir-macro-anti-patterns:adr-005:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"a516d9bd42b2066e98139c67739e170ccbff5532994f8c9ebf33fb470772feff","retrieval_text":"Record: elixir-macro-anti-patterns:adr-005:rule-02:example:wrong:01\n\nDomain: elixir-macro-anti-patterns\n\nADR: Untracked compile-time dependencies\n\nRouting title: Untracked compile-time dependencies\n\nRule 2: When a name must be built dynamically, build it inside a macro\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyModule do\n  parts = [:Foo, :Bar]\n\n  for part <- parts do\n    Module.concat(OtherModule, part).example()\n  end\nend\n```\n\nWhy: `OtherModule.unquote(part)` is spliced during macro expansion, so the expanded AST of `MyModule` contains the literal aliases `OtherModule.Foo` and `OtherModule.Bar` before dependency tracking runs. The edges are recorded exactly as if the aliases had been typed by hand. `Module.concat/2` instead builds the atom at runtime, after dependency tracking has finished, so nothing is recorded. This is a legitimate use of a macro, the one job being to move name construction from runtime to compile time (ADR-003 covers when a macro is unnecessary).","routing_description":"When dispatching to modules whose names are built dynamically, write them as literal aliases or construct them inside a macro so the compiler tracks the compile-time dependency.","routing_title":"Untracked compile-time dependencies","rule_number":2,"rule_title":"When a name must be built dynamically, build it inside a macro","schema_version":"retrieval-v1","source_description":"Building module names programmatically with Module.concat/2 or raw atoms hides the alias from the compiler's dependency tracker, so dependent files are not recompiled when the target module changes and the build silently goes stale. Write module names as literal aliases, or construct them inside a macro so the alias enters the AST at expansion time.","source_end_line":83,"source_path":"adrs/elixir-macro-anti-patterns/adr-005-untracked-compile-time-dependencies.md","source_sha256":"d9ebce0ac1a05c3209ecb390104abba99daee1f299c65b638b33b2d28a7275cc","source_start_line":72,"status":"accepted","tags":["elixir","anti-pattern","macros","compile-time-dependencies","metaprogramming","recompilation"],"updated":null}
