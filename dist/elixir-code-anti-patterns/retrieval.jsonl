{"references":[],"source_sha256":"d839293e6ffb984393f908372d06717f6fc5a54b5cd508f0a7e7c49b12bb2743","source_start_line":12,"consequences":"## Consequences\n\n- Comments that restated code disappear; the names that replace them are tracked by the compiler and cannot drift out of sync with behavior.\n- Magic numbers gain names through module attributes, making intent explicit at each use site.\n- Public contracts live in `@doc`/`@moduledoc`, where the toolchain can surface and test them, instead of in comments no tool can reach.\n- The comments that remain carry genuinely non-obvious context (a workaround rationale, a link to an external constraint), so the presence of a comment now signals something worth reading.","rule_title":null,"routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","record_id":"elixir-code-anti-patterns:adr-001","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"814a8b39a18471490adf26958ba73bedcff0edc2dee13335083f82ee0d7e271d","hydrate_id":"elixir-code-anti-patterns:adr-001","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-001\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\nRouting title: Comments overuse\n\nRouting description: When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.\n\nSource description: Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.\n\n## Context\n\nComments that restate self-explanatory code make code less readable, not more. They add visual lines a reader must scan past, and they carry a maintenance liability that the code itself does not.\n\nThe mechanism is the toolchain. The Elixir tokenizer discards every `#` comment before the compiler ever sees it, so nothing in the build checks a comment against the code beneath it. When the code changes and the comment does not, the comment silently lies, and a reader who trusts it is misled. Identifiers behave the opposite way: a function name, variable name, or module attribute is part of the program the compiler tracks, so it cannot drift out of sync with the behavior it names.\n\nElixir also draws a hard line between comments and documentation. `@moduledoc` and `@doc` compile into the BEAM Docs chunk and are reachable and testable at runtime; a `#` comment is none of that. So a comment that should be a contract belongs in `@doc`/`@moduledoc`, and a comment that merely narrates obvious code belongs nowhere.\n\n## Decision\n\n## Consequences\n\n- Comments that restated code disappear; the names that replace them are tracked by the compiler and cannot drift out of sync with behavior.\n- Magic numbers gain names through module attributes, making intent explicit at each use site.\n- Public contracts live in `@doc`/`@moduledoc`, where the toolchain can surface and test them, instead of in comments no tool can reach.\n- The comments that remain carry genuinely non-obvious context (a workaround rationale, a link to an external constraint), so the presence of a comment now signals something worth reading.\n\nRule headings:\n### Rule 1: Replace explanatory comments with expressive names\n### Rule 2: Express contracts as documentation, not comments","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"rule_number":null,"adr_title":"Comments overuse","languages":[],"source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","ordinal":1,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-001: Comments overuse"],"context":"## Context\n\nComments that restate self-explanatory code make code less readable, not more. They add visual lines a reader must scan past, and they carry a maintenance liability that the code itself does not.\n\nThe mechanism is the toolchain. The Elixir tokenizer discards every `#` comment before the compiler ever sees it, so nothing in the build checks a comment against the code beneath it. When the code changes and the comment does not, the comment silently lies, and a reader who trusts it is misled. Identifiers behave the opposite way: a function name, variable name, or module attribute is part of the program the compiler tracks, so it cannot drift out of sync with the behavior it names.\n\nElixir also draws a hard line between comments and documentation. `@moduledoc` and `@doc` compile into the BEAM Docs chunk and are reachable and testable at runtime; a `#` comment is none of that. So a comment that should be a contract belongs in `@doc`/`@moduledoc`, and a comment that merely narrates obvious code belongs nowhere.","source_end_line":100,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":1,"routing_title":"Comments overuse","display_text":"## Context\n\nComments that restate self-explanatory code make code less readable, not more. They add visual lines a reader must scan past, and they carry a maintenance liability that the code itself does not.\n\nThe mechanism is the toolchain. The Elixir tokenizer discards every `#` comment before the compiler ever sees it, so nothing in the build checks a comment against the code beneath it. When the code changes and the comment does not, the comment silently lies, and a reader who trusts it is misled. Identifiers behave the opposite way: a function name, variable name, or module attribute is part of the program the compiler tracks, so it cannot drift out of sync with the behavior it names.\n\nElixir also draws a hard line between comments and documentation. `@moduledoc` and `@doc` compile into the BEAM Docs chunk and are reachable and testable at runtime; a `#` comment is none of that. So a comment that should be a contract belongs in `@doc`/`@moduledoc`, and a comment that merely narrates obvious code belongs nowhere.\n\n## Decision\n\n## Consequences\n\n- Comments that restated code disappear; the names that replace them are tracked by the compiler and cannot drift out of sync with behavior.\n- Magic numbers gain names through module attributes, making intent explicit at each use site.\n- Public contracts live in `@doc`/`@moduledoc`, where the toolchain can surface and test them, instead of in comments no tool can reach.\n- The comments that remain carry genuinely non-obvious context (a workaround rationale, a link to an external constraint), so the presence of a comment now signals something worth reading."}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"### Rule 1: Replace explanatory comments with expressive names\n\n**Correct:**\n\n```elixir\n@five_min_in_seconds 60 * 5\n\ndefp unix_five_min_from_now do\n  now = DateTime.utc_now()\n  unix_now = DateTime.to_unix(now, :second)\n  unix_now + @five_min_in_seconds\nend\n```\n\n**Wrong:**\n\n```elixir\n# Returns the Unix timestamp of 5 minutes from the current time\ndefp unix_five_min_from_now do\n  # Get the current time\n  now = DateTime.utc_now()\n\n  # Convert it to a Unix timestamp\n  unix_now = DateTime.to_unix(now, :second)\n\n  # Add five minutes in seconds\n  unix_now + (60 * 5)\nend\n```\n\n**Why:** Every comment in the wrong version restates information the function name, the variable names, and the function calls already carry. Because the tokenizer strips those comments, nothing keeps them honest as the code evolves, so they rot into false statements. The correct version moves the same intent into names the compiler tracks. Promoting `60 * 5` to `@five_min_in_seconds` supplies the one fact the raw expression did not: what that number means at the point it is used.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Replace explanatory comments with expressive names"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":2,"parent_id":"elixir-code-anti-patterns:adr-001","polarity":null,"record_id":"elixir-code-anti-patterns:adr-001:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"68842202a8c3afba9db357aee26082c886e11d417dd6477e9ce8794afb3fbe1d","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\n### Rule 1: Replace explanatory comments with expressive names\n\nWhy: Every comment in the wrong version restates information the function name, the variable names, and the function calls already carry. Because the tokenizer strips those comments, nothing keeps them honest as the code evolves, so they rot into false statements. The correct version moves the same intent into names the compiler tracks. Promoting `60 * 5` to `@five_min_in_seconds` supplies the one fact the raw expression did not: what that number means at the point it is used.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":1,"rule_title":"Replace explanatory comments with expressive names","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":53,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"0a991171b7c8230daf3dbfbfda921613578c2f32ac80f9a4f7ceb0c62aa48e89","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\n@five_min_in_seconds 60 * 5\n\ndefp unix_five_min_from_now do\n  now = DateTime.utc_now()\n  unix_now = DateTime.to_unix(now, :second)\n  unix_now + @five_min_in_seconds\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Replace explanatory comments with expressive names","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":3,"parent_id":"elixir-code-anti-patterns:adr-001:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-001:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"a58a3b97a26f4aa28009554d211e97ddb792afe8404f7200889a267acdb95c3a","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\nRouting title: Comments overuse\n\nRule 1: Replace explanatory comments with expressive names\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\n@five_min_in_seconds 60 * 5\n\ndefp unix_five_min_from_now do\n  now = DateTime.utc_now()\n  unix_now = DateTime.to_unix(now, :second)\n  unix_now + @five_min_in_seconds\nend\n```\n\nWhy: Every comment in the wrong version restates information the function name, the variable names, and the function calls already carry. Because the tokenizer strips those comments, nothing keeps them honest as the code evolves, so they rot into false statements. The correct version moves the same intent into names the compiler tracks. Promoting `60 * 5` to `@five_min_in_seconds` supplies the one fact the raw expression did not: what that number means at the point it is used.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":1,"rule_title":"Replace explanatory comments with expressive names","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":35,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"d8fc54dc4361ba055c5d85e033973319133074bf3f4339de649c0075322277a8","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# Returns the Unix timestamp of 5 minutes from the current time\ndefp unix_five_min_from_now do\n  # Get the current time\n  now = DateTime.utc_now()\n\n  # Convert it to a Unix timestamp\n  unix_now = DateTime.to_unix(now, :second)\n\n  # Add five minutes in seconds\n  unix_now + (60 * 5)\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Replace explanatory comments with expressive names","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-01","languages":["elixir"],"ordinal":4,"parent_id":"elixir-code-anti-patterns:adr-001:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-001:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"28bb57ab9e7834e52c52fada1f9ec405de055e4539b8bc08a82d3cb08fe29f09","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\nRouting title: Comments overuse\n\nRule 1: Replace explanatory comments with expressive names\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# Returns the Unix timestamp of 5 minutes from the current time\ndefp unix_five_min_from_now do\n  # Get the current time\n  now = DateTime.utc_now()\n\n  # Convert it to a Unix timestamp\n  unix_now = DateTime.to_unix(now, :second)\n\n  # Add five minutes in seconds\n  unix_now + (60 * 5)\nend\n```\n\nWhy: Every comment in the wrong version restates information the function name, the variable names, and the function calls already carry. Because the tokenizer strips those comments, nothing keeps them honest as the code evolves, so they rot into false statements. The correct version moves the same intent into names the compiler tracks. Promoting `60 * 5` to `@five_min_in_seconds` supplies the one fact the raw expression did not: what that number means at the point it is used.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":1,"rule_title":"Replace explanatory comments with expressive names","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":51,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"59bca34f8f1d5d62f0f1c66425f7fdc2299c9112a7c892275631bd8266dc1e31","source_start_line":36,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"### Rule 2: Express contracts as documentation, not comments\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Accounts do\n  @moduledoc \"Public API for creating and authenticating users.\"\n\n  @doc \"\"\"\n  Authenticates a user from an `email` and `password`.\n\n  Returns `{:ok, user}` on success and `{:error, :unauthorized}` otherwise.\n  \"\"\"\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\n# Public API for creating and authenticating users.\ndefmodule MyApp.Accounts do\n  # Authenticates a user from an email and password.\n  # Returns {:ok, user} on success and {:error, :unauthorized} otherwise.\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```\n\n**Why:** The two versions read the same in source, but they compile to different artifacts. The `@moduledoc` and `@doc` text lands in the BEAM Docs chunk, so it is reachable through `h MyApp.Accounts`, through `Code.fetch_docs/1`, and through ExDoc, and `iex>` samples inside it run as doctests. The comment version is discarded by the tokenizer and is invisible to every one of those tools, untestable, and unreachable at runtime. A public contract written as a comment is a contract no tooling can surface or verify.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Express contracts as documentation, not comments"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-02","languages":["elixir"],"ordinal":5,"parent_id":"elixir-code-anti-patterns:adr-001","polarity":null,"record_id":"elixir-code-anti-patterns:adr-001:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"e49577b47593336979decd0643dafe511e4a41e204aebd2b170ca51fa2437b8f","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\n### Rule 2: Express contracts as documentation, not comments\n\nWhy: The two versions read the same in source, but they compile to different artifacts. The `@moduledoc` and `@doc` text lands in the BEAM Docs chunk, so it is reachable through `h MyApp.Accounts`, through `Code.fetch_docs/1`, and through ExDoc, and `iex>` samples inside it run as doctests. The comment version is discarded by the tokenizer and is invisible to every one of those tools, untestable, and unreachable at runtime. A public contract written as a comment is a contract no tooling can surface or verify.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":2,"rule_title":"Express contracts as documentation, not comments","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":93,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"ba07dcfe90e7d566d5d891e2d5eaeb0915f79ab258533214885c80b8d3185bb2","source_start_line":54,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Accounts do\n  @moduledoc \"Public API for creating and authenticating users.\"\n\n  @doc \"\"\"\n  Authenticates a user from an `email` and `password`.\n\n  Returns `{:ok, user}` on success and `{:error, :unauthorized}` otherwise.\n  \"\"\"\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Express contracts as documentation, not comments","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-02","languages":["elixir"],"ordinal":6,"parent_id":"elixir-code-anti-patterns:adr-001:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-001:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"bcbf09bab7f994bfcad899ace883f6b8aafcf6d125451182250c7315bcb5e823","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\nRouting title: Comments overuse\n\nRule 2: Express contracts as documentation, not comments\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Accounts do\n  @moduledoc \"Public API for creating and authenticating users.\"\n\n  @doc \"\"\"\n  Authenticates a user from an `email` and `password`.\n\n  Returns `{:ok, user}` on success and `{:error, :unauthorized}` otherwise.\n  \"\"\"\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```\n\nWhy: The two versions read the same in source, but they compile to different artifacts. The `@moduledoc` and `@doc` text lands in the BEAM Docs chunk, so it is reachable through `h MyApp.Accounts`, through `Code.fetch_docs/1`, and through ExDoc, and `iex>` samples inside it run as doctests. The comment version is discarded by the tokenizer and is invisible to every one of those tools, untestable, and unreachable at runtime. A public contract written as a comment is a contract no tooling can surface or verify.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":2,"rule_title":"Express contracts as documentation, not comments","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":75,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"9eeb101a0ba3a1009c7da7a88054057073da52a30cd07d19d5768ce07fda5eb2","source_start_line":56,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"adr_id":1,"adr_title":"Comments overuse","applies_to":{"content_match":["# Get the","# Returns the","# Convert","# Add ","# Set the","# Loop over"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# Public API for creating and authenticating users.\ndefmodule MyApp.Accounts do\n  # Authenticates a user from an email and password.\n  # Returns {:ok, user} on success and {:error, :unauthorized} otherwise.\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Express contracts as documentation, not comments","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-001:rule-02","languages":["elixir"],"ordinal":7,"parent_id":"elixir-code-anti-patterns:adr-001:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-001:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"ff4e9189513e70ba00e01093b62d6372b8fafa93a7e85fb68f52b5f3ebcff107","retrieval_text":"Record: elixir-code-anti-patterns:adr-001:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Comments overuse\n\nRouting title: Comments overuse\n\nRule 2: Express contracts as documentation, not comments\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# Public API for creating and authenticating users.\ndefmodule MyApp.Accounts do\n  # Authenticates a user from an email and password.\n  # Returns {:ok, user} on success and {:error, :unauthorized} otherwise.\n  def authenticate(email, password) do\n    with {:ok, user} <- fetch_by_email(email),\n         :ok <- verify_password(user, password) do\n      {:ok, user}\n    end\n  end\nend\n```\n\nWhy: The two versions read the same in source, but they compile to different artifacts. The `@moduledoc` and `@doc` text lands in the BEAM Docs chunk, so it is reachable through `h MyApp.Accounts`, through `Code.fetch_docs/1`, and through ExDoc, and `iex>` samples inside it run as doctests. The comment version is discarded by the tokenizer and is invisible to every one of those tools, untestable, and unreachable at runtime. A public contract written as a comment is a contract no tooling can surface or verify.","routing_description":"When a comment restates what a function, variable, or expression already says, delete it and sharpen the names instead, reserving `@doc`/`@moduledoc` for actual documentation.","routing_title":"Comments overuse","rule_number":2,"rule_title":"Express contracts as documentation, not comments","schema_version":"retrieval-v1","source_description":"Comments that restate self-explanatory code rot because the Elixir tokenizer discards them and nothing checks them against the code. Replace them with expressive names and module attributes, and move real contracts into `@doc`/`@moduledoc`, which compile into the BEAM Docs chunk and are testable as doctests.","source_end_line":91,"source_path":"adrs/elixir-code-anti-patterns/adr-001-comments-overuse.md","source_sha256":"c5fe6765461707e1ef2478ed3b34684bacfaecbccd5569b5a77f97ea7b946fa6","source_start_line":76,"status":"accepted","tags":["elixir","anti-pattern","comments","documentation","readability","naming"],"updated":null}
{"references":[],"source_sha256":"d203c667cf0298bf6952035dcdcae62431530ae709072d1e2c496bac32e0bcc0","source_start_line":12,"consequences":"## Consequences\n\n- `with` reads as the success path only; each step's failure is named at the function that produces it.\n- Adding a step cannot silently re-route an existing step's error, because failures no longer share one catch-all clause.\n- The normalizing functions are pure, independently testable, and reusable across other call sites.\n- There is no `else` block to keep synchronized as the chain grows or reorders.","rule_title":null,"routing_description":"When a `with` expression grows an `else` block that handles errors from multiple `<-` clauses, normalize each step's failure in a dedicated private function and drop the `else`.","record_id":"elixir-code-anti-patterns:adr-002","applies_to":{"content_match":["with ","else","<-","{:error, _}"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"a21df7946625e8a7e37149520882f0fe18674c885197b2545f39bcf628dcb642","hydrate_id":"elixir-code-anti-patterns:adr-002","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-002\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex `else` clauses in `with`\n\nRouting title: Complex `else` clauses in `with`\n\nRouting description: When a `with` expression grows an `else` block that handles errors from multiple `<-` clauses, normalize each step's failure in a dedicated private function and drop the `else`.\n\nSource description: A `with` expression's single `else` block flattens the failures of every `<-` clause into one place, stripping the provenance of which clause failed and letting structurally similar errors collapse into the wrong catch-all. Normalize each step's return in a private function so `with` expresses only the success path.\n\n## Context\n\nA `with` expression chains fallible steps with `<-`. When a step's value does not match the pattern on the left of its `<-`, the chain aborts and hands that non-matching value to the expression's single `else` block.\n\nThat single `else` is the problem. It receives the failure value from whichever clause aborted, stripped of any indication of which clause that was. Every `<-` in the chain funnels its failures into the same block, so the error-handling logic for unrelated steps is flattened together. Reading the `else`, you cannot tell which pattern on the left produced which branch on the right.\n\nThis degrades as the chain grows. Two steps that both fail with a structurally similar value (for example both returning `{:error, _}`) collapse into one catch-all clause in the `else`. A clause written to handle one step's failure silently captures another step's failure and maps it to the wrong reason. The more `<-` clauses a `with` has, the more likely unrelated failures overlap.\n\nThe fix is to give each fallible step a return shape that is unique and self-describing at its source, so `with` can express the success path alone and drop the `else` entirely.\n\n## Decision\n\n## Consequences\n\n- `with` reads as the success path only; each step's failure is named at the function that produces it.\n- Adding a step cannot silently re-route an existing step's error, because failures no longer share one catch-all clause.\n- The normalizing functions are pure, independently testable, and reusable across other call sites.\n- There is no `else` block to keep synchronized as the chain grows or reorders.\n\nRule headings:\n### Rule 1: Normalize each step's failure in a private function, not in a shared `else`","tags":["elixir","anti-pattern","with","error-handling","control-flow","readability"],"rule_number":null,"adr_title":"Complex `else` clauses in `with`","languages":[],"source_description":"A `with` expression's single `else` block flattens the failures of every `<-` clause into one place, stripping the provenance of which clause failed and letting structurally similar errors collapse into the wrong catch-all. Normalize each step's return in a private function so `with` expresses only the success path.","ordinal":8,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-002-complex-else-clauses-in-with.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-002: Complex `else` clauses in `with`"],"context":"## Context\n\nA `with` expression chains fallible steps with `<-`. When a step's value does not match the pattern on the left of its `<-`, the chain aborts and hands that non-matching value to the expression's single `else` block.\n\nThat single `else` is the problem. It receives the failure value from whichever clause aborted, stripped of any indication of which clause that was. Every `<-` in the chain funnels its failures into the same block, so the error-handling logic for unrelated steps is flattened together. Reading the `else`, you cannot tell which pattern on the left produced which branch on the right.\n\nThis degrades as the chain grows. Two steps that both fail with a structurally similar value (for example both returning `{:error, _}`) collapse into one catch-all clause in the `else`. A clause written to handle one step's failure silently captures another step's failure and maps it to the wrong reason. The more `<-` clauses a `with` has, the more likely unrelated failures overlap.\n\nThe fix is to give each fallible step a return shape that is unique and self-describing at its source, so `with` can express the success path alone and drop the `else` entirely.","source_end_line":73,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":2,"routing_title":"Complex `else` clauses in `with`","display_text":"## Context\n\nA `with` expression chains fallible steps with `<-`. When a step's value does not match the pattern on the left of its `<-`, the chain aborts and hands that non-matching value to the expression's single `else` block.\n\nThat single `else` is the problem. It receives the failure value from whichever clause aborted, stripped of any indication of which clause that was. Every `<-` in the chain funnels its failures into the same block, so the error-handling logic for unrelated steps is flattened together. Reading the `else`, you cannot tell which pattern on the left produced which branch on the right.\n\nThis degrades as the chain grows. Two steps that both fail with a structurally similar value (for example both returning `{:error, _}`) collapse into one catch-all clause in the `else`. A clause written to handle one step's failure silently captures another step's failure and maps it to the wrong reason. The more `<-` clauses a `with` has, the more likely unrelated failures overlap.\n\nThe fix is to give each fallible step a return shape that is unique and self-describing at its source, so `with` can express the success path alone and drop the `else` entirely.\n\n## Decision\n\n## Consequences\n\n- `with` reads as the success path only; each step's failure is named at the function that produces it.\n- Adding a step cannot silently re-route an existing step's error, because failures no longer share one catch-all clause.\n- The normalizing functions are pure, independently testable, and reusable across other call sites.\n- There is no `else` block to keep synchronized as the chain grows or reorders."}
{"adr_id":2,"adr_title":"Complex `else` clauses in `with`","applies_to":{"content_match":["with ","else","<-","{:error, _}"]},"date":"2026-06-28","display_text":"### Rule 1: Normalize each step's failure in a private function, not in a shared `else`\n\n**Correct:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- file_read(path),\n       {:ok, decoded} <- base_decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  end\nend\n\ndefp file_read(path) do\n  case File.read(path) do\n    {:ok, contents} -> {:ok, contents}\n    {:error, _} -> {:error, :badfile}\n  end\nend\n\ndefp base_decode64(contents) do\n  case Base.decode64(contents) do\n    {:ok, decoded} -> {:ok, decoded}\n    :error -> {:error, :badencoding}\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- File.read(path),\n       {:ok, decoded} <- Base.decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  else\n    {:error, _} -> {:error, :badfile}\n    :error -> {:error, :badencoding}\n  end\nend\n```\n\n**Why:** In the wrong version the `else` block receives the aborting value from either `<-` clause with no record of which one fired. `File.read/1` failing with `{:error, _}` and `Base.decode64/1` failing with `:error` happen to be distinguishable here only because their shapes differ. Add a third step that also returns `{:error, _}` and it collapses into the existing `{:error, _} -> {:error, :badfile}` clause, mapping an unrelated failure to `:badfile` with no warning. Normalizing inside `file_read/1` and `base_decode64/1` attaches each failure reason at the exact site that produces it, so the shapes never compete for a shared clause. The `with` then has nothing to handle but the success case and the `else` disappears.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Normalize each step's failure in a private function, not in a shared `else`"],"hydrate_id":"elixir-code-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":9,"parent_id":"elixir-code-anti-patterns:adr-002","polarity":null,"record_id":"elixir-code-anti-patterns:adr-002:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"bf504c486a682850c21c456ff8c84b342e99099c218ab90f4943e7750afbf973","retrieval_text":"Record: elixir-code-anti-patterns:adr-002:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex `else` clauses in `with`\n\n### Rule 1: Normalize each step's failure in a private function, not in a shared `else`\n\nWhy: In the wrong version the `else` block receives the aborting value from either `<-` clause with no record of which one fired. `File.read/1` failing with `{:error, _}` and `Base.decode64/1` failing with `:error` happen to be distinguishable here only because their shapes differ. Add a third step that also returns `{:error, _}` and it collapses into the existing `{:error, _} -> {:error, :badfile}` clause, mapping an unrelated failure to `:badfile` with no warning. Normalizing inside `file_read/1` and `base_decode64/1` attaches each failure reason at the exact site that produces it, so the shapes never compete for a shared clause. The `with` then has nothing to handle but the success case and the `else` disappears.","routing_description":"When a `with` expression grows an `else` block that handles errors from multiple `<-` clauses, normalize each step's failure in a dedicated private function and drop the `else`.","routing_title":"Complex `else` clauses in `with`","rule_number":1,"rule_title":"Normalize each step's failure in a private function, not in a shared `else`","schema_version":"retrieval-v1","source_description":"A `with` expression's single `else` block flattens the failures of every `<-` clause into one place, stripping the provenance of which clause failed and letting structurally similar errors collapse into the wrong catch-all. Normalize each step's return in a private function so `with` expresses only the success path.","source_end_line":66,"source_path":"adrs/elixir-code-anti-patterns/adr-002-complex-else-clauses-in-with.md","source_sha256":"da83e6846b8852dd5632c0c6c66cc17638a2ea96bd7c87e5b891ad4d2b4c6478","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","with","error-handling","control-flow","readability"],"updated":null}
{"adr_id":2,"adr_title":"Complex `else` clauses in `with`","applies_to":{"content_match":["with ","else","<-","{:error, _}"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- file_read(path),\n       {:ok, decoded} <- base_decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  end\nend\n\ndefp file_read(path) do\n  case File.read(path) do\n    {:ok, contents} -> {:ok, contents}\n    {:error, _} -> {:error, :badfile}\n  end\nend\n\ndefp base_decode64(contents) do\n  case Base.decode64(contents) do\n    {:ok, decoded} -> {:ok, decoded}\n    :error -> {:error, :badencoding}\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Normalize each step's failure in a private function, not in a shared `else`","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":10,"parent_id":"elixir-code-anti-patterns:adr-002:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-002:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"9b2570d5c3a100a8626e2941714279c19620c7603418d309561cd90d8f6ec9dc","retrieval_text":"Record: elixir-code-anti-patterns:adr-002:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex `else` clauses in `with`\n\nRouting title: Complex `else` clauses in `with`\n\nRule 1: Normalize each step's failure in a private function, not in a shared `else`\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- file_read(path),\n       {:ok, decoded} <- base_decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  end\nend\n\ndefp file_read(path) do\n  case File.read(path) do\n    {:ok, contents} -> {:ok, contents}\n    {:error, _} -> {:error, :badfile}\n  end\nend\n\ndefp base_decode64(contents) do\n  case Base.decode64(contents) do\n    {:ok, decoded} -> {:ok, decoded}\n    :error -> {:error, :badencoding}\n  end\nend\n```\n\nWhy: In the wrong version the `else` block receives the aborting value from either `<-` clause with no record of which one fired. `File.read/1` failing with `{:error, _}` and `Base.decode64/1` failing with `:error` happen to be distinguishable here only because their shapes differ. Add a third step that also returns `{:error, _}` and it collapses into the existing `{:error, _} -> {:error, :badfile}` clause, mapping an unrelated failure to `:badfile` with no warning. Normalizing inside `file_read/1` and `base_decode64/1` attaches each failure reason at the exact site that produces it, so the shapes never compete for a shared clause. The `with` then has nothing to handle but the success case and the `else` disappears.","routing_description":"When a `with` expression grows an `else` block that handles errors from multiple `<-` clauses, normalize each step's failure in a dedicated private function and drop the `else`.","routing_title":"Complex `else` clauses in `with`","rule_number":1,"rule_title":"Normalize each step's failure in a private function, not in a shared `else`","schema_version":"retrieval-v1","source_description":"A `with` expression's single `else` block flattens the failures of every `<-` clause into one place, stripping the provenance of which clause failed and letting structurally similar errors collapse into the wrong catch-all. Normalize each step's return in a private function so `with` expresses only the success path.","source_end_line":50,"source_path":"adrs/elixir-code-anti-patterns/adr-002-complex-else-clauses-in-with.md","source_sha256":"bb4c5fcd381e04ad1f47ce4b9bbbdf46a48965ec69c710e2841d22eb316e3ab3","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","with","error-handling","control-flow","readability"],"updated":null}
{"adr_id":2,"adr_title":"Complex `else` clauses in `with`","applies_to":{"content_match":["with ","else","<-","{:error, _}"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- File.read(path),\n       {:ok, decoded} <- Base.decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  else\n    {:error, _} -> {:error, :badfile}\n    :error -> {:error, :badencoding}\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Normalize each step's failure in a private function, not in a shared `else`","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-002:rule-01","languages":["elixir"],"ordinal":11,"parent_id":"elixir-code-anti-patterns:adr-002:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-002:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"23b688fd01cb63c9184ab46a33d8a4a1b917fee51de9ccf26a12a0c542d8ae7d","retrieval_text":"Record: elixir-code-anti-patterns:adr-002:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex `else` clauses in `with`\n\nRouting title: Complex `else` clauses in `with`\n\nRule 1: Normalize each step's failure in a private function, not in a shared `else`\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndef open_decoded_file(path) do\n  with {:ok, encoded} <- File.read(path),\n       {:ok, decoded} <- Base.decode64(encoded) do\n    {:ok, String.trim(decoded)}\n  else\n    {:error, _} -> {:error, :badfile}\n    :error -> {:error, :badencoding}\n  end\nend\n```\n\nWhy: In the wrong version the `else` block receives the aborting value from either `<-` clause with no record of which one fired. `File.read/1` failing with `{:error, _}` and `Base.decode64/1` failing with `:error` happen to be distinguishable here only because their shapes differ. Add a third step that also returns `{:error, _}` and it collapses into the existing `{:error, _} -> {:error, :badfile}` clause, mapping an unrelated failure to `:badfile` with no warning. Normalizing inside `file_read/1` and `base_decode64/1` attaches each failure reason at the exact site that produces it, so the shapes never compete for a shared clause. The `with` then has nothing to handle but the success case and the `else` disappears.","routing_description":"When a `with` expression grows an `else` block that handles errors from multiple `<-` clauses, normalize each step's failure in a dedicated private function and drop the `else`.","routing_title":"Complex `else` clauses in `with`","rule_number":1,"rule_title":"Normalize each step's failure in a private function, not in a shared `else`","schema_version":"retrieval-v1","source_description":"A `with` expression's single `else` block flattens the failures of every `<-` clause into one place, stripping the provenance of which clause failed and letting structurally similar errors collapse into the wrong catch-all. Normalize each step's return in a private function so `with` expresses only the success path.","source_end_line":64,"source_path":"adrs/elixir-code-anti-patterns/adr-002-complex-else-clauses-in-with.md","source_sha256":"35f8b0d44a0e5dc7299cd6e081ee041c9666e2f9aa3d8e5b8c158ad6fabef0d1","source_start_line":51,"status":"accepted","tags":["elixir","anti-pattern","with","error-handling","control-flow","readability"],"updated":null}
{"references":[],"source_sha256":"e9167660bd8bb8466ea884806c1fea27b8f8442e1b002147f1ad98ddb721ab01","source_start_line":12,"consequences":"## Consequences\n\n- Each head reads as the dispatch contract: every binding in it either pattern-matches or feeds a guard.\n- Body-only fields are bound where they are used, not announced in the signature.\n- Adding a clause or an extracted field no longer forces re-reading every head to recover which variables drive selection.\n- Multi-argument, multi-clause functions stay legible because the heads carry only selection logic.","rule_title":null,"routing_description":"When a multi-clause function head extracts struct or map fields that are only used in the body, bind just the pattern/guard variables (plus `= var`) in the head and extract the remaining fields inside the body.","record_id":"elixir-code-anti-patterns:adr-003","applies_to":{"content_match":["when",") when","{name: name, age: age}","} = "]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"ef53309def90a95ffe9f7607d6ea9f0b377f6b813e755fc17a02e5d0be3baf99","hydrate_id":"elixir-code-anti-patterns:adr-003","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-003\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex extractions in clauses\n\nRouting title: Complex extractions in clauses\n\nRouting description: When a multi-clause function head extracts struct or map fields that are only used in the body, bind just the pattern/guard variables (plus `= var`) in the head and extract the remaining fields inside the body.\n\nSource description: A multi-clause function head should bind only the variables its patterns and guards use to select a clause. Extracting body-only struct or map fields in the head hides which bindings actually drive dispatch, because nothing distinguishes a selection variable from local plumbing in the signature.\n\n## Context\n\nA multi-clause function dispatches on its heads. Pattern matching and guards in a head decide which clause runs; every other binding in that head is plain local plumbing that happens to be written in the signature. Elixir lets you extract any number of fields from a struct or map argument directly in the head, and use those bindings either for dispatch or only inside the body.\n\nThat flexibility hides a readability cost. When extractions are spread across several clauses and several arguments, a reader can no longer tell at a glance which extracted variables participate in clause selection and which are merely consumed by the body. The head stops being a precise statement of the dispatch contract and becomes a mix of contract and data access. With many clauses, arguments, and extracted fields, recovering \"what actually decides this clause\" means diffing each head against its guard by eye.\n\nThe fix is to keep the head minimal: extract only the variables a pattern or guard needs, bind the whole argument with `= var`, and pull body-only fields out inside the body where they are used.\n\n## Decision\n\n## Consequences\n\n- Each head reads as the dispatch contract: every binding in it either pattern-matches or feeds a guard.\n- Body-only fields are bound where they are used, not announced in the signature.\n- Adding a clause or an extracted field no longer forces re-reading every head to recover which variables drive selection.\n- Multi-argument, multi-clause functions stay legible because the heads carry only selection logic.\n\nRule headings:\n### Rule 1: Extract only clause-deciding variables in the head","tags":["elixir","anti-pattern","pattern-matching","multi-clause","readability","functions"],"rule_number":null,"adr_title":"Complex extractions in clauses","languages":[],"source_description":"A multi-clause function head should bind only the variables its patterns and guards use to select a clause. Extracting body-only struct or map fields in the head hides which bindings actually drive dispatch, because nothing distinguishes a selection variable from local plumbing in the signature.","ordinal":12,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-003-complex-extractions-in-clauses.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-003: Complex extractions in clauses"],"context":"## Context\n\nA multi-clause function dispatches on its heads. Pattern matching and guards in a head decide which clause runs; every other binding in that head is plain local plumbing that happens to be written in the signature. Elixir lets you extract any number of fields from a struct or map argument directly in the head, and use those bindings either for dispatch or only inside the body.\n\nThat flexibility hides a readability cost. When extractions are spread across several clauses and several arguments, a reader can no longer tell at a glance which extracted variables participate in clause selection and which are merely consumed by the body. The head stops being a precise statement of the dispatch contract and becomes a mix of contract and data access. With many clauses, arguments, and extracted fields, recovering \"what actually decides this clause\" means diffing each head against its guard by eye.\n\nThe fix is to keep the head minimal: extract only the variables a pattern or guard needs, bind the whole argument with `= var`, and pull body-only fields out inside the body where they are used.","source_end_line":58,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":3,"routing_title":"Complex extractions in clauses","display_text":"## Context\n\nA multi-clause function dispatches on its heads. Pattern matching and guards in a head decide which clause runs; every other binding in that head is plain local plumbing that happens to be written in the signature. Elixir lets you extract any number of fields from a struct or map argument directly in the head, and use those bindings either for dispatch or only inside the body.\n\nThat flexibility hides a readability cost. When extractions are spread across several clauses and several arguments, a reader can no longer tell at a glance which extracted variables participate in clause selection and which are merely consumed by the body. The head stops being a precise statement of the dispatch contract and becomes a mix of contract and data access. With many clauses, arguments, and extracted fields, recovering \"what actually decides this clause\" means diffing each head against its guard by eye.\n\nThe fix is to keep the head minimal: extract only the variables a pattern or guard needs, bind the whole argument with `= var`, and pull body-only fields out inside the body where they are used.\n\n## Decision\n\n## Consequences\n\n- Each head reads as the dispatch contract: every binding in it either pattern-matches or feeds a guard.\n- Body-only fields are bound where they are used, not announced in the signature.\n- Adding a clause or an extracted field no longer forces re-reading every head to recover which variables drive selection.\n- Multi-argument, multi-clause functions stay legible because the heads carry only selection logic."}
{"adr_id":3,"adr_title":"Complex extractions in clauses","applies_to":{"content_match":["when",") when","{name: name, age: age}","} = "]},"date":"2026-06-28","display_text":"### Rule 1: Extract only clause-deciding variables in the head\n\n**Correct:**\n\n```elixir\ndef drive(%User{age: age} = user) when age >= 18 do\n  %User{name: name} = user\n  \"#{name} can drive\"\nend\n\ndef drive(%User{age: age} = user) when age < 18 do\n  %User{name: name} = user\n  \"#{name} cannot drive\"\nend\n```\n\n**Wrong:**\n\n```elixir\ndef drive(%User{name: name, age: age}) when age >= 18 do\n  \"#{name} can drive\"\nend\n\ndef drive(%User{name: name, age: age}) when age < 18 do\n  \"#{name} cannot drive\"\nend\n```\n\n**Why:** Only `age` decides which clause of `drive/1` runs; `name` is never matched on and never reaches a guard. In the wrong version the head binds both, so a reader scanning the signatures cannot separate the dispatch variable (`age`) from the body-only field (`name`) without reading the guard and the body to confirm. That conflation scales with the function: more clauses, more arguments, and more extracted fields turn every head into a puzzle about what is load-bearing for dispatch. The correct version makes the head the contract: `%User{age: age} = user` says \"this clause matches on age,\" and `%User{name: name} = user` in the body binds the rest next to the code that consumes it.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Extract only clause-deciding variables in the head"],"hydrate_id":"elixir-code-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":13,"parent_id":"elixir-code-anti-patterns:adr-003","polarity":null,"record_id":"elixir-code-anti-patterns:adr-003:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"bec42c744b335c87a2035a77eecfe22a4b096a3cabd2c6bc412196d8baf44765","retrieval_text":"Record: elixir-code-anti-patterns:adr-003:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex extractions in clauses\n\n### Rule 1: Extract only clause-deciding variables in the head\n\nWhy: Only `age` decides which clause of `drive/1` runs; `name` is never matched on and never reaches a guard. In the wrong version the head binds both, so a reader scanning the signatures cannot separate the dispatch variable (`age`) from the body-only field (`name`) without reading the guard and the body to confirm. That conflation scales with the function: more clauses, more arguments, and more extracted fields turn every head into a puzzle about what is load-bearing for dispatch. The correct version makes the head the contract: `%User{age: age} = user` says \"this clause matches on age,\" and `%User{name: name} = user` in the body binds the rest next to the code that consumes it.","routing_description":"When a multi-clause function head extracts struct or map fields that are only used in the body, bind just the pattern/guard variables (plus `= var`) in the head and extract the remaining fields inside the body.","routing_title":"Complex extractions in clauses","rule_number":1,"rule_title":"Extract only clause-deciding variables in the head","schema_version":"retrieval-v1","source_description":"A multi-clause function head should bind only the variables its patterns and guards use to select a clause. Extracting body-only struct or map fields in the head hides which bindings actually drive dispatch, because nothing distinguishes a selection variable from local plumbing in the signature.","source_end_line":51,"source_path":"adrs/elixir-code-anti-patterns/adr-003-complex-extractions-in-clauses.md","source_sha256":"a866f7dcd2f5c1ae2b1629c0a903c72b8720ed50aa8ea7d32ce5802d1c23a3ab","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","multi-clause","readability","functions"],"updated":null}
{"adr_id":3,"adr_title":"Complex extractions in clauses","applies_to":{"content_match":["when",") when","{name: name, age: age}","} = "]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndef drive(%User{age: age} = user) when age >= 18 do\n  %User{name: name} = user\n  \"#{name} can drive\"\nend\n\ndef drive(%User{age: age} = user) when age < 18 do\n  %User{name: name} = user\n  \"#{name} cannot drive\"\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Extract only clause-deciding variables in the head","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":14,"parent_id":"elixir-code-anti-patterns:adr-003:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-003:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"34ca6b6ee360843bb586fe1a1a2e45702b499914b7ee9f8af93df3fa202df093","retrieval_text":"Record: elixir-code-anti-patterns:adr-003:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex extractions in clauses\n\nRouting title: Complex extractions in clauses\n\nRule 1: Extract only clause-deciding variables in the head\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndef drive(%User{age: age} = user) when age >= 18 do\n  %User{name: name} = user\n  \"#{name} can drive\"\nend\n\ndef drive(%User{age: age} = user) when age < 18 do\n  %User{name: name} = user\n  \"#{name} cannot drive\"\nend\n```\n\nWhy: Only `age` decides which clause of `drive/1` runs; `name` is never matched on and never reaches a guard. In the wrong version the head binds both, so a reader scanning the signatures cannot separate the dispatch variable (`age`) from the body-only field (`name`) without reading the guard and the body to confirm. That conflation scales with the function: more clauses, more arguments, and more extracted fields turn every head into a puzzle about what is load-bearing for dispatch. The correct version makes the head the contract: `%User{age: age} = user` says \"this clause matches on age,\" and `%User{name: name} = user` in the body binds the rest next to the code that consumes it.","routing_description":"When a multi-clause function head extracts struct or map fields that are only used in the body, bind just the pattern/guard variables (plus `= var`) in the head and extract the remaining fields inside the body.","routing_title":"Complex extractions in clauses","rule_number":1,"rule_title":"Extract only clause-deciding variables in the head","schema_version":"retrieval-v1","source_description":"A multi-clause function head should bind only the variables its patterns and guards use to select a clause. Extracting body-only struct or map fields in the head hides which bindings actually drive dispatch, because nothing distinguishes a selection variable from local plumbing in the signature.","source_end_line":37,"source_path":"adrs/elixir-code-anti-patterns/adr-003-complex-extractions-in-clauses.md","source_sha256":"f62ce959dd68e5f7bfb75b306e141342d46d0dc8b8fabdf07af67dc6c2f4235c","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","multi-clause","readability","functions"],"updated":null}
{"adr_id":3,"adr_title":"Complex extractions in clauses","applies_to":{"content_match":["when",") when","{name: name, age: age}","} = "]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndef drive(%User{name: name, age: age}) when age >= 18 do\n  \"#{name} can drive\"\nend\n\ndef drive(%User{name: name, age: age}) when age < 18 do\n  \"#{name} cannot drive\"\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Extract only clause-deciding variables in the head","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-003:rule-01","languages":["elixir"],"ordinal":15,"parent_id":"elixir-code-anti-patterns:adr-003:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-003:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"b3ac4557733a54d35eeddef47ad097b80ba57cb629be4bb87ba3ed6a8397bb10","retrieval_text":"Record: elixir-code-anti-patterns:adr-003:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Complex extractions in clauses\n\nRouting title: Complex extractions in clauses\n\nRule 1: Extract only clause-deciding variables in the head\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndef drive(%User{name: name, age: age}) when age >= 18 do\n  \"#{name} can drive\"\nend\n\ndef drive(%User{name: name, age: age}) when age < 18 do\n  \"#{name} cannot drive\"\nend\n```\n\nWhy: Only `age` decides which clause of `drive/1` runs; `name` is never matched on and never reaches a guard. In the wrong version the head binds both, so a reader scanning the signatures cannot separate the dispatch variable (`age`) from the body-only field (`name`) without reading the guard and the body to confirm. That conflation scales with the function: more clauses, more arguments, and more extracted fields turn every head into a puzzle about what is load-bearing for dispatch. The correct version makes the head the contract: `%User{age: age} = user` says \"this clause matches on age,\" and `%User{name: name} = user` in the body binds the rest next to the code that consumes it.","routing_description":"When a multi-clause function head extracts struct or map fields that are only used in the body, bind just the pattern/guard variables (plus `= var`) in the head and extract the remaining fields inside the body.","routing_title":"Complex extractions in clauses","rule_number":1,"rule_title":"Extract only clause-deciding variables in the head","schema_version":"retrieval-v1","source_description":"A multi-clause function head should bind only the variables its patterns and guards use to select a clause. Extracting body-only struct or map fields in the head hides which bindings actually drive dispatch, because nothing distinguishes a selection variable from local plumbing in the signature.","source_end_line":49,"source_path":"adrs/elixir-code-anti-patterns/adr-003-complex-extractions-in-clauses.md","source_sha256":"44af6d2b295de6ff545a0f1182e25d504f4233cf100d71e73a2076e8999cb4f7","source_start_line":38,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","multi-clause","readability","functions"],"updated":null}
{"references":[],"source_sha256":"3370fe5ea5b0842fa822343bf961d20424a9d482b84c2520e5e0ffe682ecb4c9","source_start_line":12,"consequences":"## Consequences\n\n- The number of atoms a node holds is bounded by source code, not by request volume or payload content.\n- Input naming an unknown atom fails loudly (a failed clause or `ArgumentError`) instead of silently enlarging the never-collected atom table.\n- The 1,048,576-entry atom-table ceiling stops being reachable from the network, closing a denial-of-service vector.\n- Every accepted string resolves to an atom the compiler already interned.","rule_title":null,"routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","record_id":"elixir-code-anti-patterns:adr-004","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"dc164383bc0d86595eb9c87be9df92fad45f2f583e1276280de8be65cebea367","hydrate_id":"elixir-code-anti-patterns:adr-004","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-004\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\nRouting title: Dynamic atom creation\n\nRouting description: When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.\n\nSource description: Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.\n\n## Context\n\nAn atom is a basic Elixir type whose value is its own name. Atoms identify resources and express the state or result of an operation (`:ok`, `:error`, `:redirect`). Creating an atom is cheap and creating one dynamically is not wrong by itself.\n\nThe hazard is the atom table. Atoms are never garbage collected: once interned, an atom lives in memory for the entire lifetime of the VM. The BEAM caps the atom table at 1,048,576 entries by default. That ceiling is generous for every atom a program writes in its source, but it exists precisely to bound applications that leak atoms by minting them at runtime.\n\nThe leak appears when atoms are derived from values the developer does not control: a status field in an HTTP request, a key in a decoded JSON response, a name from an external system. `String.to_atom/1` (and the equivalent `List.to_atom/1` and `:erlang.binary_to_atom/2`) interns a fresh atom for every distinct string it has never seen. Pointed at attacker-controlled input, it converts request volume directly into permanent memory growth, and a few hundred thousand unique values exhaust the table and crash the node. The same function on trusted, finite input is harmless. The anti-pattern is specifically the case where the number of atoms created is unbounded because the input is unbounded.\n\n## Decision\n\n## Consequences\n\n- The number of atoms a node holds is bounded by source code, not by request volume or payload content.\n- Input naming an unknown atom fails loudly (a failed clause or `ArgumentError`) instead of silently enlarging the never-collected atom table.\n- The 1,048,576-entry atom-table ceiling stops being reachable from the network, closing a denial-of-service vector.\n- Every accepted string resolves to an atom the compiler already interned.\n\nRule headings:\n### Rule 1: Map a closed set of strings to atoms with pattern matching\n### Rule 2: Guard open input with `String.to_existing_atom/1`","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"rule_number":null,"adr_title":"Dynamic atom creation","languages":[],"source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","ordinal":16,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-004: Dynamic atom creation"],"context":"## Context\n\nAn atom is a basic Elixir type whose value is its own name. Atoms identify resources and express the state or result of an operation (`:ok`, `:error`, `:redirect`). Creating an atom is cheap and creating one dynamically is not wrong by itself.\n\nThe hazard is the atom table. Atoms are never garbage collected: once interned, an atom lives in memory for the entire lifetime of the VM. The BEAM caps the atom table at 1,048,576 entries by default. That ceiling is generous for every atom a program writes in its source, but it exists precisely to bound applications that leak atoms by minting them at runtime.\n\nThe leak appears when atoms are derived from values the developer does not control: a status field in an HTTP request, a key in a decoded JSON response, a name from an external system. `String.to_atom/1` (and the equivalent `List.to_atom/1` and `:erlang.binary_to_atom/2`) interns a fresh atom for every distinct string it has never seen. Pointed at attacker-controlled input, it converts request volume directly into permanent memory growth, and a few hundred thousand unique values exhaust the table and crash the node. The same function on trusted, finite input is harmless. The anti-pattern is specifically the case where the number of atoms created is unbounded because the input is unbounded.","source_end_line":86,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":4,"routing_title":"Dynamic atom creation","display_text":"## Context\n\nAn atom is a basic Elixir type whose value is its own name. Atoms identify resources and express the state or result of an operation (`:ok`, `:error`, `:redirect`). Creating an atom is cheap and creating one dynamically is not wrong by itself.\n\nThe hazard is the atom table. Atoms are never garbage collected: once interned, an atom lives in memory for the entire lifetime of the VM. The BEAM caps the atom table at 1,048,576 entries by default. That ceiling is generous for every atom a program writes in its source, but it exists precisely to bound applications that leak atoms by minting them at runtime.\n\nThe leak appears when atoms are derived from values the developer does not control: a status field in an HTTP request, a key in a decoded JSON response, a name from an external system. `String.to_atom/1` (and the equivalent `List.to_atom/1` and `:erlang.binary_to_atom/2`) interns a fresh atom for every distinct string it has never seen. Pointed at attacker-controlled input, it converts request volume directly into permanent memory growth, and a few hundred thousand unique values exhaust the table and crash the node. The same function on trusted, finite input is harmless. The anti-pattern is specifically the case where the number of atoms created is unbounded because the input is unbounded.\n\n## Decision\n\n## Consequences\n\n- The number of atoms a node holds is bounded by source code, not by request volume or payload content.\n- Input naming an unknown atom fails loudly (a failed clause or `ArgumentError`) instead of silently enlarging the never-collected atom table.\n- The 1,048,576-entry atom-table ceiling stops being reachable from the network, closing a denial-of-service vector.\n- Every accepted string resolves to an atom the compiler already interned."}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"### Rule 1: Map a closed set of strings to atoms with pattern matching\n\nWhen the valid values are a small, fixed set, match them explicitly. Each branch can also carry its own logic.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    case status do\n      \"ok\" -> %{status: :ok, message: message}\n      \"error\" -> %{status: :error, message: message}\n      \"redirect\" -> %{status: :redirect, message: message, code: 302}\n    end\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    %{status: String.to_atom(status), message: message}\n  end\nend\n```\n\n**Why:** `status` comes off an external payload, so its set of values is unbounded. `String.to_atom/1` interns a new, never-collected atom for each distinct one, and the atom table is capped at 1,048,576 entries by default; an attacker who can send arbitrary status strings can fill that table and bring the node down. Pattern matching converts only the literals written in the source, so the atom count is fixed by the code and any unexpected value fails its clause loudly instead of growing memory.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Map a closed set of strings to atoms with pattern matching"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":17,"parent_id":"elixir-code-anti-patterns:adr-004","polarity":null,"record_id":"elixir-code-anti-patterns:adr-004:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"07e589b45069974060b649fca16c16ffae9f4743eade85a2f1790486f34feef0","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\n### Rule 1: Map a closed set of strings to atoms with pattern matching\n\nWhen the valid values are a small, fixed set, match them explicitly. Each branch can also carry its own logic.\n\nWhy: `status` comes off an external payload, so its set of values is unbounded. `String.to_atom/1` interns a new, never-collected atom for each distinct one, and the atom table is capped at 1,048,576 entries by default; an attacker who can send arbitrary status strings can fill that table and bring the node down. Pattern matching converts only the literals written in the source, so the atom count is fixed by the code and any unexpected value fails its clause loudly instead of growing memory.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":1,"rule_title":"Map a closed set of strings to atoms with pattern matching","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":51,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"622a31ce34ba78c0e8233e74afa573f4aeaea45d0e4ce4c724fbac32566f9e9a","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    case status do\n      \"ok\" -> %{status: :ok, message: message}\n      \"error\" -> %{status: :error, message: message}\n      \"redirect\" -> %{status: :redirect, message: message, code: 302}\n    end\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Map a closed set of strings to atoms with pattern matching","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":18,"parent_id":"elixir-code-anti-patterns:adr-004:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-004:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"7467734fc8847bb83c6d40e4132436a95fd1925d253de14a7ff1bda660ebfce4","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\nRouting title: Dynamic atom creation\n\nRule 1: Map a closed set of strings to atoms with pattern matching\n\nCorrect example (positive)\n\nWhen the valid values are a small, fixed set, match them explicitly. Each branch can also carry its own logic.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    case status do\n      \"ok\" -> %{status: :ok, message: message}\n      \"error\" -> %{status: :error, message: message}\n      \"redirect\" -> %{status: :redirect, message: message, code: 302}\n    end\n  end\nend\n```\n\nWhy: `status` comes off an external payload, so its set of values is unbounded. `String.to_atom/1` interns a new, never-collected atom for each distinct one, and the atom table is capped at 1,048,576 entries by default; an attacker who can send arbitrary status strings can fill that table and bring the node down. Pattern matching converts only the literals written in the source, so the atom count is fixed by the code and any unexpected value fails its clause loudly instead of growing memory.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":1,"rule_title":"Map a closed set of strings to atoms with pattern matching","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":39,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"7d5eccb4b40ea731393ddcb62397d67f25fedb98c5fb8ab24d3022e4ab679495","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    %{status: String.to_atom(status), message: message}\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Map a closed set of strings to atoms with pattern matching","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-01","languages":["elixir"],"ordinal":19,"parent_id":"elixir-code-anti-patterns:adr-004:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-004:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"80cff2561db7f092dccbbce94dd5a5a165893e91671bd0e385c2cc178dc49af3","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\nRouting title: Dynamic atom creation\n\nRule 1: Map a closed set of strings to atoms with pattern matching\n\nWrong example (negative)\n\nWhen the valid values are a small, fixed set, match them explicitly. Each branch can also carry its own logic.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def parse(%{\"status\" => status, \"message\" => message}) do\n    %{status: String.to_atom(status), message: message}\n  end\nend\n```\n\nWhy: `status` comes off an external payload, so its set of values is unbounded. `String.to_atom/1` interns a new, never-collected atom for each distinct one, and the atom table is capped at 1,048,576 entries by default; an attacker who can send arbitrary status strings can fill that table and bring the node down. Pattern matching converts only the literals written in the source, so the atom count is fixed by the code and any unexpected value fails its clause loudly instead of growing memory.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":1,"rule_title":"Map a closed set of strings to atoms with pattern matching","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":49,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"55a11dd600d232ed451fb2a7bea813f223cf4e1d24dc20a29606e99195e50e06","source_start_line":40,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"### Rule 2: Guard open input with `String.to_existing_atom/1`\n\nWhen the valid atoms are too many to enumerate inline but are known to already exist (struct fields, a schema's keys, atoms defined elsewhere in the program), convert through `String.to_existing_atom/1`.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    {:ok, String.to_existing_atom(name)}\n  rescue\n    ArgumentError -> {:error, :unknown_field}\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    String.to_atom(name)\n  end\nend\n```\n\n**Why:** `String.to_existing_atom/1` raises `ArgumentError` instead of interning when no matching atom is already in the table, so the atom count stays bounded by what the program defined at compile time no matter what arrives over the network. The caveat is code loading: the atom must already be referenced by loaded code (a literal, a module attribute, a struct field), or even a legitimate name raises. Prefer Rule 1 when the set is small and closed, since it needs no `rescue` and each value can branch independently.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Guard open input with `String.to_existing_atom/1`"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":20,"parent_id":"elixir-code-anti-patterns:adr-004","polarity":null,"record_id":"elixir-code-anti-patterns:adr-004:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"6bd5f52386bba4ac90df272405e91a4e97f7ad90b0c69355ba8af0167dcdbba7","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\n### Rule 2: Guard open input with `String.to_existing_atom/1`\n\nWhen the valid atoms are too many to enumerate inline but are known to already exist (struct fields, a schema's keys, atoms defined elsewhere in the program), convert through `String.to_existing_atom/1`.\n\nWhy: `String.to_existing_atom/1` raises `ArgumentError` instead of interning when no matching atom is already in the table, so the atom count stays bounded by what the program defined at compile time no matter what arrives over the network. The caveat is code loading: the atom must already be referenced by loaded code (a literal, a module attribute, a struct field), or even a legitimate name raises. Prefer Rule 1 when the set is small and closed, since it needs no `rescue` and each value can branch independently.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":2,"rule_title":"Guard open input with `String.to_existing_atom/1`","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":79,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"2335c2b8bd402a04020f7dd3a766856a902a177a97447a34648f10d02d4ae129","source_start_line":52,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    {:ok, String.to_existing_atom(name)}\n  rescue\n    ArgumentError -> {:error, :unknown_field}\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Guard open input with `String.to_existing_atom/1`","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":21,"parent_id":"elixir-code-anti-patterns:adr-004:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-004:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"d0315f4d9c0b1a82e501a8497aedd0b9e54a7fc1e0effcb03b02a01c71ccca7a","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\nRouting title: Dynamic atom creation\n\nRule 2: Guard open input with `String.to_existing_atom/1`\n\nCorrect example (positive)\n\nWhen the valid atoms are too many to enumerate inline but are known to already exist (struct fields, a schema's keys, atoms defined elsewhere in the program), convert through `String.to_existing_atom/1`.\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    {:ok, String.to_existing_atom(name)}\n  rescue\n    ArgumentError -> {:error, :unknown_field}\n  end\nend\n```\n\nWhy: `String.to_existing_atom/1` raises `ArgumentError` instead of interning when no matching atom is already in the table, so the atom count stays bounded by what the program defined at compile time no matter what arrives over the network. The caveat is code loading: the atom must already be referenced by loaded code (a literal, a module attribute, a struct field), or even a legitimate name raises. Prefer Rule 1 when the set is small and closed, since it needs no `rescue` and each value can branch independently.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":2,"rule_title":"Guard open input with `String.to_existing_atom/1`","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":67,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"b2e1b83092a13dba62598dbeea83e586a9aa27915edb54be85e02177c1cc9bb1","source_start_line":56,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"adr_id":4,"adr_title":"Dynamic atom creation","applies_to":{"content_match":["String.to_atom","List.to_atom","binary_to_atom","list_to_atom","String.to_existing_atom"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    String.to_atom(name)\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Guard open input with `String.to_existing_atom/1`","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-004:rule-02","languages":["elixir"],"ordinal":22,"parent_id":"elixir-code-anti-patterns:adr-004:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-004:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"e0c24494d2379fe5fd2d600995397a1dc6191d88e96ca53d4a369d9ef279f685","retrieval_text":"Record: elixir-code-anti-patterns:adr-004:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Dynamic atom creation\n\nRouting title: Dynamic atom creation\n\nRule 2: Guard open input with `String.to_existing_atom/1`\n\nWrong example (negative)\n\nWhen the valid atoms are too many to enumerate inline but are known to already exist (struct fields, a schema's keys, atoms defined elsewhere in the program), convert through `String.to_existing_atom/1`.\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.RequestHandler do\n  def to_field(name) when is_binary(name) do\n    String.to_atom(name)\n  end\nend\n```\n\nWhy: `String.to_existing_atom/1` raises `ArgumentError` instead of interning when no matching atom is already in the table, so the atom count stays bounded by what the program defined at compile time no matter what arrives over the network. The caveat is code loading: the atom must already be referenced by loaded code (a literal, a module attribute, a struct field), or even a legitimate name raises. Prefer Rule 1 when the set is small and closed, since it needs no `rescue` and each value can branch independently.","routing_description":"When converting external or untrusted strings into atoms, restrict the result to a known set with pattern matching or String.to_existing_atom/1 instead of String.to_atom/1.","routing_title":"Dynamic atom creation","rule_number":2,"rule_title":"Guard open input with `String.to_existing_atom/1`","schema_version":"retrieval-v1","source_description":"Converting uncontrolled external strings to atoms with String.to_atom/1 leaks atoms, which are never garbage collected and are capped at 1,048,576 per node. Map a fixed set with pattern matching, or guard open input with String.to_existing_atom/1.","source_end_line":77,"source_path":"adrs/elixir-code-anti-patterns/adr-004-dynamic-atom-creation.md","source_sha256":"91e4c67f28a1cb7997d06d7d078b87e4f7b0172ddd0f70b20cf76b58cc4b0f36","source_start_line":68,"status":"accepted","tags":["elixir","anti-pattern","atoms","memory","security","input-validation"],"updated":null}
{"references":[],"source_sha256":"1c294b9338056516fb3140951e47d79de2633b9d12b74b1e551b8171e0681818","source_start_line":12,"consequences":"## Consequences\n\n- Call sites name each value next to its key, so transposing two same-typed arguments is impossible by construction instead of compiling into swapped data.\n- Arity drops (`loan/6` becomes `loan/2`), removing the positional ordering contract callers had to memorize.\n- Optional inputs live in a trailing keyword list, so adding one does not change arity or break existing callers.\n- Functions whose parameters do not share a domain entity get split, surfacing multi-responsibility that the long signature was hiding.","rule_title":null,"routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","record_id":"elixir-code-anti-patterns:adr-005","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"350dbf2d7b0cc265ea2d382b960885adb91171be3ccc3a011630b5c628d726fc","hydrate_id":"elixir-code-anti-patterns:adr-005","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-005\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\nRouting title: Long parameter list\n\nRouting description: When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.\n\nSource description: Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.\n\n## Context\n\nElixir functions take all their inputs explicitly and return all their outputs, rather than reaching through shared mutable state. As a function accrues responsibility, its parameter count grows, and at some arity the interface becomes a memory test for the caller.\n\nThe mechanism that makes this dangerous is positional binding. Elixir matches arguments by position, not by name. When several adjacent parameters share a type (four strings in a row, two integers), transposing any two of them compiles cleanly, raises no `MatchError`, and triggers no compiler warning. The function runs with swapped values and the defect surfaces far downstream from the call site. Arity is also the ordering contract: every caller must reproduce the exact sequence, and every reorder or insertion is a breaking change across all of them.\n\nThe refactoring is to stop passing loose positional values. Group related arguments into a map or struct so each value is addressed by name, and split arguments that do not belong to one entity into separate functions.\n\n## Decision\n\n## Consequences\n\n- Call sites name each value next to its key, so transposing two same-typed arguments is impossible by construction instead of compiling into swapped data.\n- Arity drops (`loan/6` becomes `loan/2`), removing the positional ordering contract callers had to memorize.\n- Optional inputs live in a trailing keyword list, so adding one does not change arity or break existing callers.\n- Functions whose parameters do not share a domain entity get split, surfacing multi-responsibility that the long signature was hiding.\n\nRule headings:\n### Rule 1: Group related arguments into a map or struct\n### Rule 2: Split unrelated arguments into separate functions","tags":["elixir","anti-pattern","function-design","api-design","maps"],"rule_number":null,"adr_title":"Long parameter list","languages":[],"source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","ordinal":23,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-005: Long parameter list"],"context":"## Context\n\nElixir functions take all their inputs explicitly and return all their outputs, rather than reaching through shared mutable state. As a function accrues responsibility, its parameter count grows, and at some arity the interface becomes a memory test for the caller.\n\nThe mechanism that makes this dangerous is positional binding. Elixir matches arguments by position, not by name. When several adjacent parameters share a type (four strings in a row, two integers), transposing any two of them compiles cleanly, raises no `MatchError`, and triggers no compiler warning. The function runs with swapped values and the defect surfaces far downstream from the call site. Arity is also the ordering contract: every caller must reproduce the exact sequence, and every reorder or insertion is a breaking change across all of them.\n\nThe refactoring is to stop passing loose positional values. Group related arguments into a map or struct so each value is addressed by name, and split arguments that do not belong to one entity into separate functions.","source_end_line":96,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":5,"routing_title":"Long parameter list","display_text":"## Context\n\nElixir functions take all their inputs explicitly and return all their outputs, rather than reaching through shared mutable state. As a function accrues responsibility, its parameter count grows, and at some arity the interface becomes a memory test for the caller.\n\nThe mechanism that makes this dangerous is positional binding. Elixir matches arguments by position, not by name. When several adjacent parameters share a type (four strings in a row, two integers), transposing any two of them compiles cleanly, raises no `MatchError`, and triggers no compiler warning. The function runs with swapped values and the defect surfaces far downstream from the call site. Arity is also the ordering contract: every caller must reproduce the exact sequence, and every reorder or insertion is a breaking change across all of them.\n\nThe refactoring is to stop passing loose positional values. Group related arguments into a map or struct so each value is addressed by name, and split arguments that do not belong to one entity into separate functions.\n\n## Decision\n\n## Consequences\n\n- Call sites name each value next to its key, so transposing two same-typed arguments is impossible by construction instead of compiling into swapped data.\n- Arity drops (`loan/6` becomes `loan/2`), removing the positional ordering contract callers had to memorize.\n- Optional inputs live in a trailing keyword list, so adding one does not change arity or break existing callers.\n- Functions whose parameters do not share a domain entity get split, surfacing multi-responsibility that the long signature was hiding."}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"### Rule 1: Group related arguments into a map or struct\n\n**Correct:**\n\n```elixir\ndefmodule Library do\n  def loan(user, book) when is_map(user) and is_map(book) do\n    # reads user.email, book.title, ...\n  end\nend\n\n# Caller names every value at the boundary:\nLibrary.loan(\n  %{name: name, email: email, password: password, alias: alias},\n  %{title: title, ed: ed}\n)\n```\n\n**Wrong:**\n\n```elixir\ndefmodule Library do\n  def loan(user_name, email, password, user_alias, book_title, book_ed) do\n    # ...\n  end\nend\n\n# email and password transposed: compiles clean, wrong at runtime.\nLibrary.loan(name, password, email, alias, title, ed)\n```\n\n**Why:** In the six-arity version `email`, `password`, `user_alias`, `book_title`, and `book_ed` are all strings, so the transposed call binds the wrong value to each name with no `MatchError` and no warning. Grouping removes the positional slots that made the swap possible: in the literal `%{email: email, password: password}` written at the call site each value sits next to its own key, so transposing email and password is impossible by construction, there is no positional slot left to swap. Arity also drops from `loan/6` to `loan/2`, retiring the ordering contract every caller had to reproduce. Bind each group to a single variable and read its fields in the body rather than destructuring every key in the head (see ADR-003); if a grouping struct grows past the field count in ADR-010, that is a separate concern. For a private function, one mechanical split is two maps: one for data that changes and one for read-only data. Keep optional inputs in a trailing keyword list so adding one does not change arity.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Group related arguments into a map or struct"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":24,"parent_id":"elixir-code-anti-patterns:adr-005","polarity":null,"record_id":"elixir-code-anti-patterns:adr-005:rule-01","record_kind":"rule","references":[{"raw_text":"ADR-003","source_line":53,"target_id":"elixir-code-anti-patterns:adr-003"},{"raw_text":"ADR-010","source_line":53,"target_id":"elixir-code-anti-patterns:adr-010"}],"retrieval_sha256":"c37de44981c614c127875be02f0cdf30e0cef2a6bfdc3bdc5d3e631b7b8a5fa1","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\n### Rule 1: Group related arguments into a map or struct\n\nWhy: In the six-arity version `email`, `password`, `user_alias`, `book_title`, and `book_ed` are all strings, so the transposed call binds the wrong value to each name with no `MatchError` and no warning. Grouping removes the positional slots that made the swap possible: in the literal `%{email: email, password: password}` written at the call site each value sits next to its own key, so transposing email and password is impossible by construction, there is no positional slot left to swap. Arity also drops from `loan/6` to `loan/2`, retiring the ordering contract every caller had to reproduce. Bind each group to a single variable and read its fields in the body rather than destructuring every key in the head (see ADR-003); if a grouping struct grows past the field count in ADR-010, that is a separate concern. For a private function, one mechanical split is two maps: one for data that changes and one for read-only data. Keep optional inputs in a trailing keyword list so adding one does not change arity.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":1,"rule_title":"Group related arguments into a map or struct","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":54,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"c4bfb436638f13e5417298408036c24fd7ae339e88631c8798c156d35bc62a2b","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule Library do\n  def loan(user, book) when is_map(user) and is_map(book) do\n    # reads user.email, book.title, ...\n  end\nend\n\n# Caller names every value at the boundary:\nLibrary.loan(\n  %{name: name, email: email, password: password, alias: alias},\n  %{title: title, ed: ed}\n)\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Group related arguments into a map or struct","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":25,"parent_id":"elixir-code-anti-patterns:adr-005:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-005:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"4e5c372081e8754f3937eb3ab2dbbdbef0b532c4939367547127a9a042765d4c","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\nRouting title: Long parameter list\n\nRule 1: Group related arguments into a map or struct\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule Library do\n  def loan(user, book) when is_map(user) and is_map(book) do\n    # reads user.email, book.title, ...\n  end\nend\n\n# Caller names every value at the boundary:\nLibrary.loan(\n  %{name: name, email: email, password: password, alias: alias},\n  %{title: title, ed: ed}\n)\n```\n\nWhy: In the six-arity version `email`, `password`, `user_alias`, `book_title`, and `book_ed` are all strings, so the transposed call binds the wrong value to each name with no `MatchError` and no warning. Grouping removes the positional slots that made the swap possible: in the literal `%{email: email, password: password}` written at the call site each value sits next to its own key, so transposing email and password is impossible by construction, there is no positional slot left to swap. Arity also drops from `loan/6` to `loan/2`, retiring the ordering contract every caller had to reproduce. Bind each group to a single variable and read its fields in the body rather than destructuring every key in the head (see ADR-003); if a grouping struct grows past the field count in ADR-010, that is a separate concern. For a private function, one mechanical split is two maps: one for data that changes and one for read-only data. Keep optional inputs in a trailing keyword list so adding one does not change arity.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":1,"rule_title":"Group related arguments into a map or struct","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":39,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"00d9189038616abe006f687e646b64ce25689d4e8cb0cd20ead27ed5d22c9bdd","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule Library do\n  def loan(user_name, email, password, user_alias, book_title, book_ed) do\n    # ...\n  end\nend\n\n# email and password transposed: compiles clean, wrong at runtime.\nLibrary.loan(name, password, email, alias, title, ed)\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Group related arguments into a map or struct","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-01","languages":["elixir"],"ordinal":26,"parent_id":"elixir-code-anti-patterns:adr-005:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-005:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"dc3cfeaa50306085541907043e15d45f1a6836da25c2eda491b3b33b7c57d1f2","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\nRouting title: Long parameter list\n\nRule 1: Group related arguments into a map or struct\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule Library do\n  def loan(user_name, email, password, user_alias, book_title, book_ed) do\n    # ...\n  end\nend\n\n# email and password transposed: compiles clean, wrong at runtime.\nLibrary.loan(name, password, email, alias, title, ed)\n```\n\nWhy: In the six-arity version `email`, `password`, `user_alias`, `book_title`, and `book_ed` are all strings, so the transposed call binds the wrong value to each name with no `MatchError` and no warning. Grouping removes the positional slots that made the swap possible: in the literal `%{email: email, password: password}` written at the call site each value sits next to its own key, so transposing email and password is impossible by construction, there is no positional slot left to swap. Arity also drops from `loan/6` to `loan/2`, retiring the ordering contract every caller had to reproduce. Bind each group to a single variable and read its fields in the body rather than destructuring every key in the head (see ADR-003); if a grouping struct grows past the field count in ADR-010, that is a separate concern. For a private function, one mechanical split is two maps: one for data that changes and one for read-only data. Keep optional inputs in a trailing keyword list so adding one does not change arity.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":1,"rule_title":"Group related arguments into a map or struct","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":52,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"fc8fc519dddd7853f6cff632e43aad6f95c2e50c7e06573c9ce4d7e54a73ae48","source_start_line":40,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"### Rule 2: Split unrelated arguments into separate functions\n\n**Correct:**\n\n```elixir\ndefmodule Report do\n  def build(rows, opts) do\n    rows\n    |> sort(opts.sort_key)\n    |> paginate(opts.page_size)\n    |> format(opts.locale, opts.currency)\n  end\nend\n\n# Delivery is a separate responsibility, not a parameter of build:\ndefmodule Report.Mailer do\n  def deliver(report, recipient, host) do\n    # ...\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule Report do\n  # Sorting, pagination, formatting, and delivery in one head.\n  def generate(rows, sort_key, page_size, locale, currency, smtp_host, recipient) do\n    # ...\n  end\nend\n```\n\n**Why:** When half a dozen parameters do not share a domain entity, the arity is counting responsibilities, not one entity's fields, and the function is trying to do too much. Forcing `smtp_host` and `recipient` into the same map as `sort_key` would be a false grouping the data does not justify. Each independent parameter is a separate axis of change, so a function that holds all of them has multiple reasons to change. Splitting gives `build/2` only the report-shaping inputs and `deliver/3` only the delivery inputs, so each function changes for one reason and composes with the others through the pipe.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Split unrelated arguments into separate functions"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":27,"parent_id":"elixir-code-anti-patterns:adr-005","polarity":null,"record_id":"elixir-code-anti-patterns:adr-005:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"fee70940742efff4955efbe09fccbe367a5001b8ee3a7da6f0d7f4fd941baeb5","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\n### Rule 2: Split unrelated arguments into separate functions\n\nWhy: When half a dozen parameters do not share a domain entity, the arity is counting responsibilities, not one entity's fields, and the function is trying to do too much. Forcing `smtp_host` and `recipient` into the same map as `sort_key` would be a false grouping the data does not justify. Each independent parameter is a separate axis of change, so a function that holds all of them has multiple reasons to change. Splitting gives `build/2` only the report-shaping inputs and `deliver/3` only the delivery inputs, so each function changes for one reason and composes with the others through the pipe.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":2,"rule_title":"Split unrelated arguments into separate functions","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":89,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"f28284d29b88a2e2e7185419c53cd464c92d8c5371b267dcac8e417176f515b7","source_start_line":55,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule Report do\n  def build(rows, opts) do\n    rows\n    |> sort(opts.sort_key)\n    |> paginate(opts.page_size)\n    |> format(opts.locale, opts.currency)\n  end\nend\n\n# Delivery is a separate responsibility, not a parameter of build:\ndefmodule Report.Mailer do\n  def deliver(report, recipient, host) do\n    # ...\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Split unrelated arguments into separate functions","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":28,"parent_id":"elixir-code-anti-patterns:adr-005:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-005:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"41679b62e2ea6e856885cf86cd644bba839df73ba8acf0407df4c0b62e70a5f8","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\nRouting title: Long parameter list\n\nRule 2: Split unrelated arguments into separate functions\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule Report do\n  def build(rows, opts) do\n    rows\n    |> sort(opts.sort_key)\n    |> paginate(opts.page_size)\n    |> format(opts.locale, opts.currency)\n  end\nend\n\n# Delivery is a separate responsibility, not a parameter of build:\ndefmodule Report.Mailer do\n  def deliver(report, recipient, host) do\n    # ...\n  end\nend\n```\n\nWhy: When half a dozen parameters do not share a domain entity, the arity is counting responsibilities, not one entity's fields, and the function is trying to do too much. Forcing `smtp_host` and `recipient` into the same map as `sort_key` would be a false grouping the data does not justify. Each independent parameter is a separate axis of change, so a function that holds all of them has multiple reasons to change. Splitting gives `build/2` only the report-shaping inputs and `deliver/3` only the delivery inputs, so each function changes for one reason and composes with the others through the pipe.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":2,"rule_title":"Split unrelated arguments into separate functions","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":76,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"81c38cfbe97cc1a28ece51459fba328b3c9987e50bbd8891d68bb5c62e6610dc","source_start_line":57,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"adr_id":5,"adr_title":"Long parameter list","applies_to":{"content_match":["def ","\\\\ nil","\\\\ false","\\\\ 0"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule Report do\n  # Sorting, pagination, formatting, and delivery in one head.\n  def generate(rows, sort_key, page_size, locale, currency, smtp_host, recipient) do\n    # ...\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Split unrelated arguments into separate functions","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-005:rule-02","languages":["elixir"],"ordinal":29,"parent_id":"elixir-code-anti-patterns:adr-005:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-005:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"833fec9174d5b85a19416ce75d93c4893716bca39aea5ae828a234b6b6fac5f6","retrieval_text":"Record: elixir-code-anti-patterns:adr-005:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Long parameter list\n\nRouting title: Long parameter list\n\nRule 2: Split unrelated arguments into separate functions\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule Report do\n  # Sorting, pagination, formatting, and delivery in one head.\n  def generate(rows, sort_key, page_size, locale, currency, smtp_host, recipient) do\n    # ...\n  end\nend\n```\n\nWhy: When half a dozen parameters do not share a domain entity, the arity is counting responsibilities, not one entity's fields, and the function is trying to do too much. Forcing `smtp_host` and `recipient` into the same map as `sort_key` would be a false grouping the data does not justify. Each independent parameter is a separate axis of change, so a function that holds all of them has multiple reasons to change. Splitting gives `build/2` only the report-shaping inputs and `deliver/3` only the delivery inputs, so each function changes for one reason and composes with the others through the pipe.","routing_description":"When a function takes more than a handful of positional parameters, group related ones into a map or struct and split unrelated ones into separate single-responsibility functions.","routing_title":"Long parameter list","rule_number":2,"rule_title":"Split unrelated arguments into separate functions","schema_version":"retrieval-v1","source_description":"Functions with many positional parameters bind arguments by position, not by name, so same-typed parameters transpose silently with no MatchError or compiler warning. Group related arguments into a map or struct so each value is named at the call site, and split unrelated arguments into separate functions.","source_end_line":87,"source_path":"adrs/elixir-code-anti-patterns/adr-005-long-parameter-list.md","source_sha256":"55bafe30283324d2877991f10c519e95e0e85626aa8cb2fd2fed9f04f1e55f0f","source_start_line":77,"status":"accepted","tags":["elixir","anti-pattern","function-design","api-design","maps"],"updated":null}
{"references":[],"source_sha256":"e95dea773bb4a9da0ed60408feb75f4e4ce6cff1df7b16a8f5a3214eee37a6c3","source_start_line":12,"consequences":"## Consequences\n\n- Two of your library's modules can never clash with a third party's, because the unique prefix is carried by every name.\n- Module names read as self-documenting ownership: the prefix tells a reader and a tool which package a module belongs to.\n- Extensions of another library (`:plug_auth`, `:ecto_enum`) carry their own root namespace instead of borrowing the host's.\n- The sanctioned exceptions stay narrow: `defimpl`, owner-invited sub-namespaces such as `Mix.Tasks.*`, and packages you maintain on both sides of the boundary.","rule_title":null,"routing_description":"When naming modules in a library or package, prefix every module with the library's own name so module names stay globally unique on the node.","record_id":"elixir-code-anti-patterns:adr-006","applies_to":{"content_match":["defmodule Plug.","defmodule Phoenix.","defmodule Ecto.","defmodule Jason.","defmodule Mix."]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"4cf1c3aeae142d44e6cc0fa4817f7cc16212724aa27cf106ed5b4757236f2113","hydrate_id":"elixir-code-anti-patterns:adr-006","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-006\n\nDomain: elixir-code-anti-patterns\n\nADR: Namespace trespassing\n\nRouting title: Namespace trespassing\n\nRouting description: When naming modules in a library or package, prefix every module with the library's own name so module names stay globally unique on the node.\n\nSource description: A library must define every module under a prefix derived from its own package name, because the BEAM loads exactly one module per fully qualified name per node, so two libraries defining the same name become mutually incompatible.\n\n## Context\n\nThe BEAM identifies a module by its fully qualified name, which is an atom. At any instant the virtual machine holds exactly one loaded instance per module name across the entire node. Two libraries that both define a module under the same name cannot coexist in a single release: code loading is keyed on the name alone, so whichever beam file loads wins and the other library silently runs against an implementation it did not write.\n\nA library prevents this by treating its own name as a prefix and defining every module beneath it. A package named `:my_lib` defines `MyLib`, `MyLib.User`, `MyLib.Application`, and nothing outside the `MyLib` namespace. The prefix is the only thing that makes the names globally unique, because the runtime has no per-library scoping. Defining a module outside your own namespace, trespassing into a namespace owned by another package or the standard library, is the anti-pattern.\n\nThe hazard is latent and lands on someone else. An extension package `:plug_auth` that defines `Plug.Auth` works fine until `Plug` itself ships a `Plug.Auth` module in a later release. From that point the two packages define the same name and become mutually incompatible, and the clash surfaces at a downstream user's dependency resolution rather than at your build.\n\n## Decision\n\n## Consequences\n\n- Two of your library's modules can never clash with a third party's, because the unique prefix is carried by every name.\n- Module names read as self-documenting ownership: the prefix tells a reader and a tool which package a module belongs to.\n- Extensions of another library (`:plug_auth`, `:ecto_enum`) carry their own root namespace instead of borrowing the host's.\n- The sanctioned exceptions stay narrow: `defimpl`, owner-invited sub-namespaces such as `Mix.Tasks.*`, and packages you maintain on both sides of the boundary.\n\nRule headings:\n### Rule 1: Prefix every module with your library's name","tags":["elixir","anti-pattern","modules","libraries","code-loading"],"rule_number":null,"adr_title":"Namespace trespassing","languages":[],"source_description":"A library must define every module under a prefix derived from its own package name, because the BEAM loads exactly one module per fully qualified name per node, so two libraries defining the same name become mutually incompatible.","ordinal":30,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-006-namespace-trespassing.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-006: Namespace trespassing"],"context":"## Context\n\nThe BEAM identifies a module by its fully qualified name, which is an atom. At any instant the virtual machine holds exactly one loaded instance per module name across the entire node. Two libraries that both define a module under the same name cannot coexist in a single release: code loading is keyed on the name alone, so whichever beam file loads wins and the other library silently runs against an implementation it did not write.\n\nA library prevents this by treating its own name as a prefix and defining every module beneath it. A package named `:my_lib` defines `MyLib`, `MyLib.User`, `MyLib.Application`, and nothing outside the `MyLib` namespace. The prefix is the only thing that makes the names globally unique, because the runtime has no per-library scoping. Defining a module outside your own namespace, trespassing into a namespace owned by another package or the standard library, is the anti-pattern.\n\nThe hazard is latent and lands on someone else. An extension package `:plug_auth` that defines `Plug.Auth` works fine until `Plug` itself ships a `Plug.Auth` module in a later release. From that point the two packages define the same name and become mutually incompatible, and the clash surfaces at a downstream user's dependency resolution rather than at your build.","source_end_line":54,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":6,"routing_title":"Namespace trespassing","display_text":"## Context\n\nThe BEAM identifies a module by its fully qualified name, which is an atom. At any instant the virtual machine holds exactly one loaded instance per module name across the entire node. Two libraries that both define a module under the same name cannot coexist in a single release: code loading is keyed on the name alone, so whichever beam file loads wins and the other library silently runs against an implementation it did not write.\n\nA library prevents this by treating its own name as a prefix and defining every module beneath it. A package named `:my_lib` defines `MyLib`, `MyLib.User`, `MyLib.Application`, and nothing outside the `MyLib` namespace. The prefix is the only thing that makes the names globally unique, because the runtime has no per-library scoping. Defining a module outside your own namespace, trespassing into a namespace owned by another package or the standard library, is the anti-pattern.\n\nThe hazard is latent and lands on someone else. An extension package `:plug_auth` that defines `Plug.Auth` works fine until `Plug` itself ships a `Plug.Auth` module in a later release. From that point the two packages define the same name and become mutually incompatible, and the clash surfaces at a downstream user's dependency resolution rather than at your build.\n\n## Decision\n\n## Consequences\n\n- Two of your library's modules can never clash with a third party's, because the unique prefix is carried by every name.\n- Module names read as self-documenting ownership: the prefix tells a reader and a tool which package a module belongs to.\n- Extensions of another library (`:plug_auth`, `:ecto_enum`) carry their own root namespace instead of borrowing the host's.\n- The sanctioned exceptions stay narrow: `defimpl`, owner-invited sub-namespaces such as `Mix.Tasks.*`, and packages you maintain on both sides of the boundary."}
{"adr_id":6,"adr_title":"Namespace trespassing","applies_to":{"content_match":["defmodule Plug.","defmodule Phoenix.","defmodule Ecto.","defmodule Jason.","defmodule Mix."]},"date":"2026-06-28","display_text":"### Rule 1: Prefix every module with your library's name\n\n**Correct:**\n\n```elixir\n# package :plug_auth\ndefmodule PlugAuth do\n  # ...\nend\n\ndefmodule PlugAuth.Pipeline do\n  # ...\nend\n```\n\n**Wrong:**\n\n```elixir\n# package :plug_auth\ndefmodule Plug.Auth do\n  # ...\nend\n```\n\n**Why:** The BEAM loads exactly one module per fully qualified name per node. If `Plug` later defines its own `Plug.Auth`, the two definitions collide and the libraries cannot be used together, with the loaded one shadowing the other. A unique top-level prefix derived from the package name is the only guarantee against this collision. Three exceptions are sanctioned: protocol implementations are defined under the protocol's namespace by design (`defimpl`); a namespace owner may explicitly invite extensions into a sub-namespace, as Elixir does with custom Mix tasks under `Mix.Tasks.*`; and if you maintain both packages, you may share a namespace and own the responsibility for any future conflict.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Prefix every module with your library's name"],"hydrate_id":"elixir-code-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":31,"parent_id":"elixir-code-anti-patterns:adr-006","polarity":null,"record_id":"elixir-code-anti-patterns:adr-006:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"cd34076e56972ee4d6806cdeba715a748255003d25dbaa76bc86162f0a8f96fb","retrieval_text":"Record: elixir-code-anti-patterns:adr-006:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Namespace trespassing\n\n### Rule 1: Prefix every module with your library's name\n\nWhy: The BEAM loads exactly one module per fully qualified name per node. If `Plug` later defines its own `Plug.Auth`, the two definitions collide and the libraries cannot be used together, with the loaded one shadowing the other. A unique top-level prefix derived from the package name is the only guarantee against this collision. Three exceptions are sanctioned: protocol implementations are defined under the protocol's namespace by design (`defimpl`); a namespace owner may explicitly invite extensions into a sub-namespace, as Elixir does with custom Mix tasks under `Mix.Tasks.*`; and if you maintain both packages, you may share a namespace and own the responsibility for any future conflict.","routing_description":"When naming modules in a library or package, prefix every module with the library's own name so module names stay globally unique on the node.","routing_title":"Namespace trespassing","rule_number":1,"rule_title":"Prefix every module with your library's name","schema_version":"retrieval-v1","source_description":"A library must define every module under a prefix derived from its own package name, because the BEAM loads exactly one module per fully qualified name per node, so two libraries defining the same name become mutually incompatible.","source_end_line":47,"source_path":"adrs/elixir-code-anti-patterns/adr-006-namespace-trespassing.md","source_sha256":"fdf326ad990d992a6e160951f84c5477837519994dbf0bf82adb3ef154959da1","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","modules","libraries","code-loading"],"updated":null}
{"adr_id":6,"adr_title":"Namespace trespassing","applies_to":{"content_match":["defmodule Plug.","defmodule Phoenix.","defmodule Ecto.","defmodule Jason.","defmodule Mix."]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\n# package :plug_auth\ndefmodule PlugAuth do\n  # ...\nend\n\ndefmodule PlugAuth.Pipeline do\n  # ...\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Prefix every module with your library's name","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":32,"parent_id":"elixir-code-anti-patterns:adr-006:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-006:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"e21ef21d24cdfac8a5ceebde18c136441dbc7d4557fccb1e958e644af1148bfa","retrieval_text":"Record: elixir-code-anti-patterns:adr-006:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Namespace trespassing\n\nRouting title: Namespace trespassing\n\nRule 1: Prefix every module with your library's name\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\n# package :plug_auth\ndefmodule PlugAuth do\n  # ...\nend\n\ndefmodule PlugAuth.Pipeline do\n  # ...\nend\n```\n\nWhy: The BEAM loads exactly one module per fully qualified name per node. If `Plug` later defines its own `Plug.Auth`, the two definitions collide and the libraries cannot be used together, with the loaded one shadowing the other. A unique top-level prefix derived from the package name is the only guarantee against this collision. Three exceptions are sanctioned: protocol implementations are defined under the protocol's namespace by design (`defimpl`); a namespace owner may explicitly invite extensions into a sub-namespace, as Elixir does with custom Mix tasks under `Mix.Tasks.*`; and if you maintain both packages, you may share a namespace and own the responsibility for any future conflict.","routing_description":"When naming modules in a library or package, prefix every module with the library's own name so module names stay globally unique on the node.","routing_title":"Namespace trespassing","rule_number":1,"rule_title":"Prefix every module with your library's name","schema_version":"retrieval-v1","source_description":"A library must define every module under a prefix derived from its own package name, because the BEAM loads exactly one module per fully qualified name per node, so two libraries defining the same name become mutually incompatible.","source_end_line":36,"source_path":"adrs/elixir-code-anti-patterns/adr-006-namespace-trespassing.md","source_sha256":"12927dc8bec4c1ab82eb7623346d31676e394bfacdc51dbfc368de31625b9c36","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","modules","libraries","code-loading"],"updated":null}
{"adr_id":6,"adr_title":"Namespace trespassing","applies_to":{"content_match":["defmodule Plug.","defmodule Phoenix.","defmodule Ecto.","defmodule Jason.","defmodule Mix."]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\n# package :plug_auth\ndefmodule Plug.Auth do\n  # ...\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Prefix every module with your library's name","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-006:rule-01","languages":["elixir"],"ordinal":33,"parent_id":"elixir-code-anti-patterns:adr-006:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-006:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"b4a8d5c9a643d26abcb4e0111490df545f52e1e4c5c5c331c54b588d8bbeef59","retrieval_text":"Record: elixir-code-anti-patterns:adr-006:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Namespace trespassing\n\nRouting title: Namespace trespassing\n\nRule 1: Prefix every module with your library's name\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\n# package :plug_auth\ndefmodule Plug.Auth do\n  # ...\nend\n```\n\nWhy: The BEAM loads exactly one module per fully qualified name per node. If `Plug` later defines its own `Plug.Auth`, the two definitions collide and the libraries cannot be used together, with the loaded one shadowing the other. A unique top-level prefix derived from the package name is the only guarantee against this collision. Three exceptions are sanctioned: protocol implementations are defined under the protocol's namespace by design (`defimpl`); a namespace owner may explicitly invite extensions into a sub-namespace, as Elixir does with custom Mix tasks under `Mix.Tasks.*`; and if you maintain both packages, you may share a namespace and own the responsibility for any future conflict.","routing_description":"When naming modules in a library or package, prefix every module with the library's own name so module names stay globally unique on the node.","routing_title":"Namespace trespassing","rule_number":1,"rule_title":"Prefix every module with your library's name","schema_version":"retrieval-v1","source_description":"A library must define every module under a prefix derived from its own package name, because the BEAM loads exactly one module per fully qualified name per node, so two libraries defining the same name become mutually incompatible.","source_end_line":45,"source_path":"adrs/elixir-code-anti-patterns/adr-006-namespace-trespassing.md","source_sha256":"6723d7fd237610bff29b87452ac49fcbe46d66d2b4fcd441c58f0e1681ea367c","source_start_line":37,"status":"accepted","tags":["elixir","anti-pattern","modules","libraries","code-loading"],"updated":null}
{"references":[{"raw_text":"ADR-010","source_line":71,"target_id":"elixir-code-anti-patterns:adr-010"}],"source_sha256":"d7a5e22cac1ff54b3faab484ba5da47584afd51e5439ca0493b6817fbe03681c","source_start_line":12,"consequences":"## Consequences\n\n- A missing required key raises `KeyError` at the access site, not after a `nil` has travelled through unrelated code and crashed somewhere else.\n- The access notation documents, per key, whether that key is required (`map.key`) or optional (`map[:key]`), making the function's expectations legible to readers and the compiler.\n- Under static access on structs, the compiler warns on misspelled or non-existent fields at compile time.\n- For data shared across modules, an `@enforce_keys` struct makes required keys static by default and raises on construction. This adds a compile-time dependency: a change to the struct's fields forces dependent modules to recompile. Weigh that (and field-count limits) before defining wide structs; see ADR-010.\n- Dynamic access stays where it belongs: optional keys and `Access`-based traversal with runtime keys (`map[some_var]`).","rule_title":null,"routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","record_id":"elixir-code-anti-patterns:adr-007","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"e1576cc9f6fd4f6c43743e1db15f9ad6c731ccc5a592342b75e86886018833be","hydrate_id":"elixir-code-anti-patterns:adr-007","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-007\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\nRouting title: Non-assertive map access\n\nRouting description: When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.\n\nSource description: Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.\n\n## Context\n\nElixir maps expose two access notations, and they are not interchangeable. `map.key` compiles to a call that returns the value when the key exists and raises `KeyError` when it does not. For structs, whose field set is known at compile time, the compiler can additionally warn on a misspelled field before the code ever runs. `map[:key]` instead routes through the `Access` behaviour: it returns the value when the key exists and `nil` when it does not. It also supports runtime keys such as `map[some_var]`, which static access cannot.\n\nThe anti-pattern is reaching for the dynamic `map[:key]` form on a key that is always supposed to be there. Because dynamic access cannot raise on a missing key, an absent required key yields `nil` instead of a crash. That `nil` then propagates through unrelated code and surfaces as a failure far from its cause (for example an `ArithmeticError` deep inside a later calculation), where the original mistake (a caller that built an incomplete map) is no longer visible. The notation also erases intent: a reader and the compiler can no longer tell which keys the function requires and which it merely tolerates.\n\n## Decision\n\n## Consequences\n\n- A missing required key raises `KeyError` at the access site, not after a `nil` has travelled through unrelated code and crashed somewhere else.\n- The access notation documents, per key, whether that key is required (`map.key`) or optional (`map[:key]`), making the function's expectations legible to readers and the compiler.\n- Under static access on structs, the compiler warns on misspelled or non-existent fields at compile time.\n- For data shared across modules, an `@enforce_keys` struct makes required keys static by default and raises on construction. This adds a compile-time dependency: a change to the struct's fields forces dependent modules to recompile. Weigh that (and field-count limits) before defining wide structs; see ADR-010.\n- Dynamic access stays where it belongs: optional keys and `Access`-based traversal with runtime keys (`map[some_var]`).\n\nRule headings:\n### Rule 1: Access required keys statically with `map.key`\n### Rule 2: Access optional keys dynamically with `map[:key]`","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"rule_number":null,"adr_title":"Non-assertive map access","languages":[],"source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","ordinal":34,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-007: Non-assertive map access"],"context":"## Context\n\nElixir maps expose two access notations, and they are not interchangeable. `map.key` compiles to a call that returns the value when the key exists and raises `KeyError` when it does not. For structs, whose field set is known at compile time, the compiler can additionally warn on a misspelled field before the code ever runs. `map[:key]` instead routes through the `Access` behaviour: it returns the value when the key exists and `nil` when it does not. It also supports runtime keys such as `map[some_var]`, which static access cannot.\n\nThe anti-pattern is reaching for the dynamic `map[:key]` form on a key that is always supposed to be there. Because dynamic access cannot raise on a missing key, an absent required key yields `nil` instead of a crash. That `nil` then propagates through unrelated code and surfaces as a failure far from its cause (for example an `ArithmeticError` deep inside a later calculation), where the original mistake (a caller that built an incomplete map) is no longer visible. The notation also erases intent: a reader and the compiler can no longer tell which keys the function requires and which it merely tolerates.","source_end_line":73,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":7,"routing_title":"Non-assertive map access","display_text":"## Context\n\nElixir maps expose two access notations, and they are not interchangeable. `map.key` compiles to a call that returns the value when the key exists and raises `KeyError` when it does not. For structs, whose field set is known at compile time, the compiler can additionally warn on a misspelled field before the code ever runs. `map[:key]` instead routes through the `Access` behaviour: it returns the value when the key exists and `nil` when it does not. It also supports runtime keys such as `map[some_var]`, which static access cannot.\n\nThe anti-pattern is reaching for the dynamic `map[:key]` form on a key that is always supposed to be there. Because dynamic access cannot raise on a missing key, an absent required key yields `nil` instead of a crash. That `nil` then propagates through unrelated code and surfaces as a failure far from its cause (for example an `ArithmeticError` deep inside a later calculation), where the original mistake (a caller that built an incomplete map) is no longer visible. The notation also erases intent: a reader and the compiler can no longer tell which keys the function requires and which it merely tolerates.\n\n## Decision\n\n## Consequences\n\n- A missing required key raises `KeyError` at the access site, not after a `nil` has travelled through unrelated code and crashed somewhere else.\n- The access notation documents, per key, whether that key is required (`map.key`) or optional (`map[:key]`), making the function's expectations legible to readers and the compiler.\n- Under static access on structs, the compiler warns on misspelled or non-existent fields at compile time.\n- For data shared across modules, an `@enforce_keys` struct makes required keys static by default and raises on construction. This adds a compile-time dependency: a change to the struct's fields forces dependent modules to recompile. Weigh that (and field-count limits) before defining wide structs; see ADR-010.\n- Dynamic access stays where it belongs: optional keys and `Access`-based traversal with runtime keys (`map[some_var]`)."}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"### Rule 1: Access required keys statically with `map.key`\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # Every point must carry :x and :y.\n  def magnitude(point) do\n    :math.sqrt(point.x * point.x + point.y * point.y)\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def magnitude(point) do\n    :math.sqrt(point[:x] * point[:x] + point[:y] * point[:y])\n  end\nend\n```\n\n**Why:** `point[:x]` goes through the `Access` behaviour and returns `nil` when `:x` is absent. A missing required key therefore produces `nil`, which feeds the multiplication and raises `ArithmeticError` on `nil * nil`, far from the caller that actually built the bad map. `point.x` raises `KeyError` at the access site the instant the key is missing, naming the key and the map, and for structs the compiler warns on a typo'd field at compile time. When you need to assert several required keys at once, pattern match them in the function head (`def magnitude(%{x: x, y: y})`), which is the assertive form covered by ADR-008.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Access required keys statically with `map.key`"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-01","languages":["elixir"],"ordinal":35,"parent_id":"elixir-code-anti-patterns:adr-007","polarity":null,"record_id":"elixir-code-anti-patterns:adr-007:rule-01","record_kind":"rule","references":[{"raw_text":"ADR-008","source_line":43,"target_id":"elixir-code-anti-patterns:adr-008"}],"retrieval_sha256":"a7dfaa776af2ebeecce491954861c4e7f79d7c8037a06e0a32b4792f381322aa","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\n### Rule 1: Access required keys statically with `map.key`\n\nWhy: `point[:x]` goes through the `Access` behaviour and returns `nil` when `:x` is absent. A missing required key therefore produces `nil`, which feeds the multiplication and raises `ArithmeticError` on `nil * nil`, far from the caller that actually built the bad map. `point.x` raises `KeyError` at the access site the instant the key is missing, naming the key and the map, and for structs the compiler warns on a typo'd field at compile time. When you need to assert several required keys at once, pattern match them in the function head (`def magnitude(%{x: x, y: y})`), which is the assertive form covered by ADR-008.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":1,"rule_title":"Access required keys statically with `map.key`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":44,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"fc231d951c3692eaca58371ef6c4a5933a8f46bb2d7d00a16cd026c9a5e05c47","source_start_line":20,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # Every point must carry :x and :y.\n  def magnitude(point) do\n    :math.sqrt(point.x * point.x + point.y * point.y)\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Access required keys statically with `map.key`","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-01","languages":["elixir"],"ordinal":36,"parent_id":"elixir-code-anti-patterns:adr-007:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-007:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"5572f7d4a1b2f6cf765e1a0ab4bff463a15743b805740356c7efcd92e0a9b10b","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\nRouting title: Non-assertive map access\n\nRule 1: Access required keys statically with `map.key`\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # Every point must carry :x and :y.\n  def magnitude(point) do\n    :math.sqrt(point.x * point.x + point.y * point.y)\n  end\nend\n```\n\nWhy: `point[:x]` goes through the `Access` behaviour and returns `nil` when `:x` is absent. A missing required key therefore produces `nil`, which feeds the multiplication and raises `ArithmeticError` on `nil * nil`, far from the caller that actually built the bad map. `point.x` raises `KeyError` at the access site the instant the key is missing, naming the key and the map, and for structs the compiler warns on a typo'd field at compile time. When you need to assert several required keys at once, pattern match them in the function head (`def magnitude(%{x: x, y: y})`), which is the assertive form covered by ADR-008.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":1,"rule_title":"Access required keys statically with `map.key`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":32,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"1b3a54bc65d1f9e909ec256ac84bb22fe4cfb04fe5314ea30b82eedd9e85f098","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def magnitude(point) do\n    :math.sqrt(point[:x] * point[:x] + point[:y] * point[:y])\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Access required keys statically with `map.key`","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-01","languages":["elixir"],"ordinal":37,"parent_id":"elixir-code-anti-patterns:adr-007:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-007:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"dd3fa2676a3c6fe995ae34dae14e25e8b7fed06358bcb52a0bcf59bbb5a53afa","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\nRouting title: Non-assertive map access\n\nRule 1: Access required keys statically with `map.key`\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def magnitude(point) do\n    :math.sqrt(point[:x] * point[:x] + point[:y] * point[:y])\n  end\nend\n```\n\nWhy: `point[:x]` goes through the `Access` behaviour and returns `nil` when `:x` is absent. A missing required key therefore produces `nil`, which feeds the multiplication and raises `ArithmeticError` on `nil * nil`, far from the caller that actually built the bad map. `point.x` raises `KeyError` at the access site the instant the key is missing, naming the key and the map, and for structs the compiler warns on a typo'd field at compile time. When you need to assert several required keys at once, pattern match them in the function head (`def magnitude(%{x: x, y: y})`), which is the assertive form covered by ADR-008.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":1,"rule_title":"Access required keys statically with `map.key`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":42,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"06923e9ad8c0358922b3cb3f21d71a889704687f6023cd3b2bbd12d70e4ae430","source_start_line":33,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"### Rule 2: Access optional keys dynamically with `map[:key]`\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # :z is present only for 3D points.\n  def depth(point), do: point[:z]\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def depth(point), do: point.z\nend\n```\n\n**Why:** `point.z` raises `KeyError` whenever `:z` is missing. For a genuinely optional key that is wrong: absence is a valid state and should yield `nil`, not a crash. `point[:z]` returns `nil` through the `Access` behaviour for a missing key, which is exactly the optional semantics you want. Reserve static access for keys whose absence is a bug and dynamic access for keys whose absence is allowed. When the default for a missing optional key is something other than `nil`, use `Map.get(point, :z, default)` rather than overloading either notation.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Access optional keys dynamically with `map[:key]`"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-02","languages":["elixir"],"ordinal":38,"parent_id":"elixir-code-anti-patterns:adr-007","polarity":null,"record_id":"elixir-code-anti-patterns:adr-007:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"9901e7d878fbb2de5eb5bbf3dc65b8c6c6e4fdff0347d10b7a400c9f8d35e562","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\n### Rule 2: Access optional keys dynamically with `map[:key]`\n\nWhy: `point.z` raises `KeyError` whenever `:z` is missing. For a genuinely optional key that is wrong: absence is a valid state and should yield `nil`, not a crash. `point[:z]` returns `nil` through the `Access` behaviour for a missing key, which is exactly the optional semantics you want. Reserve static access for keys whose absence is a bug and dynamic access for keys whose absence is allowed. When the default for a missing optional key is something other than `nil`, use `Map.get(point, :z, default)` rather than overloading either notation.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":2,"rule_title":"Access optional keys dynamically with `map[:key]`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":65,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"1bd37a3f19f23c74648fe8e39981fb5052a353ef8ea7d1388762ea374bf60686","source_start_line":45,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # :z is present only for 3D points.\n  def depth(point), do: point[:z]\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Access optional keys dynamically with `map[:key]`","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-02","languages":["elixir"],"ordinal":39,"parent_id":"elixir-code-anti-patterns:adr-007:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-007:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"18387481b6d4858a17ac791c262a1206edbfc97633d1866ba87b02b46cfaecbe","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\nRouting title: Non-assertive map access\n\nRule 2: Access optional keys dynamically with `map[:key]`\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  # :z is present only for 3D points.\n  def depth(point), do: point[:z]\nend\n```\n\nWhy: `point.z` raises `KeyError` whenever `:z` is missing. For a genuinely optional key that is wrong: absence is a valid state and should yield `nil`, not a crash. `point[:z]` returns `nil` through the `Access` behaviour for a missing key, which is exactly the optional semantics you want. Reserve static access for keys whose absence is a bug and dynamic access for keys whose absence is allowed. When the default for a missing optional key is something other than `nil`, use `Map.get(point, :z, default)` rather than overloading either notation.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":2,"rule_title":"Access optional keys dynamically with `map[:key]`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":55,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"8a5b9d94f47381b4e4b6b068e07de14ef8c7a6bd3fb6546487dc364bce8a597c","source_start_line":47,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"adr_id":7,"adr_title":"Non-assertive map access","applies_to":{"content_match":["[:","get_in(","Access.get","Map.get("]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def depth(point), do: point.z\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Access optional keys dynamically with `map[:key]`","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-007:rule-02","languages":["elixir"],"ordinal":40,"parent_id":"elixir-code-anti-patterns:adr-007:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-007:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"9985faec6cfadd3f995e7e4fd56344c2b26f1a8d332922d2a14b324cc35e3ae0","retrieval_text":"Record: elixir-code-anti-patterns:adr-007:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive map access\n\nRouting title: Non-assertive map access\n\nRule 2: Access optional keys dynamically with `map[:key]`\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Geometry do\n  def depth(point), do: point.z\nend\n```\n\nWhy: `point.z` raises `KeyError` whenever `:z` is missing. For a genuinely optional key that is wrong: absence is a valid state and should yield `nil`, not a crash. `point[:z]` returns `nil` through the `Access` behaviour for a missing key, which is exactly the optional semantics you want. Reserve static access for keys whose absence is a bug and dynamic access for keys whose absence is allowed. When the default for a missing optional key is something other than `nil`, use `Map.get(point, :z, default)` rather than overloading either notation.","routing_description":"When reading a map key that must always exist, use static `map.key` access and reserve `map[:key]` for genuinely optional keys.","routing_title":"Non-assertive map access","rule_number":2,"rule_title":"Access optional keys dynamically with `map[:key]`","schema_version":"retrieval-v1","source_description":"Access required map keys with static `map.key`, which raises `KeyError` at the access site, and reserve dynamic `map[:key]` for optional keys. Dynamic access on a missing required key returns `nil` through the Access behaviour, deferring the crash far from its cause.","source_end_line":63,"source_path":"adrs/elixir-code-anti-patterns/adr-007-non-assertive-map-access.md","source_sha256":"990feec34b33a51985baa0c845d5e78965e8cd35786720e3e800eff68712b473","source_start_line":56,"status":"accepted","tags":["elixir","anti-pattern","maps","access","assertiveness","structs"],"updated":null}
{"references":[{"raw_text":"ADR-007","source_line":18,"target_id":"elixir-code-anti-patterns:adr-007"}],"source_sha256":"0e7fdb8a49e491bd2a3b5fdb785c1beba84977d13477820658e194253e7b2467","source_start_line":12,"consequences":"## Consequences\n\n- Malformed input crashes at the line that destructures it, naming the offending value, instead of producing a plausible wrong answer somewhere downstream.\n- Failures stay inside the process that hit them, where the supervisor can report and restart, rather than propagating corrupt data to callers.\n- Adding a new return shape to a matched function surfaces as a `CaseClauseError` at every call site that did not handle it, instead of being silently captured by `_`.\n- Wildcards are reserved for values that are genuinely unused, keeping each clause's intent explicit.","rule_title":null,"routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","record_id":"elixir-code-anti-patterns:adr-008","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"e1f5d8d6504a85866f19f259b797a22bdbc947fa7056732402ad6a2339a75ec1","hydrate_id":"elixir-code-anti-patterns:adr-008","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-008\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\nRouting title: Non-assertive pattern matching\n\nRouting description: When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.\n\nSource description: Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.\n\n## Context\n\nElixir systems are built from many supervised processes. An error in one process is localized: the supervisor detects it, reports it, and can restart the process while the rest of the application keeps running. That containment only works if errors actually surface. Code that defends against malformed input by returning a plausible-looking value instead of failing defeats containment, because the bad value escapes the process that produced it and corrupts callers far from the source.\n\nThe BEAM offers assertive constructs for exactly this: a match with `=`, function-head and `case/2` patterns, and guards. A failed match raises `MatchError` (or `CaseClauseError`) at the precise line of the mismatch, with the offending value in the message. That is a loud, localized failure a supervisor can act on. Defensive accessors like `Enum.at/2`, which returns `nil` for an out-of-range index, instead convert a structural violation into a silent wrong answer that looks like success.\n\nThis is the pattern-matching sibling of ADR-007 Non-assertive map access, which covers reading keys out of maps.\n\n## Decision\n\n## Consequences\n\n- Malformed input crashes at the line that destructures it, naming the offending value, instead of producing a plausible wrong answer somewhere downstream.\n- Failures stay inside the process that hit them, where the supervisor can report and restart, rather than propagating corrupt data to callers.\n- Adding a new return shape to a matched function surfaces as a `CaseClauseError` at every call site that did not handle it, instead of being silently captured by `_`.\n- Wildcards are reserved for values that are genuinely unused, keeping each clause's intent explicit.\n\nRule headings:\n### Rule 1: Destructure with a match, not positional accessors\n### Rule 2: Match every expected return shape, not a catch-all `_`","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"rule_number":null,"adr_title":"Non-assertive pattern matching","languages":[],"source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","ordinal":41,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-008: Non-assertive pattern matching"],"context":"## Context\n\nElixir systems are built from many supervised processes. An error in one process is localized: the supervisor detects it, reports it, and can restart the process while the rest of the application keeps running. That containment only works if errors actually surface. Code that defends against malformed input by returning a plausible-looking value instead of failing defeats containment, because the bad value escapes the process that produced it and corrupts callers far from the source.\n\nThe BEAM offers assertive constructs for exactly this: a match with `=`, function-head and `case/2` patterns, and guards. A failed match raises `MatchError` (or `CaseClauseError`) at the precise line of the mismatch, with the offending value in the message. That is a loud, localized failure a supervisor can act on. Defensive accessors like `Enum.at/2`, which returns `nil` for an out-of-range index, instead convert a structural violation into a silent wrong answer that looks like success.\n\nThis is the pattern-matching sibling of ADR-007 Non-assertive map access, which covers reading keys out of maps.","source_end_line":84,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":8,"routing_title":"Non-assertive pattern matching","display_text":"## Context\n\nElixir systems are built from many supervised processes. An error in one process is localized: the supervisor detects it, reports it, and can restart the process while the rest of the application keeps running. That containment only works if errors actually surface. Code that defends against malformed input by returning a plausible-looking value instead of failing defeats containment, because the bad value escapes the process that produced it and corrupts callers far from the source.\n\nThe BEAM offers assertive constructs for exactly this: a match with `=`, function-head and `case/2` patterns, and guards. A failed match raises `MatchError` (or `CaseClauseError`) at the precise line of the mismatch, with the offending value in the message. That is a loud, localized failure a supervisor can act on. Defensive accessors like `Enum.at/2`, which returns `nil` for an out-of-range index, instead convert a structural violation into a silent wrong answer that looks like success.\n\nThis is the pattern-matching sibling of ADR-007 Non-assertive map access, which covers reading keys out of maps.\n\n## Decision\n\n## Consequences\n\n- Malformed input crashes at the line that destructures it, naming the offending value, instead of producing a plausible wrong answer somewhere downstream.\n- Failures stay inside the process that hit them, where the supervisor can report and restart, rather than propagating corrupt data to callers.\n- Adding a new return shape to a matched function surfaces as a `CaseClauseError` at every call site that did not handle it, instead of being silently captured by `_`.\n- Wildcards are reserved for values that are genuinely unused, keeping each clause's intent explicit."}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"### Rule 1: Destructure with a match, not positional accessors\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      [key, value] = String.split(pair, \"=\")\n      key == desired_key && value\n    end)\n  end\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      key_value = String.split(pair, \"=\")\n      Enum.at(key_value, 0) == desired_key && Enum.at(key_value, 1)\n    end)\n  end\nend\n```\n\n**Why:** A well-formed pair `\"lab=ASERG\"` splits to `[\"lab\", \"ASERG\"]`, but a malformed segment `\"university=institution=UFMG\"` splits to `[\"university\", \"institution\", \"UFMG\"]`. `Enum.at/2` never fails on the malformed list: it reads index 0 and index 1 and reports `\"institution\"` as the value, so `get_value/2` returns a wrong answer that looks correct and propagates to its caller. The match `[key, value] = String.split(pair, \"=\")` asserts exactly two parts and raises `MatchError` on anything else, naming the offending list in the message. The crash is localized to the process that hit it and hands the decision (treat as invalid, or handle a newly discovered case) back to the caller instead of silently emitting corrupt data.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Destructure with a match, not positional accessors"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-01","languages":["elixir"],"ordinal":42,"parent_id":"elixir-code-anti-patterns:adr-008","polarity":null,"record_id":"elixir-code-anti-patterns:adr-008:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"b613287e1a06e14800f5f7b7c8a35d8bb3d56512bd14a0385fa497aeae2eab9e","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\n### Rule 1: Destructure with a match, not positional accessors\n\nWhy: A well-formed pair `\"lab=ASERG\"` splits to `[\"lab\", \"ASERG\"]`, but a malformed segment `\"university=institution=UFMG\"` splits to `[\"university\", \"institution\", \"UFMG\"]`. `Enum.at/2` never fails on the malformed list: it reads index 0 and index 1 and reports `\"institution\"` as the value, so `get_value/2` returns a wrong answer that looks correct and propagates to its caller. The match `[key, value] = String.split(pair, \"=\")` asserts exactly two parts and raises `MatchError` on anything else, naming the offending list in the message. The crash is localized to the process that hit it and hands the decision (treat as invalid, or handle a newly discovered case) back to the caller instead of silently emitting corrupt data.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":1,"rule_title":"Destructure with a match, not positional accessors","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":55,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"29326f7c488b6c1498c8de9c54cffd549e975342bbda3e59e3435a0f83a95e8a","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      [key, value] = String.split(pair, \"=\")\n      key == desired_key && value\n    end)\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Destructure with a match, not positional accessors","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-01","languages":["elixir"],"ordinal":43,"parent_id":"elixir-code-anti-patterns:adr-008:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-008:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"d278d1d9f24886da59af35a2c3ecb54de302a9d614abfaca4ce28f5cde7ed38e","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\nRouting title: Non-assertive pattern matching\n\nRule 1: Destructure with a match, not positional accessors\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      [key, value] = String.split(pair, \"=\")\n      key == desired_key && value\n    end)\n  end\nend\n```\n\nWhy: A well-formed pair `\"lab=ASERG\"` splits to `[\"lab\", \"ASERG\"]`, but a malformed segment `\"university=institution=UFMG\"` splits to `[\"university\", \"institution\", \"UFMG\"]`. `Enum.at/2` never fails on the malformed list: it reads index 0 and index 1 and reports `\"institution\"` as the value, so `get_value/2` returns a wrong answer that looks correct and propagates to its caller. The match `[key, value] = String.split(pair, \"=\")` asserts exactly two parts and raises `MatchError` on anything else, naming the offending list in the message. The crash is localized to the process that hit it and hands the decision (treat as invalid, or handle a newly discovered case) back to the caller instead of silently emitting corrupt data.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":1,"rule_title":"Destructure with a match, not positional accessors","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":38,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"78741924f4ab7914f5475ced926b91ed8426362267f2a4009178c19e5a077e81","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      key_value = String.split(pair, \"=\")\n      Enum.at(key_value, 0) == desired_key && Enum.at(key_value, 1)\n    end)\n  end\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Destructure with a match, not positional accessors","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-01","languages":["elixir"],"ordinal":44,"parent_id":"elixir-code-anti-patterns:adr-008:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-008:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"51867897b87d8e41cdfb8b685041b35b0fde0e8b2c53aec67c00fb037fbc45b8","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\nRouting title: Non-assertive pattern matching\n\nRule 1: Destructure with a match, not positional accessors\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Extract do\n  def get_value(query_string, desired_key) do\n    query_string\n    |> String.split(\"&\")\n    |> Enum.find_value(fn pair ->\n      key_value = String.split(pair, \"=\")\n      Enum.at(key_value, 0) == desired_key && Enum.at(key_value, 1)\n    end)\n  end\nend\n```\n\nWhy: A well-formed pair `\"lab=ASERG\"` splits to `[\"lab\", \"ASERG\"]`, but a malformed segment `\"university=institution=UFMG\"` splits to `[\"university\", \"institution\", \"UFMG\"]`. `Enum.at/2` never fails on the malformed list: it reads index 0 and index 1 and reports `\"institution\"` as the value, so `get_value/2` returns a wrong answer that looks correct and propagates to its caller. The match `[key, value] = String.split(pair, \"=\")` asserts exactly two parts and raises `MatchError` on anything else, naming the offending list in the message. The crash is localized to the process that hit it and hands the decision (treat as invalid, or handle a newly discovered case) back to the caller instead of silently emitting corrupt data.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":1,"rule_title":"Destructure with a match, not positional accessors","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":53,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"170c6c5e83f1cc4a18542a02cc2e8101a127e1d5f387312fc26c515655f8d12e","source_start_line":39,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"### Rule 2: Match every expected return shape, not a catch-all `_`\n\n**Correct:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  {:error, _reason} -> render_error()\nend\n```\n\n**Wrong:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  _ -> render_error()\nend\n```\n\n**Why:** A bare `_` clause matches any value, so it absorbs return shapes the author never considered. If `fetch_user/1` later gains a `{:pending, token}` return, the catch-all swallows it and renders an error for a non-error result, with no crash to point at the regression. The correct version asserts the `{:error, _reason}` tuple shape (only the unused reason is wildcarded) and leaves any unanticipated shape unmatched, so a new return raises `CaseClauseError` at the call site. That loud, localized failure is the signal that the contract changed. Reserve `_` for the inner values that are genuinely irrelevant, not for the whole result.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Match every expected return shape, not a catch-all `_`"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-02","languages":["elixir"],"ordinal":45,"parent_id":"elixir-code-anti-patterns:adr-008","polarity":null,"record_id":"elixir-code-anti-patterns:adr-008:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"e01b146f84d585f4382a07110ed11a9e10edb9991bc71f83e6a48de6d0eb5667","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\n### Rule 2: Match every expected return shape, not a catch-all `_`\n\nWhy: A bare `_` clause matches any value, so it absorbs return shapes the author never considered. If `fetch_user/1` later gains a `{:pending, token}` return, the catch-all swallows it and renders an error for a non-error result, with no crash to point at the regression. The correct version asserts the `{:error, _reason}` tuple shape (only the unused reason is wildcarded) and leaves any unanticipated shape unmatched, so a new return raises `CaseClauseError` at the call site. That loud, localized failure is the signal that the contract changed. Reserve `_` for the inner values that are genuinely irrelevant, not for the whole result.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":2,"rule_title":"Match every expected return shape, not a catch-all `_`","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":77,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"2a0e1540c32e4e193b7f32c62f3712901c7c17d8ad4d4c758494d33760cc754a","source_start_line":56,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  {:error, _reason} -> render_error()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Match every expected return shape, not a catch-all `_`","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-02","languages":["elixir"],"ordinal":46,"parent_id":"elixir-code-anti-patterns:adr-008:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-008:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"84e9b6fd2114705079037276c28ec825a7087fed677430019ce0b77cb2efcbb2","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\nRouting title: Non-assertive pattern matching\n\nRule 2: Match every expected return shape, not a catch-all `_`\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  {:error, _reason} -> render_error()\nend\n```\n\nWhy: A bare `_` clause matches any value, so it absorbs return shapes the author never considered. If `fetch_user/1` later gains a `{:pending, token}` return, the catch-all swallows it and renders an error for a non-error result, with no crash to point at the regression. The correct version asserts the `{:error, _reason}` tuple shape (only the unused reason is wildcarded) and leaves any unanticipated shape unmatched, so a new return raises `CaseClauseError` at the call site. That loud, localized failure is the signal that the contract changed. Reserve `_` for the inner values that are genuinely irrelevant, not for the whole result.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":2,"rule_title":"Match every expected return shape, not a catch-all `_`","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":66,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"51f0434c6fe84a745bd5b771c474c94864beb47302ce39f3b394acec7731c8a2","source_start_line":58,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"adr_id":8,"adr_title":"Non-assertive pattern matching","applies_to":{"content_match":["Enum.at(","_ ->","List.first(","List.last(","elem("]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  _ -> render_error()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Match every expected return shape, not a catch-all `_`","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-008:rule-02","languages":["elixir"],"ordinal":47,"parent_id":"elixir-code-anti-patterns:adr-008:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-008:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"31e73c9f2c0370fa474c19153afd002911f36518ba7f3125cf8413c09bc6877c","retrieval_text":"Record: elixir-code-anti-patterns:adr-008:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive pattern matching\n\nRouting title: Non-assertive pattern matching\n\nRule 2: Match every expected return shape, not a catch-all `_`\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ncase MyApp.Accounts.fetch_user(id) do\n  {:ok, user} -> render(user)\n  _ -> render_error()\nend\n```\n\nWhy: A bare `_` clause matches any value, so it absorbs return shapes the author never considered. If `fetch_user/1` later gains a `{:pending, token}` return, the catch-all swallows it and renders an error for a non-error result, with no crash to point at the regression. The correct version asserts the `{:error, _reason}` tuple shape (only the unused reason is wildcarded) and leaves any unanticipated shape unmatched, so a new return raises `CaseClauseError` at the call site. That loud, localized failure is the signal that the contract changed. Reserve `_` for the inner values that are genuinely irrelevant, not for the whole result.","routing_description":"When extracting fields from data of an assumed shape or branching on a function's return, destructure with a pattern match and enumerate the expected clauses instead of reaching for positional accessors or a catch-all `_`.","routing_title":"Non-assertive pattern matching","rule_number":2,"rule_title":"Match every expected return shape, not a catch-all `_`","schema_version":"retrieval-v1","source_description":"Defensive accessors like Enum.at/2 and catch-all `_` clauses return a plausible value for malformed or unexpected input instead of failing, so corruption escapes the process that produced it. Destructure with a match and enumerate the expected case clauses so a mismatch raises a localized MatchError or CaseClauseError the supervisor can act on.","source_end_line":75,"source_path":"adrs/elixir-code-anti-patterns/adr-008-non-assertive-pattern-matching.md","source_sha256":"5ec9007b478cd0a55de04d374aaabc2c6c5ab35d2f75a0d7d3189f493a3fdd83","source_start_line":67,"status":"accepted","tags":["elixir","anti-pattern","pattern-matching","error-handling","assertive-code"],"updated":null}
{"references":[{"raw_text":"ADR-007","source_line":18,"target_id":"elixir-code-anti-patterns:adr-007"},{"raw_text":"ADR-008","source_line":18,"target_id":"elixir-code-anti-patterns:adr-008"}],"source_sha256":"54b660078930fdee0206cae054346abb53d45f30cc80cfc30c0d56a34eee5a99","source_start_line":12,"consequences":"## Consequences\n\n- Boolean expressions raise `BadBooleanError` (or `ArgumentError` for `not/1`) on a non-boolean operand instead of silently coercing it as true.\n- The operator choice documents intent: `&&`/`||`/`!` mark a genuinely truthy value, `and`/`or`/`not` mark a guaranteed boolean.\n- Erlang return values such as `:undefined` and `:error` fail at the boundary instead of propagating through the system as truthy.\n- Because `and/2`, `or/2`, and `not/1` are guard-safe and compile to BEAM short-circuit operators, the same expressions can move into guards unchanged.","rule_title":null,"routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","record_id":"elixir-code-anti-patterns:adr-009","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"476c9ad57620182d388e764c9afb4111e79f5b5db2c2d005024821103f9a3f02","hydrate_id":"elixir-code-anti-patterns:adr-009","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-009\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\nRouting title: Non-assertive truthiness\n\nRouting description: When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.\n\nSource description: Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.\n\n## Context\n\nElixir has truthiness: `nil` and `false` are falsy, every other term is truthy. The operators `&&/2`, `||/2`, and `!/1` are built on truthiness. They accept any term, treat only `nil` and `false` as falsy, and short-circuit on that basis. Using them on genuinely truthy values is correct and idiomatic. The anti-pattern is reaching for them when every operand is already expected to be a boolean.\n\nElixir also provides `and/2`, `or/2`, and `not/1`. These assert their operand is a boolean: `and/2` and `or/2` require the left operand to be `true` or `false` and raise `BadBooleanError` on anything else, and `not/1` raises `ArgumentError` on a non-boolean. They compile to the BEAM `andalso`, `orelse`, and `not` operators and are allowed in guards, which the truthiness operators are not. Choosing them where booleans are guaranteed promotes the operand's type from an assumption to an enforced contract.\n\nThe risk is sharpest at Erlang boundaries. Erlang has no concept of truthiness and never returns `nil`; its functions return `:error`, `:undefined`, or other atoms in places an Elixir function would return `nil` or `false`. Every such atom except `false` is truthy, so a truthiness operator silently accepts an unexpected `:undefined` or `:error` as a real value. This is the truthiness member of the assertive-code family; see ADR-007 for map access and ADR-008 for pattern matching.\n\n## Decision\n\n## Consequences\n\n- Boolean expressions raise `BadBooleanError` (or `ArgumentError` for `not/1`) on a non-boolean operand instead of silently coercing it as true.\n- The operator choice documents intent: `&&`/`||`/`!` mark a genuinely truthy value, `and`/`or`/`not` mark a guaranteed boolean.\n- Erlang return values such as `:undefined` and `:error` fail at the boundary instead of propagating through the system as truthy.\n- Because `and/2`, `or/2`, and `not/1` are guard-safe and compile to BEAM short-circuit operators, the same expressions can move into guards unchanged.\n\nRule headings:\n### Rule 1: Use `and`/`or`/`not` when operands are booleans\n### Rule 2: Use the strict operators at Erlang boundaries","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"rule_number":null,"adr_title":"Non-assertive truthiness","languages":[],"source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","ordinal":48,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-009: Non-assertive truthiness"],"context":"## Context\n\nElixir has truthiness: `nil` and `false` are falsy, every other term is truthy. The operators `&&/2`, `||/2`, and `!/1` are built on truthiness. They accept any term, treat only `nil` and `false` as falsy, and short-circuit on that basis. Using them on genuinely truthy values is correct and idiomatic. The anti-pattern is reaching for them when every operand is already expected to be a boolean.\n\nElixir also provides `and/2`, `or/2`, and `not/1`. These assert their operand is a boolean: `and/2` and `or/2` require the left operand to be `true` or `false` and raise `BadBooleanError` on anything else, and `not/1` raises `ArgumentError` on a non-boolean. They compile to the BEAM `andalso`, `orelse`, and `not` operators and are allowed in guards, which the truthiness operators are not. Choosing them where booleans are guaranteed promotes the operand's type from an assumption to an enforced contract.\n\nThe risk is sharpest at Erlang boundaries. Erlang has no concept of truthiness and never returns `nil`; its functions return `:error`, `:undefined`, or other atoms in places an Elixir function would return `nil` or `false`. Every such atom except `false` is truthy, so a truthiness operator silently accepts an unexpected `:undefined` or `:error` as a real value. This is the truthiness member of the assertive-code family; see ADR-007 for map access and ADR-008 for pattern matching.","source_end_line":77,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":9,"routing_title":"Non-assertive truthiness","display_text":"## Context\n\nElixir has truthiness: `nil` and `false` are falsy, every other term is truthy. The operators `&&/2`, `||/2`, and `!/1` are built on truthiness. They accept any term, treat only `nil` and `false` as falsy, and short-circuit on that basis. Using them on genuinely truthy values is correct and idiomatic. The anti-pattern is reaching for them when every operand is already expected to be a boolean.\n\nElixir also provides `and/2`, `or/2`, and `not/1`. These assert their operand is a boolean: `and/2` and `or/2` require the left operand to be `true` or `false` and raise `BadBooleanError` on anything else, and `not/1` raises `ArgumentError` on a non-boolean. They compile to the BEAM `andalso`, `orelse`, and `not` operators and are allowed in guards, which the truthiness operators are not. Choosing them where booleans are guaranteed promotes the operand's type from an assumption to an enforced contract.\n\nThe risk is sharpest at Erlang boundaries. Erlang has no concept of truthiness and never returns `nil`; its functions return `:error`, `:undefined`, or other atoms in places an Elixir function would return `nil` or `false`. Every such atom except `false` is truthy, so a truthiness operator silently accepts an unexpected `:undefined` or `:error` as a real value. This is the truthiness member of the assertive-code family; see ADR-007 for map access and ADR-008 for pattern matching.\n\n## Decision\n\n## Consequences\n\n- Boolean expressions raise `BadBooleanError` (or `ArgumentError` for `not/1`) on a non-boolean operand instead of silently coercing it as true.\n- The operator choice documents intent: `&&`/`||`/`!` mark a genuinely truthy value, `and`/`or`/`not` mark a guaranteed boolean.\n- Erlang return values such as `:undefined` and `:error` fail at the boundary instead of propagating through the system as truthy.\n- Because `and/2`, `or/2`, and `not/1` are guard-safe and compile to BEAM short-circuit operators, the same expressions can move into guards unchanged."}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"### Rule 1: Use `and`/`or`/`not` when operands are booleans\n\nWhen every operand is known to be a boolean (typically the result of a type guard or a predicate), use the strict boolean operators.\n\n**Correct:**\n\n```elixir\nif is_binary(name) and is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```\n\n**Wrong:**\n\n```elixir\nif is_binary(name) && is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```\n\n**Why:** `is_binary/1` and `is_integer/1` always return a boolean. `&&/2` is the truthiness operator: it accepts any term and treats everything except `nil` and `false` as true. Using it where booleans are guaranteed is more permissive than the code needs and hides the contract that the operands are booleans. `and/2` requires its left operand to be `true` or `false` and raises `BadBooleanError` otherwise, so it both documents the boolean contract and turns a future non-boolean operand into an immediate crash instead of a silent truthy coercion. `and/2` compiles to the BEAM `andalso` short-circuit and is permitted in guards; `&&/2` is neither.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Use `and`/`or`/`not` when operands are booleans"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-01","languages":["elixir"],"ordinal":49,"parent_id":"elixir-code-anti-patterns:adr-009","polarity":null,"record_id":"elixir-code-anti-patterns:adr-009:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"56b39011cfa196524268d5be0a0cb14490da297b0ab010e56fec20a9bc472a44","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\n### Rule 1: Use `and`/`or`/`not` when operands are booleans\n\nWhen every operand is known to be a boolean (typically the result of a type guard or a predicate), use the strict boolean operators.\n\nWhy: `is_binary/1` and `is_integer/1` always return a boolean. `&&/2` is the truthiness operator: it accepts any term and treats everything except `nil` and `false` as true. Using it where booleans are guaranteed is more permissive than the code needs and hides the contract that the operands are booleans. `and/2` requires its left operand to be `true` or `false` and raises `BadBooleanError` otherwise, so it both documents the boolean contract and turns a future non-boolean operand into an immediate crash instead of a silent truthy coercion. `and/2` compiles to the BEAM `andalso` short-circuit and is permitted in guards; `&&/2` is neither.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":1,"rule_title":"Use `and`/`or`/`not` when operands are booleans","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":47,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"72e8fa8a67703fc253fceaf3becd57aa8fd5fbcf39fc5b2b6c5ffd96c88bd6db","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\nif is_binary(name) and is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Use `and`/`or`/`not` when operands are booleans","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-01","languages":["elixir"],"ordinal":50,"parent_id":"elixir-code-anti-patterns:adr-009:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-009:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"dec24999ff282ebf267932b46ef622d901aae147d3daab0135b3934346ad9a8d","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\nRouting title: Non-assertive truthiness\n\nRule 1: Use `and`/`or`/`not` when operands are booleans\n\nCorrect example (positive)\n\nWhen every operand is known to be a boolean (typically the result of a type guard or a predicate), use the strict boolean operators.\n\n**Correct:**\n\n```elixir\nif is_binary(name) and is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```\n\nWhy: `is_binary/1` and `is_integer/1` always return a boolean. `&&/2` is the truthiness operator: it accepts any term and treats everything except `nil` and `false` as true. Using it where booleans are guaranteed is more permissive than the code needs and hides the contract that the operands are booleans. `and/2` requires its left operand to be `true` or `false` and raises `BadBooleanError` otherwise, so it both documents the boolean contract and turns a future non-boolean operand into an immediate crash instead of a silent truthy coercion. `and/2` compiles to the BEAM `andalso` short-circuit and is permitted in guards; `&&/2` is neither.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":1,"rule_title":"Use `and`/`or`/`not` when operands are booleans","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":35,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"c011fe1a5fc1310cefde9523778ee929798484299dd172e4d1c11b20a79bbb61","source_start_line":26,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\nif is_binary(name) && is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Use `and`/`or`/`not` when operands are booleans","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-01","languages":["elixir"],"ordinal":51,"parent_id":"elixir-code-anti-patterns:adr-009:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-009:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"510f83dc05356b867cde5f3a78be8a0f547d995643eb74e0e3d966f63b23ff61","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\nRouting title: Non-assertive truthiness\n\nRule 1: Use `and`/`or`/`not` when operands are booleans\n\nWrong example (negative)\n\nWhen every operand is known to be a boolean (typically the result of a type guard or a predicate), use the strict boolean operators.\n\n**Wrong:**\n\n```elixir\nif is_binary(name) && is_integer(age) do\n  greet(name, age)\nelse\n  reject()\nend\n```\n\nWhy: `is_binary/1` and `is_integer/1` always return a boolean. `&&/2` is the truthiness operator: it accepts any term and treats everything except `nil` and `false` as true. Using it where booleans are guaranteed is more permissive than the code needs and hides the contract that the operands are booleans. `and/2` requires its left operand to be `true` or `false` and raises `BadBooleanError` otherwise, so it both documents the boolean contract and turns a future non-boolean operand into an immediate crash instead of a silent truthy coercion. `and/2` compiles to the BEAM `andalso` short-circuit and is permitted in guards; `&&/2` is neither.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":1,"rule_title":"Use `and`/`or`/`not` when operands are booleans","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":45,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"307afb57d28a09c49729ac434d3b3364270cfc22e7dc3f9a7146f6d21061c197","source_start_line":36,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"### Rule 2: Use the strict operators at Erlang boundaries\n\nWhen a value comes from an Erlang API, combine it with the strict operators so an unexpected non-boolean fails loud.\n\n**Correct:**\n\n```elixir\ndefp authorized?(token) do\n  # :my_authz.check/1 returns true | false | :undefined\n  :my_authz.check(token) or admin_override?()\nend\n```\n\n**Wrong:**\n\n```elixir\ndefp authorized?(token) do\n  :my_authz.check(token) || admin_override?()\nend\n```\n\n**Why:** Erlang has no truthiness and never returns `nil`; `check/1` can return `:undefined`. To `||/2` the atom `:undefined` is truthy, so the wrong version short-circuits and returns `:undefined`, and the caller treats authorization as granted on a value that signals \"no answer.\" `or/2` requires a boolean left operand and raises `BadBooleanError` on `:undefined`, converting a silent misinterpretation into a loud failure exactly at the boundary where the unexpected shape entered. The same reasoning applies to `and/2` and `not/1` against any Erlang return that may be `:error`, `:undefined`, or another non-boolean atom.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Use the strict operators at Erlang boundaries"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-02","languages":["elixir"],"ordinal":52,"parent_id":"elixir-code-anti-patterns:adr-009","polarity":null,"record_id":"elixir-code-anti-patterns:adr-009:rule-02","record_kind":"rule","references":[],"retrieval_sha256":"3e9cd2494cfeb7b4d2fe3faee8687b551cf55dda6267f07df34e036abef06491","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-02\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\n### Rule 2: Use the strict operators at Erlang boundaries\n\nWhen a value comes from an Erlang API, combine it with the strict operators so an unexpected non-boolean fails loud.\n\nWhy: Erlang has no truthiness and never returns `nil`; `check/1` can return `:undefined`. To `||/2` the atom `:undefined` is truthy, so the wrong version short-circuits and returns `:undefined`, and the caller treats authorization as granted on a value that signals \"no answer.\" `or/2` requires a boolean left operand and raises `BadBooleanError` on `:undefined`, converting a silent misinterpretation into a loud failure exactly at the boundary where the unexpected shape entered. The same reasoning applies to `and/2` and `not/1` against any Erlang return that may be `:error`, `:undefined`, or another non-boolean atom.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":2,"rule_title":"Use the strict operators at Erlang boundaries","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":70,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"a35f3862d30d046ede62f2e0f1635895a8a5860dd5bbcf740151db3240a20434","source_start_line":48,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefp authorized?(token) do\n  # :my_authz.check/1 returns true | false | :undefined\n  :my_authz.check(token) or admin_override?()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Use the strict operators at Erlang boundaries","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-02","languages":["elixir"],"ordinal":53,"parent_id":"elixir-code-anti-patterns:adr-009:rule-02","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-009:rule-02:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"3001f049f4767d3ec5423aeabc7370d52d17af5536f154a9f6c547d4e3232f57","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-02:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\nRouting title: Non-assertive truthiness\n\nRule 2: Use the strict operators at Erlang boundaries\n\nCorrect example (positive)\n\nWhen a value comes from an Erlang API, combine it with the strict operators so an unexpected non-boolean fails loud.\n\n**Correct:**\n\n```elixir\ndefp authorized?(token) do\n  # :my_authz.check/1 returns true | false | :undefined\n  :my_authz.check(token) or admin_override?()\nend\n```\n\nWhy: Erlang has no truthiness and never returns `nil`; `check/1` can return `:undefined`. To `||/2` the atom `:undefined` is truthy, so the wrong version short-circuits and returns `:undefined`, and the caller treats authorization as granted on a value that signals \"no answer.\" `or/2` requires a boolean left operand and raises `BadBooleanError` on `:undefined`, converting a silent misinterpretation into a loud failure exactly at the boundary where the unexpected shape entered. The same reasoning applies to `and/2` and `not/1` against any Erlang return that may be `:error`, `:undefined`, or another non-boolean atom.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":2,"rule_title":"Use the strict operators at Erlang boundaries","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":60,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"efe96874ca5afa449d5930ed1662bd13f5aa8bdbb9c4610e63e715e7fe57f3c3","source_start_line":52,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"adr_id":9,"adr_title":"Non-assertive truthiness","applies_to":{"content_match":["&&","||","&& is_","|| is_"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefp authorized?(token) do\n  :my_authz.check(token) || admin_override?()\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 2: Use the strict operators at Erlang boundaries","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-009:rule-02","languages":["elixir"],"ordinal":54,"parent_id":"elixir-code-anti-patterns:adr-009:rule-02","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-009:rule-02:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"26550789b37f2f70fc9fb3ffa49831f4f10c559cb9762d44d7710fd7bb4d21ee","retrieval_text":"Record: elixir-code-anti-patterns:adr-009:rule-02:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Non-assertive truthiness\n\nRouting title: Non-assertive truthiness\n\nRule 2: Use the strict operators at Erlang boundaries\n\nWrong example (negative)\n\nWhen a value comes from an Erlang API, combine it with the strict operators so an unexpected non-boolean fails loud.\n\n**Wrong:**\n\n```elixir\ndefp authorized?(token) do\n  :my_authz.check(token) || admin_override?()\nend\n```\n\nWhy: Erlang has no truthiness and never returns `nil`; `check/1` can return `:undefined`. To `||/2` the atom `:undefined` is truthy, so the wrong version short-circuits and returns `:undefined`, and the caller treats authorization as granted on a value that signals \"no answer.\" `or/2` requires a boolean left operand and raises `BadBooleanError` on `:undefined`, converting a silent misinterpretation into a loud failure exactly at the boundary where the unexpected shape entered. The same reasoning applies to `and/2` and `not/1` against any Erlang return that may be `:error`, `:undefined`, or another non-boolean atom.","routing_description":"When combining boolean operands or values returned by Erlang APIs, use `and`/`or`/`not` instead of `&&`/`||`/`!` so a non-boolean operand raises instead of coercing as truthy.","routing_title":"Non-assertive truthiness","rule_number":2,"rule_title":"Use the strict operators at Erlang boundaries","schema_version":"retrieval-v1","source_description":"Elixir's `&&/2`, `||/2`, and `!/1` operate on truthiness and silently coerce any non-`nil`, non-`false` term as true. When operands are guaranteed booleans, use `and/2`, `or/2`, and `not/1`, which require a boolean operand and raise `BadBooleanError` otherwise, catching unexpected non-boolean values such as Erlang's `:undefined` at the boundary.","source_end_line":68,"source_path":"adrs/elixir-code-anti-patterns/adr-009-non-assertive-truthiness.md","source_sha256":"fb277f3b5f96cda7162ec97dbf2b995983e392d0aca056c1209e8d7943045a90","source_start_line":61,"status":"accepted","tags":["elixir","anti-pattern","truthiness","booleans","erlang-interop","assertive-code"],"updated":null}
{"references":[],"source_sha256":"ad13a8d44008cd915bab2d5d572beaa320b66471bae98288ab254edb38e7da4c","source_start_line":12,"consequences":"## Consequences\n\n- The struct stays a flat map, so `%s{field: v}` updates reuse the shared key tuple and allocate only a new values tuple.\n- Many instances built inside one module share a single compile-time key tuple, cutting the memory cost of construction.\n- Optional fields nested under `:metadata` become matchable by presence rather than by a sentinel `nil`.\n- Grouping is a deliberate API tradeoff: frequently accessed fields stay top level, while cold or co-accessed fields move into nested maps, structs, or tuples.","rule_title":null,"routing_description":"When a defstruct approaches or exceeds 32 declared fields, nest optional, cold, or co-accessed fields so the struct stays a flat map.","record_id":"elixir-code-anti-patterns:adr-010","applies_to":{"content_match":["defstruct","__struct__","%__MODULE__{"]},"parent_id":null,"decision":"## Decision","retrieval_sha256":"88e58377277be9bdb76e83500360646d6a8366c984905b135539ed6df33a83c6","hydrate_id":"elixir-code-anti-patterns:adr-010","status":"accepted","retrieval_text":"Record: elixir-code-anti-patterns:adr-010\n\nDomain: elixir-code-anti-patterns\n\nADR: Structs with 32 fields or more\n\nRouting title: Structs with 32 fields or more\n\nRouting description: When a defstruct approaches or exceeds 32 declared fields, nest optional, cold, or co-accessed fields so the struct stays a flat map.\n\nSource description: A struct with 32 or more declared fields crosses the BEAM threshold from flat-map to hash-map representation, losing per-update key-tuple sharing and the compile-time key tuple shared across instances, which raises memory use. Keep declared fields under 32 by nesting optional or rarely-touched fields.\n\n## Context\n\nA struct is a compile-time map with a fixed set of keys plus a hidden `__struct__` metadata key. The BEAM has two internal map representations: a flat map and a hash map. A flat map is stored as two tuples, one holding the keys and one holding the values. A hash map (a HAMT) has a more elaborate node structure that scales to large key counts but does not share its key space.\n\nThe VM uses a flat map for up to 32 keys and a hash map above that. Because a struct carries the extra `__struct__` key, the practical ceiling is 31 declared fields. A struct at or above 32 declared fields crosses into hash-map representation, and two optimizations disappear at once. First, updating a flat map (`%s{field: v}`) reuses the existing key tuple and allocates only a new values tuple, so the keys are never recopied. Second, every instance of a given struct constructed inside the same module shares one key tuple fixed at compile time, so building many structs with `%MyStruct{...}` notation does not re-pay for the keys each time. A hash-map struct loses both: keys are no longer shared on update, and instances no longer share a compile-time key tuple.\n\nThe fix is structural, not cosmetic. Keep the declared field count under 32 by grouping fields into nested data so the top-level struct stays a flat map.\n\n## Decision\n\n## Consequences\n\n- The struct stays a flat map, so `%s{field: v}` updates reuse the shared key tuple and allocate only a new values tuple.\n- Many instances built inside one module share a single compile-time key tuple, cutting the memory cost of construction.\n- Optional fields nested under `:metadata` become matchable by presence rather than by a sentinel `nil`.\n- Grouping is a deliberate API tradeoff: frequently accessed fields stay top level, while cold or co-accessed fields move into nested maps, structs, or tuples.\n\nRule headings:\n### Rule 1: Keep a struct under 32 declared fields","tags":["elixir","anti-pattern","structs","memory","performance","data-modeling"],"rule_number":null,"adr_title":"Structs with 32 fields or more","languages":[],"source_description":"A struct with 32 or more declared fields crosses the BEAM threshold from flat-map to hash-map representation, losing per-update key-tuple sharing and the compile-time key tuple shared across instances, which raises memory use. Keep declared fields under 32 by nesting optional or rarely-touched fields.","ordinal":55,"polarity":null,"updated":null,"source_path":"adrs/elixir-code-anti-patterns/adr-010-structs-with-32-fields-or-more.md","schema_version":"retrieval-v1","record_kind":"adr_summary","heading_path":["ADR-010: Structs with 32 fields or more"],"context":"## Context\n\nA struct is a compile-time map with a fixed set of keys plus a hidden `__struct__` metadata key. The BEAM has two internal map representations: a flat map and a hash map. A flat map is stored as two tuples, one holding the keys and one holding the values. A hash map (a HAMT) has a more elaborate node structure that scales to large key counts but does not share its key space.\n\nThe VM uses a flat map for up to 32 keys and a hash map above that. Because a struct carries the extra `__struct__` key, the practical ceiling is 31 declared fields. A struct at or above 32 declared fields crosses into hash-map representation, and two optimizations disappear at once. First, updating a flat map (`%s{field: v}`) reuses the existing key tuple and allocates only a new values tuple, so the keys are never recopied. Second, every instance of a given struct constructed inside the same module shares one key tuple fixed at compile time, so building many structs with `%MyStruct{...}` notation does not re-pay for the keys each time. A hash-map struct loses both: keys are no longer shared on update, and instances no longer share a compile-time key tuple.\n\nThe fix is structural, not cosmetic. Keep the declared field count under 32 by grouping fields into nested data so the top-level struct stays a flat map.","source_end_line":74,"domain":"elixir-code-anti-patterns","date":"2026-06-28","adr_id":10,"routing_title":"Structs with 32 fields or more","display_text":"## Context\n\nA struct is a compile-time map with a fixed set of keys plus a hidden `__struct__` metadata key. The BEAM has two internal map representations: a flat map and a hash map. A flat map is stored as two tuples, one holding the keys and one holding the values. A hash map (a HAMT) has a more elaborate node structure that scales to large key counts but does not share its key space.\n\nThe VM uses a flat map for up to 32 keys and a hash map above that. Because a struct carries the extra `__struct__` key, the practical ceiling is 31 declared fields. A struct at or above 32 declared fields crosses into hash-map representation, and two optimizations disappear at once. First, updating a flat map (`%s{field: v}`) reuses the existing key tuple and allocates only a new values tuple, so the keys are never recopied. Second, every instance of a given struct constructed inside the same module shares one key tuple fixed at compile time, so building many structs with `%MyStruct{...}` notation does not re-pay for the keys each time. A hash-map struct loses both: keys are no longer shared on update, and instances no longer share a compile-time key tuple.\n\nThe fix is structural, not cosmetic. Keep the declared field count under 32 by grouping fields into nested data so the top-level struct stays a flat map.\n\n## Decision\n\n## Consequences\n\n- The struct stays a flat map, so `%s{field: v}` updates reuse the shared key tuple and allocate only a new values tuple.\n- Many instances built inside one module share a single compile-time key tuple, cutting the memory cost of construction.\n- Optional fields nested under `:metadata` become matchable by presence rather than by a sentinel `nil`.\n- Grouping is a deliberate API tradeoff: frequently accessed fields stay top level, while cold or co-accessed fields move into nested maps, structs, or tuples."}
{"adr_id":10,"adr_title":"Structs with 32 fields or more","applies_to":{"content_match":["defstruct","__struct__","%__MODULE__{"]},"date":"2026-06-28","display_text":"### Rule 1: Keep a struct under 32 declared fields\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 8 declared fields: stays a flat map.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    profile: %{},\n    billing: %{},\n    preferences: %{},\n    metadata: %{}\n  ]\nend\n```\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 35 declared fields: forced into hash-map representation.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    :phone,\n    :avatar_url,\n    :timezone,\n    :locale,\n    :billing_street,\n    :billing_city,\n    :billing_zip,\n    :marketing_opt_in,\n    :last_login_at\n    # ...and 22 more fields (35 total)\n  ]\nend\n```\n\n**Why:** At 35 declared fields the VM represents `%MyApp.Account{}` as a hash map. Updating it (`%{account | status: :active}`) no longer shares the key tuple, so each update copies keys as well as values, and the many account structs built inside one module no longer share a single compile-time key tuple. The flat-map version keeps both forms of sharing. Three techniques bring the count back under the threshold: nest optional fields (those initialized to `nil`) under a `:metadata` or `:optionals` key, which also lets you pattern match on presence instead of probing for `nil`; nest rarely read or written fields into a nested struct; and collapse fields that are always accessed together (`billing_street`, `billing_city`, `billing_zip`) into a tuple or composite. Balance the grouping against ergonomics: fields that are read and written frequently are poor candidates for nesting.","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Keep a struct under 32 declared fields"],"hydrate_id":"elixir-code-anti-patterns:adr-010:rule-01","languages":["elixir"],"ordinal":56,"parent_id":"elixir-code-anti-patterns:adr-010","polarity":null,"record_id":"elixir-code-anti-patterns:adr-010:rule-01","record_kind":"rule","references":[],"retrieval_sha256":"016c00706f9d8c190d51d98cd156d2b337660eaf9889d998fa0a2d815344a902","retrieval_text":"Record: elixir-code-anti-patterns:adr-010:rule-01\n\nDomain: elixir-code-anti-patterns\n\nADR: Structs with 32 fields or more\n\n### Rule 1: Keep a struct under 32 declared fields\n\nWhy: At 35 declared fields the VM represents `%MyApp.Account{}` as a hash map. Updating it (`%{account | status: :active}`) no longer shares the key tuple, so each update copies keys as well as values, and the many account structs built inside one module no longer share a single compile-time key tuple. The flat-map version keeps both forms of sharing. Three techniques bring the count back under the threshold: nest optional fields (those initialized to `nil`) under a `:metadata` or `:optionals` key, which also lets you pattern match on presence instead of probing for `nil`; nest rarely read or written fields into a nested struct; and collapse fields that are always accessed together (`billing_street`, `billing_city`, `billing_zip`) into a tuple or composite. Balance the grouping against ergonomics: fields that are read and written frequently are poor candidates for nesting.","routing_description":"When a defstruct approaches or exceeds 32 declared fields, nest optional, cold, or co-accessed fields so the struct stays a flat map.","routing_title":"Structs with 32 fields or more","rule_number":1,"rule_title":"Keep a struct under 32 declared fields","schema_version":"retrieval-v1","source_description":"A struct with 32 or more declared fields crosses the BEAM threshold from flat-map to hash-map representation, losing per-update key-tuple sharing and the compile-time key tuple shared across instances, which raises memory use. Keep declared fields under 32 by nesting optional or rarely-touched fields.","source_end_line":67,"source_path":"adrs/elixir-code-anti-patterns/adr-010-structs-with-32-fields-or-more.md","source_sha256":"6bacde531c5600b95308945ce0e7b0b932c9cd24c26e9335ebf338774953004f","source_start_line":22,"status":"accepted","tags":["elixir","anti-pattern","structs","memory","performance","data-modeling"],"updated":null}
{"adr_id":10,"adr_title":"Structs with 32 fields or more","applies_to":{"content_match":["defstruct","__struct__","%__MODULE__{"]},"date":"2026-06-28","display_text":"**Correct:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 8 declared fields: stays a flat map.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    profile: %{},\n    billing: %{},\n    preferences: %{},\n    metadata: %{}\n  ]\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Keep a struct under 32 declared fields","**Correct:**"],"hydrate_id":"elixir-code-anti-patterns:adr-010:rule-01","languages":["elixir"],"ordinal":57,"parent_id":"elixir-code-anti-patterns:adr-010:rule-01","polarity":"positive","record_id":"elixir-code-anti-patterns:adr-010:rule-01:example:correct:01","record_kind":"example","references":[],"retrieval_sha256":"888cdca45d18bf7a58d72b8c9be6b8787c5df5e7dcb27eced99eb792625f0034","retrieval_text":"Record: elixir-code-anti-patterns:adr-010:rule-01:example:correct:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Structs with 32 fields or more\n\nRouting title: Structs with 32 fields or more\n\nRule 1: Keep a struct under 32 declared fields\n\nCorrect example (positive)\n\n**Correct:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 8 declared fields: stays a flat map.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    profile: %{},\n    billing: %{},\n    preferences: %{},\n    metadata: %{}\n  ]\nend\n```\n\nWhy: At 35 declared fields the VM represents `%MyApp.Account{}` as a hash map. Updating it (`%{account | status: :active}`) no longer shares the key tuple, so each update copies keys as well as values, and the many account structs built inside one module no longer share a single compile-time key tuple. The flat-map version keeps both forms of sharing. Three techniques bring the count back under the threshold: nest optional fields (those initialized to `nil`) under a `:metadata` or `:optionals` key, which also lets you pattern match on presence instead of probing for `nil`; nest rarely read or written fields into a nested struct; and collapse fields that are always accessed together (`billing_street`, `billing_city`, `billing_zip`) into a tuple or composite. Balance the grouping against ergonomics: fields that are read and written frequently are poor candidates for nesting.","routing_description":"When a defstruct approaches or exceeds 32 declared fields, nest optional, cold, or co-accessed fields so the struct stays a flat map.","routing_title":"Structs with 32 fields or more","rule_number":1,"rule_title":"Keep a struct under 32 declared fields","schema_version":"retrieval-v1","source_description":"A struct with 32 or more declared fields crosses the BEAM threshold from flat-map to hash-map representation, losing per-update key-tuple sharing and the compile-time key tuple shared across instances, which raises memory use. Keep declared fields under 32 by nesting optional or rarely-touched fields.","source_end_line":41,"source_path":"adrs/elixir-code-anti-patterns/adr-010-structs-with-32-fields-or-more.md","source_sha256":"5e5860b4bbfc3226cbe8784223f37d0c27766dccfb9d38310972184410cb7a28","source_start_line":24,"status":"accepted","tags":["elixir","anti-pattern","structs","memory","performance","data-modeling"],"updated":null}
{"adr_id":10,"adr_title":"Structs with 32 fields or more","applies_to":{"content_match":["defstruct","__struct__","%__MODULE__{"]},"date":"2026-06-28","display_text":"**Wrong:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 35 declared fields: forced into hash-map representation.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    :phone,\n    :avatar_url,\n    :timezone,\n    :locale,\n    :billing_street,\n    :billing_city,\n    :billing_zip,\n    :marketing_opt_in,\n    :last_login_at\n    # ...and 22 more fields (35 total)\n  ]\nend\n```","domain":"elixir-code-anti-patterns","heading_path":["Decision","Rule 1: Keep a struct under 32 declared fields","**Wrong:**"],"hydrate_id":"elixir-code-anti-patterns:adr-010:rule-01","languages":["elixir"],"ordinal":58,"parent_id":"elixir-code-anti-patterns:adr-010:rule-01","polarity":"negative","record_id":"elixir-code-anti-patterns:adr-010:rule-01:example:wrong:01","record_kind":"example","references":[],"retrieval_sha256":"e413fabd5b747db2300ad42d0619edbe29a5518e84b55046c84a5150402e7a47","retrieval_text":"Record: elixir-code-anti-patterns:adr-010:rule-01:example:wrong:01\n\nDomain: elixir-code-anti-patterns\n\nADR: Structs with 32 fields or more\n\nRouting title: Structs with 32 fields or more\n\nRule 1: Keep a struct under 32 declared fields\n\nWrong example (negative)\n\n**Wrong:**\n\n```elixir\ndefmodule MyApp.Account do\n  # 35 declared fields: forced into hash-map representation.\n  defstruct [\n    :id,\n    :email,\n    :name,\n    :status,\n    :phone,\n    :avatar_url,\n    :timezone,\n    :locale,\n    :billing_street,\n    :billing_city,\n    :billing_zip,\n    :marketing_opt_in,\n    :last_login_at\n    # ...and 22 more fields (35 total)\n  ]\nend\n```\n\nWhy: At 35 declared fields the VM represents `%MyApp.Account{}` as a hash map. Updating it (`%{account | status: :active}`) no longer shares the key tuple, so each update copies keys as well as values, and the many account structs built inside one module no longer share a single compile-time key tuple. The flat-map version keeps both forms of sharing. Three techniques bring the count back under the threshold: nest optional fields (those initialized to `nil`) under a `:metadata` or `:optionals` key, which also lets you pattern match on presence instead of probing for `nil`; nest rarely read or written fields into a nested struct; and collapse fields that are always accessed together (`billing_street`, `billing_city`, `billing_zip`) into a tuple or composite. Balance the grouping against ergonomics: fields that are read and written frequently are poor candidates for nesting.","routing_description":"When a defstruct approaches or exceeds 32 declared fields, nest optional, cold, or co-accessed fields so the struct stays a flat map.","routing_title":"Structs with 32 fields or more","rule_number":1,"rule_title":"Keep a struct under 32 declared fields","schema_version":"retrieval-v1","source_description":"A struct with 32 or more declared fields crosses the BEAM threshold from flat-map to hash-map representation, losing per-update key-tuple sharing and the compile-time key tuple shared across instances, which raises memory use. Keep declared fields under 32 by nesting optional or rarely-touched fields.","source_end_line":65,"source_path":"adrs/elixir-code-anti-patterns/adr-010-structs-with-32-fields-or-more.md","source_sha256":"ab18db826b431c67b3212f8001e48fa2a732299e9cf35fc9063f95a36f94a6fb","source_start_line":42,"status":"accepted","tags":["elixir","anti-pattern","structs","memory","performance","data-modeling"],"updated":null}
